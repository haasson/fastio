// Public barrel of the ai-assistant module.
// server/ai/* остаётся на месте (per plan).

// Composables
export * from './composables/useAiChat'

// components/ — AiChat (импорт через ~/features/ai-assistant/components/AiChat.vue).
