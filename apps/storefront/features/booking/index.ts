// Public barrel of the booking module.
// Бронь стола в ресторане (retail). Использует /api/reservations endpoints.
// Vue-компоненты импортятся deep-path (~/features/booking/components/<X>.vue).

export { default as useBooking } from './composables/useBooking'
