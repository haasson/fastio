// Public barrel of the appointments module.
// Запись на услуги: booking-flow (выбор слота/мастера/контакты) + личный кабинет (история визитов).
//
// Vue-компоненты импортятся deep-path (~/features/appointments/components/<X>.vue).
// Если потребуются shared utils — выноси отсюда в barrel.

export {}
