/** Дефолтная длительность триала. Используется как fallback если billing_config недоступен. */
export const DEFAULT_TRIAL_DAYS = 14

/** Дефолтный цвет для нового филиала. */
export const DEFAULT_BRANCH_COLOR = '#6366f1'
