-- Старт shift-цикла теперь хранится на ресурсе, а не на шаблоне.
-- День 1 шаблона = cycle_start_date. Цикл повторяется по кругу бесконечно
-- вперёд и назад (вычисляется лениво на любой день, без материализации).

ALTER TABLE resources
  ADD COLUMN cycle_start_date date;

-- Если applied_template_id указывает на shift-шаблон, должен быть и cycle_start_date.
-- Делаем мягкий CHECK, не блокируя существующие записи: NULL допустим, проверка
-- корректности связки ложится на приложение (проверим в коде applyShift).
