{
  "tasks": {
    "test": "deno test",
    "test:coverage": "deno test --coverage=coverage && deno coverage coverage"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "lint": {
    "exclude": ["**/_shared/**"]
  }
}
