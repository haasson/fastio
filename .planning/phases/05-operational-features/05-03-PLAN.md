---
phase: 05-operational-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/deferred/OPS-01-transactional-email-PLAN.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [OPS-01]
tags: [backlog, deferral, planning-only]

must_haves:
  truths:
    - "OPS-01 is recorded as DEFERRED in REQUIREMENTS.md with a backlog reference, not silently dropped"
    - "A future agent picking up OPS-01 from the backlog has a complete spec: blocker (checkout email field), implementation outline (Edge Function + Resend or SMTP), and acceptance criteria"
    - "ROADMAP.md Phase 5 entry notes that OPS-01 was deferred per D-01 and the spec lives in .planning/deferred/"
    - "The OPS-01 spec file is committed alongside the existing .planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md — same directory, same shape"
  artifacts:
    - path: ".planning/deferred/OPS-01-transactional-email-PLAN.md"
      provides: "Standalone backlog spec for OPS-01 transactional email — blocker, design, acceptance criteria"
      contains: "OPS-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability — OPS-01 marked Deferred (not just Pending) with link to backlog spec"
      contains: "Deferred"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 entry references the deferral and points to the backlog spec"
      contains: "OPS-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md OPS-01 row"
      to: ".planning/deferred/OPS-01-transactional-email-PLAN.md"
      via: "link in Status column or notes section"
      pattern: "OPS-01-transactional-email-PLAN"
    - from: ".planning/ROADMAP.md Phase 5 entry"
      to: ".planning/deferred/OPS-01-transactional-email-PLAN.md"
      via: "footnote or inline reference, mirroring SEC-04 deferral pattern (ROADMAP.md line 42)"
      pattern: "OPS-01-transactional-email-PLAN"
---

<objective>
Record the OPS-01 deferral as a real artifact instead of an undocumented omission. Per CONTEXT.md decision D-01, OPS-01 (transactional email after order placement) is not implementable in Phase 5 because the prerequisite — a `customer_email` field collected at checkout — does not exist yet. This plan moves OPS-01 into the project's existing `.planning/deferred/` directory (already populated by SEC-04, per ROADMAP.md line 42) and updates REQUIREMENTS.md + ROADMAP.md to reference the deferral.

No production code is modified. This is a planning-only artifact that prevents OPS-01 from being silently dropped between phases.

Purpose: Honor the locked decision D-01 (CONTEXT.md) without losing the requirement. When the project later adds a `customer_email` checkout field (likely v2), a developer can pick up OPS-01 from `.planning/deferred/` with a complete spec and ship it without re-discovering the missing dependency.

Output:
- New file `.planning/deferred/OPS-01-transactional-email-PLAN.md` — complete backlog spec.
- Modified `.planning/REQUIREMENTS.md` — OPS-01 row updated to reflect deferral.
- Modified `.planning/ROADMAP.md` — Phase 5 entry updated.
</objective>

## Phase Goal

**As a** future maintainer who is asked to ship transactional email, **I want to** find a complete backlog spec for OPS-01 (blocker, design, acceptance criteria) in `.planning/deferred/`, **so that** I can ship the feature without re-doing the discovery work the Phase 5 discuss-phase session already produced.

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-features/05-CONTEXT.md
@.planning/phases/05-operational-features/05-RESEARCH.md
@.planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md

<interfaces>
<!-- Existing deferral precedent in the project — use as the structural template. -->

Existing deferral file: `.planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md`
- Same directory, same naming pattern (`{REQ-ID}-{slug}-PLAN.md`).
- Referenced from ROADMAP.md line 42 with a phrase like "Перенесено … план … вынесен в `.planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md`".

ROADMAP.md current Phase 5 entry (lines 138–150):
- Header `### Phase 5: Operational Features`
- Requirements line: `**Requirements**: OPS-01, OPS-02, OPS-03`
- Three success criteria, the first of which is the transactional email criterion being deferred.

REQUIREMENTS.md OPS-01 row (line 46) and traceability table (line 102):
- Line 46: `- [ ] **OPS-01**: Транзакционный email — …`
- Line 102 in traceability table: `| OPS-01 | Phase 5 | Pending |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/deferred/OPS-01-transactional-email-PLAN.md (backlog spec)</name>
  <files>.planning/deferred/OPS-01-transactional-email-PLAN.md</files>
  <read_first>
    - .planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md (structural template — frontmatter, sections, level of detail)
    - .planning/phases/05-operational-features/05-CONTEXT.md (D-01 — the locked deferral decision plus the deferred-ideas section explaining the blocker)
    - .planning/phases/05-operational-features/05-RESEARCH.md (the user_constraints and phase_requirements sections that document the deferral rationale)
    - .planning/REQUIREMENTS.md (line 46 — the original OPS-01 requirement text)
  </read_first>
  <action>
    Create `.planning/deferred/OPS-01-transactional-email-PLAN.md` using the same structural conventions as the sibling `SEC-04-staging-supabase-e2e-PLAN.md`. The file MUST include these top-level sections (in this order):

    1. Frontmatter — minimum keys: `requirement: OPS-01`, `deferred_from_phase: 05-operational-features`, `deferred_date: <YYYY-MM-DD>`, `decision_ref: D-01 (05-CONTEXT.md)`, `blocker: customer_email field at checkout`.

    2. `# OPS-01 — Транзакционный email (DEFERRED)` heading.

    3. `## Original Requirement` — copy the exact text of OPS-01 from REQUIREMENTS.md line 46 verbatim.

    4. `## Why Deferred` — explicit single-paragraph statement: customer_email is not collected at checkout today; sending email requires a recipient address; therefore email confirmation is impossible without a precursor change to the checkout form. Cite CONTEXT.md D-01 by line/section.

    5. `## Prerequisite Work` — a numbered list of the minimum changes needed before OPS-01 can be started. At least these items:
       1. Add an optional `customer_email` text field to the storefront checkout form (component path TBD by the future implementer — likely under `apps/storefront/features/checkout/`).
       2. Add the email column to the `orders` table via a Supabase migration (or confirm it already exists in the schema — verify before planning).
       3. Surface the email field in the admin order detail view for support traceability.

    6. `## Implementation Outline (when un-deferred)` — bulleted high-level approach. Include:
       - Trigger: Postgres database webhook on `orders` INSERT (asynchronous — does not block order creation).
       - Sender: Edge Function under `supabase/functions/send-order-email/index.ts` (Deno runtime, project pattern).
       - Email provider: Resend (preferred — already mentioned in STATE.md Research Flags as a candidate, API key availability flagged unconfirmed) OR self-hosted SMTP via the existing FastIO GoTrue / email-template infra in `apps/ops/`.
       - Template: HTML email with the order summary (items, totals, payment, delivery info) and a link to `/order/{id}?t={guest_token}` (the same URL OPS-02 already uses).
       - Idempotency: use the `processed_webhook_events` table (already exists per TECHDEBT.md) to guard against duplicate sends from webhook retries.

    7. `## Acceptance Criteria` — checklist mirroring ROADMAP.md Phase 5 criterion 1 plus implementation invariants:
       - [ ] A customer who provides email at checkout receives a transactional email within 2 minutes containing the order summary and a link to /order/{id}?t={token}.
       - [ ] The email send is idempotent — webhook retries do not produce duplicate emails.
       - [ ] Failure to send (provider 5xx, rate-limit) is captured in Sentry/GlitchTip with the order id as a tag and does NOT prevent the order from being created.
       - [ ] If no customer_email is provided at checkout, no email is attempted and no error is logged (silent skip is correct).

    8. `## Out of Scope for OPS-01` — explicitly list things that are NOT in this requirement to prevent scope creep: SMS, push notifications, marketing emails, multi-language templates beyond Russian, attachments.

    9. `## Risk if Never Shipped` — one sentence: "Customers must rely on the /order/{id} link they were redirected to immediately after checkout (OPS-02) — link loss = order loss visibility for the customer (the venue still has the order)."

    10. `## References` — link back to:
        - `.planning/phases/05-operational-features/05-CONTEXT.md` (D-01)
        - `.planning/phases/05-operational-features/05-RESEARCH.md` (user_constraints + phase_requirements)
        - `.planning/STATE.md` (Research Flag: "Resend API key availability unconfirmed")

    DO NOT include implementation code blocks beyond identifier names (paths, function names, table column names). The spec is a future-work outline, not an executable plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/deferred/OPS-01-transactional-email-PLAN.md'; if(!fs.existsSync(p)){console.error('OPS-01 deferral spec missing');process.exit(1)} const c=fs.readFileSync(p,'utf8'); const required=['requirement: OPS-01','blocker: customer_email','## Original Requirement','## Why Deferred','## Prerequisite Work','## Implementation Outline','## Acceptance Criteria','## Out of Scope','## References','customer_email','/order/','guest_token','processed_webhook_events','Resend']; const missing=required.filter(s=&gt;!c.includes(s)); if(missing.length){console.error('Missing sections/keywords:',missing);process.exit(1)} console.log('OPS-01 deferral spec OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/deferred/OPS-01-transactional-email-PLAN.md`
    - File frontmatter contains `requirement: OPS-01`, `deferred_from_phase: 05-operational-features`, `decision_ref: D-01`, and `blocker: customer_email field at checkout`
    - File contains all required section headings: `## Original Requirement`, `## Why Deferred`, `## Prerequisite Work`, `## Implementation Outline`, `## Acceptance Criteria`, `## Out of Scope`, `## Risk if Never Shipped`, `## References`
    - File mentions `customer_email`, `/order/`, `guest_token`, `processed_webhook_events`, and `Resend` (the candidate provider)
    - File does NOT contain executable production code (no `import …`, no function bodies — only identifier names and paths)
    - File path matches sibling `.planning/deferred/SEC-04-staging-supabase-e2e-PLAN.md` naming pattern: `{REQ-ID}-{kebab-slug}-PLAN.md`
  </acceptance_criteria>
  <done>
    Backlog spec exists at `.planning/deferred/OPS-01-transactional-email-PLAN.md` and contains all sections enumerated in the action.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md to reflect the deferral</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find OPS-01 entry on line 46 and the traceability table row near line 102)
    - .planning/ROADMAP.md (full file — find Phase 5 entry at lines 138–150 and study the SEC-04 deferral footnote at line 42 as the precedent format)
    - .planning/deferred/OPS-01-transactional-email-PLAN.md (the file Task 1 created — needs to be linked from the two updated files)
  </read_first>
  <action>
    Two targeted edits, no other changes:

    A) `.planning/REQUIREMENTS.md`:
       - Line 46 (currently `- [ ] **OPS-01**: Транзакционный email — …`): keep the checkbox unchecked and append the deferral suffix at the end of the same line: `**[DEFERRED → .planning/deferred/OPS-01-transactional-email-PLAN.md, см. Phase 5 D-01]**`.
       - Traceability table row near line 102 (currently `| OPS-01 | Phase 5 | Pending |`): change the Status cell from `Pending` to `Deferred → backlog`.

    B) `.planning/ROADMAP.md`:
       - Phase 5 entry (header `### Phase 5: Operational Features`, lines 138–150): in the `**Requirements**:` line, do NOT remove OPS-01 from the list (keep the historical record).
       - Immediately after the success criteria block (after the third success-criterion bullet, before the `**Plans**: TBD` line — or wherever the current SEC-04 footnote pattern places it in Phase 1), insert a new footnote-style paragraph mirroring the SEC-04 line 42 format:
         `> **Отложено 2026-05-23:** OPS-01 (транзакционный email) перенесён в backlog — `customer_email` не собирается на checkout, реализация невозможна без отдельной задачи. Готовая спека: `.planning/deferred/OPS-01-transactional-email-PLAN.md`. См. D-01 в `.planning/phases/05-operational-features/05-CONTEXT.md`.`
       - Update the first success criterion of Phase 5 to add a parenthetical: `(DEFERRED — see footnote)` immediately after the criterion text, so a reader scanning the criteria sees the deferral inline.

    Do NOT modify any other entries in either file. Do NOT touch the `## v2 Requirements` or `## Out of Scope` sections in REQUIREMENTS.md — OPS-01 belongs in v1 and is marked Deferred, not moved to v2 (because the requirement is still in scope for Launch Readiness; only this phase cannot deliver it).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const m=fs.readFileSync('.planning/ROADMAP.md','utf8'); if(!/OPS-01.*\[DEFERRED.*OPS-01-transactional-email-PLAN\.md/.test(r)){console.error('REQUIREMENTS.md OPS-01 line missing deferral marker');process.exit(1)} if(!/\| OPS-01 \| Phase 5 \| Deferred/.test(r)){console.error('REQUIREMENTS.md traceability row not updated');process.exit(1)} if(!/Отложено.*OPS-01.*OPS-01-transactional-email-PLAN\.md/s.test(m)){console.error('ROADMAP.md missing OPS-01 deferral footnote');process.exit(1)} if(!/DEFERRED.*see footnote/.test(m)){console.error('ROADMAP.md success criterion not annotated');process.exit(1)} console.log('REQUIREMENTS + ROADMAP updates OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` OPS-01 entry on the original line contains the literal substring `[DEFERRED → .planning/deferred/OPS-01-transactional-email-PLAN.md`
    - `.planning/REQUIREMENTS.md` traceability table row for OPS-01 reads `| OPS-01 | Phase 5 | Deferred → backlog |` (or equivalent — must contain `Deferred`, not `Pending`)
    - `.planning/ROADMAP.md` Phase 5 entry contains a `> **Отложено 2026-05-23:**` footnote line that names `OPS-01` and links to `.planning/deferred/OPS-01-transactional-email-PLAN.md`
    - `.planning/ROADMAP.md` Phase 5 first success criterion (transactional email) is annotated with `(DEFERRED — see footnote)` inline
    - `.planning/ROADMAP.md` Phase 5 `**Requirements**:` line still lists `OPS-01, OPS-02, OPS-03` (the historical mapping is preserved)
    - No other lines in either file are modified (a `git diff --stat` for these two files shows only the additions described above)
    - OPS-01 is NOT moved into the `## v2 Requirements` section of REQUIREMENTS.md (it stays in v1, deferred — not re-categorized)
  </acceptance_criteria>
  <done>
    Both planning files updated. OPS-01 is now traceably deferred with a footnote pointing to the backlog spec file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner / future maintainer → planning docs | Edits to `.planning/` shape future scope; incorrect deferral could silently drop a v1 requirement |
| `.planning/deferred/` → future implementer | The spec is the only memory of OPS-01 once Phase 5 closes; loss of the spec = loss of the requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Repudiation | Deferral without trace | mitigate | Three artifacts cross-link (deferred spec ⇄ REQUIREMENTS.md row ⇄ ROADMAP.md footnote). Removing any one is detectable by a single grep; removing all three would obviously be intentional scope reduction visible in git history. |
| T-05-03-02 | Information Disclosure | Spec mentions Resend (third-party) and API key concerns | accept | The spec only names Resend as a candidate; no actual API key is present. The "API key availability unconfirmed" flag from STATE.md is documented honestly. |
| T-05-03-03 | Tampering | Deferral spec is later edited to silently remove the OPS-01 acceptance criteria | mitigate | The spec is committed; git history is the audit trail. The `### Acceptance Criteria` section in the spec is mandatory and verified by Task 1 automated check. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No supply-chain surface area. |

## ASVS L1 Categories Touched

None directly — this plan modifies only `.planning/` documentation. The future OPS-01 work (when un-deferred) will need its own threat model covering V2 (Authentication around outbound email sending), V8 (Data Protection for storing customer_email), and V9 (Communications for the Resend or SMTP integration).
</threat_model>

<verification>
- `.planning/deferred/OPS-01-transactional-email-PLAN.md` exists and contains all required sections.
- `.planning/REQUIREMENTS.md` OPS-01 row + traceability table reflect the deferral.
- `.planning/ROADMAP.md` Phase 5 entry has the OPS-01 deferral footnote.
- `git diff apps/ packages/ supabase/` is empty (zero production code touched).
- `git diff .planning/` shows only the three expected changes.
</verification>

<success_criteria>
- OPS-01 has a complete backlog spec at `.planning/deferred/OPS-01-transactional-email-PLAN.md`.
- REQUIREMENTS.md and ROADMAP.md both reference the deferral with a link to the spec.
- A future agent searching for `OPS-01` finds: (a) the deferred spec file, (b) the REQUIREMENTS.md row with the deferral marker, and (c) the ROADMAP.md footnote — three independent breadcrumbs.
- No production code is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/05-operational-features/05-03-SUMMARY.md` when done. Record:
- The three files modified (deferred spec, REQUIREMENTS.md, ROADMAP.md).
- The deferral decision reference (D-01 in CONTEXT.md).
- The intended re-pickup signal — when a `customer_email` field is added to the storefront checkout, OPS-01 becomes implementable.
</output>
