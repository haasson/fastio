---
phase: 02-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glitchtip-deploy.md
autonomous: false
requirements: [OBS-01]
user_setup:
  - service: coolify
    why: "Deploy GlitchTip docker-compose stack (web/worker/postgres/redis) as a new Coolify resource"
    dashboard_config:
      - task: "Create docker-compose resource for GlitchTip on the existing VPS, set domain errors.fastio.ru, generate SECRET_KEY, set GLITCHTIP_DOMAIN + EMAIL_URL, deploy"
        location: "Coolify dashboard (coolify.fastio.ru) -> new resource -> Docker Compose"
      - task: "Create the first admin account via GlitchTip UI, then disable open registration (ENABLE_OPEN_USER_REGISTRATION=false) and redeploy"
        location: "https://errors.fastio.ru -> register, then Coolify env"
  - service: dns
    why: "errors.fastio.ru must resolve to the VPS; wildcard *.fastio.ru cert already covers it"
    dashboard_config:
      - task: "Add A record errors.fastio.ru -> 109.71.242.205"
        location: "Timeweb Cloud DNS zone fastio.ru"

must_haves:
  truths:
    - "GlitchTip web UI is reachable at https://errors.fastio.ru over HTTPS"
    - "An admin account exists and open registration is disabled"
    - "At least one GlitchTip project exists with a DSN that downstream plans can paste into Coolify env"
  artifacts:
    - path: "docs/glitchtip-deploy.md"
      provides: "Reproducible GlitchTip deployment runbook (compose stack, env, DNS, admin bootstrap, DSN extraction)"
      min_lines: 40
  key_links:
    - from: "errors.fastio.ru"
      to: "VPS 109.71.242.205"
      via: "DNS A record + Coolify Traefik wildcard cert"
      pattern: "errors\\.fastio\\.ru"
---

<objective>
Deploy a self-hosted GlitchTip instance on Coolify so the team has a Sentry-compatible error sink reachable at https://errors.fastio.ru. This is the foundation: OBS-01 SDK wiring (plan 02) and OBS-02 Telegram alerts (plan 03) both need a live GlitchTip DSN and project to exist first.

Purpose: Without a running ingest endpoint, no server error can be captured anywhere. Phase success criterion 1 ("error appears in GlitchTip within 60s") is unverifiable until this exists.
Output: A live GlitchTip instance, an admin account, at least one project with a DSN, and a runbook at docs/glitchtip-deploy.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability/02-CONTEXT.md

<notes>
- D-06: official recommended stack — separate containers web / worker / postgres / redis. GlitchTip's own Postgres, NOT shared with Supabase.
- D-07: same VPS (109.71.242.205), domain errors.fastio.ru, disable open registration after first admin account.
- Coolify wildcard cert for *.fastio.ru already exists — no per-host cert work. DNS zone fastio.ru is managed in Timeweb Cloud.
- Claude's Discretion: GlitchTip version = latest stable official image; docker-compose ports/volumes/SECRET_KEY values; project naming scheme.
- This project is code-written-by-agents; docs are agent-to-agent — write docs/glitchtip-deploy.md terse and directive, not stakeholder prose.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GlitchTip deployment runbook</name>
  <files>docs/glitchtip-deploy.md</files>
  <read_first>
    - .planning/phases/02-observability/02-CONTEXT.md (D-06, D-07 — stack and placement decisions)
    - .planning/codebase/INTEGRATIONS.md (existing Coolify/Timeweb deployment conventions, if present)
  </read_first>
  <action>
    Fetch the current official GlitchTip docker-compose recommendation from glitchtip.com/documentation (use WebFetch or Context7 for the latest stable compose file shape) and capture a concrete, runnable docker-compose for Coolify per D-06: services web, worker, postgres, redis, with GlitchTip's own postgres volume (NOT Supabase). Document required env: SECRET_KEY (generate via openssl rand), DATABASE_URL pointing at the bundled postgres, REDIS_URL, GLITCHTIP_DOMAIN=https://errors.fastio.ru, EMAIL_URL (use the existing Timeweb SMTP credentials referenced in MEMORY/INTEGRATIONS), ENABLE_OPEN_USER_REGISTRATION (true for bootstrap, then false). Document the DNS step (A record errors.fastio.ru -> 109.71.242.205 in Timeweb Cloud) and that the existing *.fastio.ru wildcard cert covers it. Document the admin-bootstrap sequence: deploy, register first account, then flip ENABLE_OPEN_USER_REGISTRATION=false and redeploy. Document where to read the project DSN after creating a project (Project Settings -> the DSN is what plan 02 pastes into NUXT_PUBLIC_SENTRY_DSN). Keep it directive and terse (agent-to-agent doc).
  </action>
  <verify>
    <automated>test -f docs/glitchtip-deploy.md && grep -q "errors.fastio.ru" docs/glitchtip-deploy.md && grep -q "ENABLE_OPEN_USER_REGISTRATION" docs/glitchtip-deploy.md && grep -qi "secret_key" docs/glitchtip-deploy.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/glitchtip-deploy.md exists and contains a complete docker-compose with web, worker, postgres, redis services
    - The runbook covers: SECRET_KEY generation, GlitchTip-owned postgres (not Supabase), DNS A record, admin bootstrap, disabling open registration, and where to find the project DSN
    - The verify command prints OK
  </acceptance_criteria>
  <done>A reader can follow docs/glitchtip-deploy.md end-to-end to stand up GlitchTip without external research.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The deployment runbook (docs/glitchtip-deploy.md) with full compose, env, DNS, and bootstrap steps. Coolify resource creation, DNS record creation, and GlitchTip admin-account creation cannot be done by Claude — there is no Coolify provisioning API/CLI configured in this repo, so the user performs the dashboard steps following the runbook.
  </what-built>
  <how-to-verify>
    1. In Timeweb Cloud DNS, add A record: errors.fastio.ru -> 109.71.242.205
    2. In Coolify (coolify.fastio.ru), create a Docker Compose resource using the compose from docs/glitchtip-deploy.md; set SECRET_KEY, GLITCHTIP_DOMAIN=https://errors.fastio.ru, EMAIL_URL, ENABLE_OPEN_USER_REGISTRATION=true; assign domain errors.fastio.ru; deploy.
    3. Open https://errors.fastio.ru — confirm it loads over HTTPS (wildcard cert) and shows the GlitchTip login/register screen.
    4. Register the first admin account.
    5. Set ENABLE_OPEN_USER_REGISTRATION=false in Coolify env and redeploy.
    6. Create a project (e.g. "fastio"); copy its DSN from Project Settings.
    Expected: HTTPS UI reachable, admin account created, registration closed, a project DSN in hand.
  </how-to-verify>
  <resume-signal>Paste the project DSN (or confirm it's saved for plan 02) and type "approved", or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- https://errors.fastio.ru returns the GlitchTip UI over valid HTTPS
- A project exists; its DSN is recorded for plan 02 to consume
- Open registration is disabled
</verification>

<success_criteria>
- GlitchTip is live and reachable at errors.fastio.ru
- docs/glitchtip-deploy.md is a complete, followable runbook
- A DSN exists for the admin and/or storefront projects, ready for plan 02
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> GlitchTip web UI | errors.fastio.ru is internet-reachable; auth + closed registration are the only gate |
| GlitchTip -> its own Postgres/Redis | internal docker network on the VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | GlitchTip open registration | mitigate | Disable ENABLE_OPEN_USER_REGISTRATION after first admin account (D-07); checkpoint verifies it |
| T-02-02 | Information Disclosure | GlitchTip Postgres shared with app data | mitigate | Dedicated GlitchTip-owned Postgres, isolated from Supabase (D-06) |
| T-02-03 | Spoofing | errors.fastio.ru TLS | mitigate | Served behind Coolify Traefik with existing *.fastio.ru wildcard cert; verify HTTPS at checkpoint |
| T-02-SC | Tampering | no npm/pip/cargo installs in this plan | accept | Pure infra + docs; no package installs, no legitimacy gate needed |
</threat_model>

<output>
Create `.planning/phases/02-observability/02-01-SUMMARY.md` when done. Record the project DSN(s) and the GlitchTip URL so plans 02 and 03 can consume them.
</output>
