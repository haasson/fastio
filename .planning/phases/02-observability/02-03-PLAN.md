---
phase: 02-observability
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/glitchtip-telegram-alerts.md
autonomous: false
requirements: [OBS-02]
user_setup:
  - service: glitchtip
    why: "Configure the native GlitchTip -> Telegram alert integration (zero code, UI config only)"
    dashboard_config:
      - task: "In Project Settings -> Alerts, add a Telegram/webhook alert that posts to the team ops channel on every new issue"
        location: "https://errors.fastio.ru -> Project -> Settings -> Alerts"
  - service: telegram
    why: "The ops bot must be able to post to the team alert chat"
    dashboard_config:
      - task: "Reuse the existing ops bot (NUXT_TELEGRAM_OPS_BOT_TOKEN) and alert chat (NUXT_TELEGRAM_ALERT_CHAT_ID); ensure the bot is a member of the chat"
        location: "Telegram + existing env (already configured for notify-alert)"

must_haves:
  truths:
    - "A new error event in GlitchTip produces a Telegram message in the team channel within 5 minutes"
    - "The existing threshold-based notify-alert Edge Function monitoring is left unchanged and runs independently"
  artifacts:
    - path: "docs/glitchtip-telegram-alerts.md"
      provides: "Runbook for the GlitchTip native Telegram alert integration (config steps, no code)"
      min_lines: 25
  key_links:
    - from: "GlitchTip new-issue event"
      to: "Telegram team channel"
      via: "native GlitchTip Telegram/webhook alert integration"
      pattern: "Telegram"
---

<objective>
Make GlitchTip errors visible in Telegram with zero code, using GlitchTip's native Telegram alert integration (D-04). After this plan, a new error event in GlitchTip lands as a Telegram message in the team channel within 5 minutes (phase success criterion 2).

Purpose: The team should not have to poll the GlitchTip UI. The native integration formats and sends the message itself — no Edge Function, no app code. The existing notify-alert.post.ts (threshold-based Edge Function monitoring) serves a different purpose and stays untouched (D-05).
Output: A configured GlitchTip Telegram alert and a runbook at docs/glitchtip-telegram-alerts.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability/02-CONTEXT.md
@.planning/phases/02-observability/02-01-SUMMARY.md

<notes>
- D-04: use GlitchTip native Telegram integration — config in Project Settings -> Alerts. Zero code. GlitchTip formats and sends on each new issue.
- D-05: existing apps/admin/server/api/telegram/notify-alert.post.ts stays as-is — different purpose (threshold-based Edge Function monitoring). Do NOT modify it. The two run in parallel and independently.
- Existing Telegram infra: NUXT_TELEGRAM_OPS_BOT_TOKEN, NUXT_TELEGRAM_ALERT_CHAT_ID (see .planning/codebase/INTEGRATIONS.md §Telegram Bot API). Reuse the ops bot + alert chat.
- Verify the exact mechanism GlitchTip 5.x exposes for Telegram: it may be a native Telegram integration or a generic webhook the team points at a Telegram-bot relay. Confirm against current GlitchTip docs before writing the runbook so the steps match the deployed version.
- Docs are agent-to-agent — terse, directive.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GlitchTip Telegram alert runbook</name>
  <files>docs/glitchtip-telegram-alerts.md</files>
  <read_first>
    - .planning/phases/02-observability/02-CONTEXT.md (D-04, D-05)
    - .planning/codebase/INTEGRATIONS.md (§Telegram Bot API — ops bot token + alert chat id env vars)
    - apps/admin/server/api/telegram/notify-alert.post.ts (read-only — to document that it stays separate and why)
  </read_first>
  <action>
    Confirm GlitchTip 5.x's current alert mechanism for Telegram (WebFetch glitchtip.com/documentation alerts, or the deployed instance's UI) — whether it is a first-class Telegram integration or a generic webhook to point at the existing ops bot. Write docs/glitchtip-telegram-alerts.md documenting: which env (NUXT_TELEGRAM_OPS_BOT_TOKEN, NUXT_TELEGRAM_ALERT_CHAT_ID) the team reuses; the exact GlitchTip UI path (Project -> Settings -> Alerts) to add a new-issue alert routed to Telegram; the expected message latency (<5 min, per success criterion 2); and an explicit note that notify-alert.post.ts is a separate threshold-based monitor that is NOT touched and runs in parallel (D-05). Keep it directive (agent-to-agent).
  </action>
  <verify>
    <automated>test -f docs/glitchtip-telegram-alerts.md && grep -qi "telegram" docs/glitchtip-telegram-alerts.md && grep -q "notify-alert" docs/glitchtip-telegram-alerts.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/glitchtip-telegram-alerts.md exists with the GlitchTip UI alert-config steps and the reused env var names
    - The doc explicitly states notify-alert.post.ts is untouched and independent (D-05)
    - The verify command prints OK
  </acceptance_criteria>
  <done>A reader can configure the GlitchTip Telegram alert from the doc alone, and knows the existing notify-alert monitor is separate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Telegram alert runbook. The GlitchTip alert configuration lives in the GlitchTip UI (Project Settings -> Alerts) and the end-to-end test requires a real error event — Claude cannot operate the GlitchTip UI or read the Telegram channel.
  </what-built>
  <how-to-verify>
    1. In GlitchTip (errors.fastio.ru), open the project -> Settings -> Alerts and add a new-issue alert routed to Telegram per docs/glitchtip-telegram-alerts.md (reusing the ops bot + alert chat).
    2. Trigger a fresh error (reuse the temporary throwing route from plan 02-02, or throw a new distinct error so GlitchTip treats it as a new issue).
    3. Confirm a Telegram message arrives in the team channel within 5 minutes describing the new issue.
    Expected: Telegram message in the team channel < 5 min after the new error.
  </how-to-verify>
  <resume-signal>Type "approved" once the Telegram alert arrives, or describe what failed (no message / wrong chat / delayed beyond 5 min).</resume-signal>
</task>

</tasks>

<verification>
- A new GlitchTip issue produces a Telegram message in the team channel within 5 minutes (phase success criterion 2)
- notify-alert.post.ts is unchanged (git diff shows no modification)
</verification>

<success_criteria>
- GlitchTip native Telegram alert configured and verified end-to-end
- Existing threshold-based monitor untouched (D-05)
- Runbook committed at docs/glitchtip-telegram-alerts.md
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GlitchTip -> Telegram Bot API | issue payloads (titles, possibly stack snippets) sent to a Telegram chat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | error titles posted to Telegram chat | accept | Private team chat; only the bot + team members; titles are low-sensitivity |
| T-02-08 | Spoofing | ops bot token reuse | mitigate | Reuse existing NUXT_TELEGRAM_OPS_BOT_TOKEN already scoped to the ops chat; no new token surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero code, no package installs in this plan — no legitimacy gate needed |
</threat_model>

<output>
Create `.planning/phases/02-observability/02-03-SUMMARY.md` when done.
</output>
