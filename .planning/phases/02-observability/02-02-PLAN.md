---
phase: 02-observability
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/storefront/nuxt.config.ts
  - apps/admin/nuxt.config.ts
  - apps/storefront/server/middleware/tenant.ts
  - apps/storefront/sentry.server.config.ts
  - apps/admin/sentry.server.config.ts
autonomous: false
requirements: [OBS-01]
user_setup:
  - service: coolify
    why: "Sentry DSN and source-map upload token must be set as env in Coolify for admin and storefront"
    env_vars:
      - name: NUXT_PUBLIC_SENTRY_DSN
        source: "GlitchTip Project Settings (DSN from plan 02-01)"
      - name: SENTRY_AUTH_TOKEN
        source: "GlitchTip -> Profile/Org -> Auth Tokens (for source-map upload at build)"

must_haves:
  truths:
    - "An intentionally thrown server-side error in a Nitro route appears in GlitchTip with a stack trace"
    - "Server-side errors from the storefront carry the tenant slug as a tag"
    - "The production start command runs Nitro with the Sentry --import loader (autoInstrument), so server-side Sentry actually initializes"
    - "Source maps are uploaded to GlitchTip on build so stack traces are de-minified"
  artifacts:
    - path: "apps/storefront/nuxt.config.ts"
      provides: "@sentry/nuxt module options: autoInstrument + autoUploadSourceMaps"
      contains: "sentry"
    - path: "apps/admin/nuxt.config.ts"
      provides: "@sentry/nuxt module options: autoInstrument + autoUploadSourceMaps"
      contains: "sentry"
    - path: "apps/storefront/server/middleware/tenant.ts"
      provides: "Sentry.setTag('tenant', tenant.slug) after successful resolve"
      contains: "setTag"
  key_links:
    - from: "apps/storefront/server/middleware/tenant.ts"
      to: "@sentry/nuxt setTag"
      via: "Sentry.setTag('tenant', tenant.slug) after applyTenantToContext"
      pattern: "setTag\\(['\"]tenant['\"]"
    - from: "apps/storefront/nuxt.config.ts"
      to: "GlitchTip source-map upload"
      via: "autoUploadSourceMaps + SENTRY_AUTH_TOKEN env"
      pattern: "autoUploadSourceMaps"
---

<objective>
Wire @sentry/nuxt so server-side errors actually reach the GlitchTip instance from plan 02-01, with the tenant slug tagged on every storefront error and source maps uploaded for readable stack traces. This is the OBS-01 vertical slice: after this plan, throwing an error in a Nitro route produces a tenant-tagged, de-minified issue in GlitchTip within 60 seconds (phase success criterion 1).

Purpose: The SDK is installed but server-side Sentry only initializes if Nitro is started with the `--import` loader. STATE.md Known Risk: "Sentry --import flag in Nitro start command is mandatory — without it, zero server-side errors are captured." D-02 solves this via the module's autoInstrument option.
Output: Configured nuxt.config.ts in both apps, tenant tag in storefront middleware, and Coolify env wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability/02-CONTEXT.md
@.planning/phases/02-observability/02-01-SUMMARY.md

@apps/storefront/sentry.server.config.ts
@apps/admin/sentry.server.config.ts
@apps/storefront/server/middleware/tenant.ts

<interfaces>
From apps/storefront/sentry.server.config.ts (and admin — identical shape):
```ts
import * as Sentry from '@sentry/nuxt'
const dsn = process.env.NUXT_PUBLIC_SENTRY_DSN || process.env.SENTRY_DSN
if (!import.meta.dev && dsn) {
  Sentry.init({ dsn, tracesSampleRate: /* env or */ 0.01 })
}
```

From apps/storefront/server/middleware/tenant.ts — tenant is applied here:
```ts
function applyTenantToContext(event: H3Event, tenant: Tenant): void {
  event.context.tenantId = tenant.id
  event.context.tenant = tenant
}
// called in the main handler right after a tenant is resolved (cache or fresh).
// tenant.slug is available on the Tenant type.
```

Both apps already register the module: `modules: [..., '@sentry/nuxt/module']`.
Both already expose `runtimeConfig.public.sentryDsn` (env NUXT_PUBLIC_SENTRY_DSN).
</interfaces>

<notes>
- D-01: tenant tag goes in apps/storefront/server/middleware/tenant.ts AFTER successful resolve. Do NOT add per-catch-block tagging — this single setTag covers all server errors for the request. (Admin is a client SPA; tenant tagging there is out of scope for D-01 — only storefront server middleware.)
- D-02: use @sentry/nuxt module-level options in nuxt.config.ts — autoInstrument (adds --import to the prod start command via Node loader) + autoUploadSourceMaps: true. SENTRY_AUTH_TOKEN goes in Coolify env (build-time, for upload).
- D-03: SDK already ^10.47.0, sentry.server/client.config.ts already exist. Only DSN + module options to add — NO new npm packages.
- Claude's Discretion: leave tracesSampleRate at 0.01 (1%) in sentry.server.config.ts — do not change it.
- Verify @sentry/nuxt 10.x module option names against current docs (Context7 or sentry.io docs) before editing — confirm exact keys for autoInstrument and source-map upload (the option may live under `sourceMapsUploadOptions` / `autoUploadSourceMaps` depending on version).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sentry module options to both nuxt.config.ts</name>
  <files>apps/storefront/nuxt.config.ts, apps/admin/nuxt.config.ts</files>
  <read_first>
    - apps/storefront/nuxt.config.ts (current module list + runtimeConfig.public.sentryDsn)
    - apps/admin/nuxt.config.ts (same, plus the isDev ssr toggle — do not break it)
    - .planning/phases/02-observability/02-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
    Verify the exact @sentry/nuxt 10.x option keys via Context7 (resolve-library-id @sentry/nuxt, then query "nuxt module options autoInstrument source maps upload"). Add a `sentry` config block to both nuxt.config.ts enabling autoInstrument (so the prod start command gets the Node --import loader — solves the STATE.md Known Risk) and source-map upload to GlitchTip (autoUploadSourceMaps / sourceMapsUploadOptions per the confirmed key) keyed off SENTRY_AUTH_TOKEN. Point the source-map upload URL at the self-hosted GlitchTip instance (errors.fastio.ru), not sentry.io — GlitchTip is Sentry-API-compatible; set the url/org/project options the module needs for a self-hosted endpoint. Do NOT touch the admin isDev ssr logic or the existing runtimeConfig blocks beyond adding the sentry section. Per D-03, add no new dependencies.
  </action>
  <verify>
    <automated>grep -q "sentry" apps/storefront/nuxt.config.ts && grep -q "sentry" apps/admin/nuxt.config.ts && grep -qi "autoInstrument" apps/storefront/nuxt.config.ts && grep -qi "autoInstrument" apps/admin/nuxt.config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both nuxt.config.ts have a sentry block with autoInstrument enabled and source-map upload configured against the self-hosted GlitchTip URL
    - No new npm dependency added (pnpm-lock.yaml unchanged)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Both apps' nuxt.config.ts enable autoInstrument and source-map upload; typecheck is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tag storefront server errors with tenant slug</name>
  <files>apps/storefront/server/middleware/tenant.ts</files>
  <read_first>
    - apps/storefront/server/middleware/tenant.ts (full file — find both resolve paths: cache-hit/fresh in the main handler, and the dev fallback)
    - apps/storefront/sentry.server.config.ts (confirm Sentry import style)
    - .planning/phases/02-observability/02-CONTEXT.md (D-01)
  </read_first>
  <action>
    Per D-01, after a tenant is successfully resolved and applied to context, call Sentry.setTag('tenant', tenant.slug) so every subsequent server-side error in that request carries the tenant tag automatically — no per-catch-block tagging. Import Sentry as `import * as Sentry from '@sentry/nuxt'`. Place the setTag on the success path right after applyTenantToContext in the main handler, and also on the dev-fallback success path (devFallbackOrThrow, after applyTenantToContext) so local repro is tagged too. Guard against a missing slug defensively (tenant.slug should always exist, but do not throw if it's nullish). Do not tag on the 503 / not-found paths — there is no tenant to name there.
  </action>
  <verify>
    <automated>grep -E "setTag\(['\"]tenant['\"]" apps/storefront/server/middleware/tenant.ts && grep -q "from '@sentry/nuxt'" apps/storefront/server/middleware/tenant.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Sentry.setTag('tenant', tenant.slug) is called on every successful tenant-resolve path (main handler + dev fallback), never on 503/not-found paths
    - Sentry is imported via `import * as Sentry from '@sentry/nuxt'`
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Resolved-tenant requests set the tenant tag; unresolved requests do not. Typecheck green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sentry module wiring (autoInstrument + source-map upload) in both apps and the tenant tag in storefront middleware. The Coolify env (NUXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN) and the live error-capture round-trip require a deploy against the real GlitchTip — Claude cannot deploy to Coolify or read the GlitchTip UI.
  </what-built>
  <how-to-verify>
    1. In Coolify, set NUXT_PUBLIC_SENTRY_DSN (from plan 02-01 project) and SENTRY_AUTH_TOKEN (GlitchTip auth token) for BOTH admin and storefront, then deploy.
    2. Add a temporary throwing test route to the storefront (e.g. a Nitro route that throws new Error('obs-01 smoke test')), hit it once, then confirm in GlitchTip within 60 seconds you see the issue with: a readable (de-minified) stack trace AND a `tenant` tag matching the slug you hit. Remove the temporary route afterward.
    3. Confirm the deployed start command shows the Sentry --import loader is active (server errors are captured at all — if zero events arrive, autoInstrument is not wired).
    Expected: tenant-tagged, de-minified error visible in GlitchTip within 60s.
  </how-to-verify>
  <resume-signal>Type "approved" once the tagged error is visible in GlitchTip, or describe what failed (no event / no tenant tag / minified trace).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- A thrown Nitro error appears in GlitchTip < 60s with a de-minified stack trace and a `tenant` tag (phase success criterion 1)
</verification>

<success_criteria>
- autoInstrument + source-map upload configured in both apps
- Storefront server errors tagged with tenant slug per D-01
- Live round-trip confirmed: tenant-tagged error visible in GlitchTip
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nitro server -> GlitchTip ingest | error payloads (incl. stack frames) leave the app to errors.fastio.ru |
| build pipeline -> GlitchTip | source maps uploaded with SENTRY_AUTH_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | error payloads may contain request data / PII | accept | Self-hosted GlitchTip on own VPS (no third-party); tracesSampleRate stays 0.01 limiting volume; tenant tag is a slug, not secret |
| T-02-05 | Spoofing | SENTRY_AUTH_TOKEN in build env | mitigate | Token stored only in Coolify env (not committed); never echoed in logs |
| T-02-06 | Repudiation | server errors lost if autoInstrument not active | mitigate | autoInstrument (D-02) guarantees --import loader; checkpoint verifies a real event arrives |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages (D-03) — @sentry/nuxt already present; no legitimacy gate needed |
</threat_model>

<output>
Create `.planning/phases/02-observability/02-02-SUMMARY.md` when done.
</output>
