---
phase: 03-e2e-testing
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - apps/admin/features/onboarding/components/OnboardingWizard.vue
  - apps/admin/features/onboarding/components/OnboardingOption.vue
  - apps/admin/features/onboarding/components/OnboardingStepType.vue
  - apps/admin/features/onboarding/components/OnboardingSidebarEntry.vue
  - apps/admin/features/onboarding/components/OnboardingStepInfo.vue
  - apps/admin/features/onboarding/components/OnboardingStepBranch.vue
  - apps/admin/features/onboarding/components/OnboardingStepMenuStyle.vue
  - apps/admin/features/onboarding/components/OnboardingStepModules.vue
  - apps/admin/features/onboarding/components/OnboardingStepPlan.vue
  - apps/admin/features/onboarding/components/OnboardingStepComplete.vue
  - tests/e2e/onboarding-flow.spec.ts
autonomous: true
requirements:
  - E2E-04
user_setup: []

must_haves:
  truths:
    - "OnboardingWizard и все step-компоненты имеют data-testid, по которым Playwright находит wizard, кнопки навигации (next/back/finish) и опции выбора business type (retail/services)"
    - "Существует тест onboarding-flow.spec.ts, который логинится как pre-seeded owner (новый тенант с onboardingCompleted=false), последовательно проходит все шаги визарда (type → menuStyle → plan → info → branch → modules → complete), нажимает «Перейти в админку» и проверяет, что admin-nav стал видимым (онбординг закрылся)"
    - "Тест работает против staging (e2e тенант с onboardingCompleted=false) — на локальном demo тенант имеет onboardingCompleted=true и wizard не показывается, поэтому test.skip с понятным сообщением если состояние онбординга не позволяет тест"
    - "Onboarding seed для staging имеет onboarding_state=NULL/onboardingCompleted=false, чтобы wizard показался при первом логине (Pitfall: тест зависит от триггер-условия wizard'а)"
  artifacts:
    - path: "tests/e2e/onboarding-flow.spec.ts"
      provides: "E2E тест полного onboarding flow"
      min_lines: 70
    - path: "apps/admin/features/onboarding/components/OnboardingWizard.vue"
      provides: "data-testid атрибуты для wizard и навигационных кнопок"
      contains: "data-testid=\"onboarding-wizard\""
    - path: "apps/admin/features/onboarding/components/OnboardingOption.vue"
      provides: "пропс testid для bind data-testid на button"
      contains: "testid"
  key_links:
    - from: "onboarding-flow.spec.ts"
      to: "OnboardingWizard.vue"
      via: "getByTestId('onboarding-wizard')"
      pattern: "onboarding-wizard"
    - from: "OnboardingStepType.vue"
      to: "OnboardingOption (с testid prop)"
      via: "testid prop binding"
      pattern: "onboarding-type-(retail|services)"
---

<objective>
Закрыть E2E-04 vertical slice: создать полный E2E тест onboarding wizard, который имитирует первый вход владельца нового тенанта и проходит wizard от выбора business type до завершения. Это закрывает success criterion фазы 3 ("owner onboarding flow steps through register → create venue → configure menu → publish storefront").

Назначение: Onboarding критичен — это первый touchpoint владельца с системой. Регрессия в любом шаге (тип бизнеса, выбор плана, базовая инфа, branch, modules) ломает activation funnel. Сейчас onboarding компоненты не имеют ни одного `data-testid` (verified в RESEARCH.md grep) — тест без них хрупкий и зависит от русского текста кнопок (что меняется при локализации/редизайне). Wave 0 этого плана — добавить testid, Wave 1 — написать тест.

Output: data-testid атрибуты на onboarding-компонентах (минимально необходимый набор для теста); новый файл `tests/e2e/onboarding-flow.spec.ts`, который логинится как pre-seeded owner и проходит wizard end-to-end. На staging это работает за счёт того, что seed создаёт tenant с `onboarding_state=NULL`. На локальном demo onboarding уже completed, поэтому тест корректно skip'ает себя с понятным сообщением.

Что НЕ покрывается этим планом и зафиксировано как TODO:
- Регистрация нового тенанта через landing form — требует email-интерцепции (Inbucket); вне scope (RESEARCH.md A3, Open Question #4).
- «Publish storefront» как отдельный шаг — в текущем UX нет отдельной кнопки publish; финальный шаг — `OnboardingStepComplete` с кнопкой «Перейти в админку», и активация витрины происходит автоматически при `onboardingCompleted=true` (per `apps/admin/layouts/default.vue:139`). Тест проверяет, что wizard закрылся; verify реальной доступности витрины — отдельный smoke (smoke-storefront.spec.ts существующий).

Этот план в Wave 3 (после 03-02/03-03/03-04), чтобы стабилизировать CI до того, как добавлять самый сложный flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-e2e-testing/03-RESEARCH.md
@.planning/phases/03-e2e-testing/03-PATTERNS.md
@apps/admin/features/onboarding/components/OnboardingWizard.vue
@apps/admin/features/onboarding/components/OnboardingOption.vue
@apps/admin/features/onboarding/components/OnboardingStepType.vue
@apps/admin/layouts/default.vue
@tests/e2e/admin-login.spec.ts
@tests/e2e/appointment-flow.spec.ts
@tests/e2e/fixtures.ts

<interfaces>
OnboardingWizard.vue структура (verified):
- Корневой `<div class="wizard-root">` — на нём data-testid="onboarding-wizard".
- Триггер показа: `showOnboarding = !tenantStore.loading && !tenantStore.tenant.onboardingCompleted` (layouts/default.vue:139).
- Шаги retail: type → menuStyle → plan → info → branch → modules → complete (7 шагов).
- Шаги services: type → plan → info → branch → complete (5 шагов).
- Кнопки в .nav:
  - "Назад" (line ~58): `<UiButton v-if="step > 1 && currentStep !== 'complete'" type="secondary" @click="prev">`
  - "Далее" (line ~67): `<UiButton v-if="currentStep !== 'complete'" type="primary" :loading="saving" :disabled="!canAdvance" @click="next">`
  - "Перейти в админку" (line ~76): `<UiButton v-else type="primary" :loading="saving" @click="finish">`

OnboardingOption.vue (verified):
- Корневой `<button type="button" class="option">` — пропсов нет под data-testid, нужно добавить опциональный `testid?: string`.
- defineEmits: `{ select: [] }`.

OnboardingStepType.vue (verified):
- Использует `<OnboardingOption v-for="option in options" :key="option.type" :icon="..." :title="..." :desc="..." :selected="..." @select="$emit('update:modelValue', option.type)" />`.
- options array содержит `{ type: 'retail', ... }` и `{ type: 'services', ... }`.
- Нужно добавить `:testid="`onboarding-type-${option.type}`"` к OnboardingOption.

OnboardingStepMenuStyle.vue (структуру не прочитали в research — для теста нужен только переход «next», без выбора → canAdvance возвращает true по дефолту, можно просто кликать onboarding-next).

OnboardingStepPlan.vue:
- canAdvance возвращает true только если form.plan !== null
- В шаблоне рендерит карточки плана — НУЖНО добавить data-testid="onboarding-plan-{key}" на каждую карточку плана либо на selectable элемент. Без этого тест не сможет выбрать план.

OnboardingStepInfo.vue:
- v-model:name, v-model:phone, v-model:timezone — нужно добавить testid на inputs:
  - onboarding-info-name (input)
  - onboarding-info-phone (input)
  - timezone обычно select — onboarding-info-timezone (либо оставить дефолт из tenant)
- validate() метод exposed — defineExpose

OnboardingStepBranch.vue:
- Похожа на StepInfo, валидирует адрес/название branch.
- Нужно add testid на основные inputs (минимум branch-name, чтобы тест мог заполнить).

OnboardingStepModules.vue:
- v-model:delivery, v-model:pickup, v-model:dine-in — toggle switches.
- В retail дефолт delivery+pickup=true (canAdvance=true сразу). Можно просто кликать "Далее".

OnboardingStepComplete.vue:
- Финальный шаг, ничего не валидирует, кнопка переключается на "Перейти в админку".

tenantStore.update() и tenantStore.changePlan() — реальные API вызовы → реальные данные в БД. Тест требует, чтобы тенант мог быть переинициализирован между прогонами:
- staging: globalSetup truncate + reseed возвращает onboardingCompleted=false (Pitfall 1 + Task 1 в 03-01)
- локально: demo тенант уже completed → тест skip

fixtures.adminEmail/adminPassword:
- На локальном: demo@fastio.app / demo seed password
- На staging: e2e@fastio.app / e2e-pass-12345 (из 03-01 Task 3 seed)

Проверка состояния онбординга в тесте (предварительно):
- После login, перед тем как пытаться пройти wizard, тест может прочитать `localStorage.getItem('tenant')` / Pinia store через `page.evaluate` ИЛИ просто проверить наличие `[data-testid="onboarding-wizard"]` с коротким timeout (5s). Если wizard не появился — skip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — добавить data-testid в onboarding компоненты</name>
  <files>
    apps/admin/features/onboarding/components/OnboardingWizard.vue
    apps/admin/features/onboarding/components/OnboardingOption.vue
    apps/admin/features/onboarding/components/OnboardingStepType.vue
    apps/admin/features/onboarding/components/OnboardingStepInfo.vue
    apps/admin/features/onboarding/components/OnboardingStepBranch.vue
    apps/admin/features/onboarding/components/OnboardingStepPlan.vue
  </files>
  <read_first>
    - apps/admin/features/onboarding/components/OnboardingWizard.vue (полный — для wizard root + 3 nav кнопки)
    - apps/admin/features/onboarding/components/OnboardingOption.vue (полный — для добавления testid prop)
    - apps/admin/features/onboarding/components/OnboardingStepType.vue (полный — для biding testid в шаблоне)
    - apps/admin/features/onboarding/components/OnboardingStepInfo.vue (полный — какие input компоненты используются, какие пропсы пробрасывают $attrs)
    - apps/admin/features/onboarding/components/OnboardingStepBranch.vue (полный — то же)
    - apps/admin/features/onboarding/components/OnboardingStepPlan.vue (полный — найти selectable элементы плана)
    - packages/ui/src/components/UiButton.vue (verify $attrs passthrough — определяет, можно ли вешать data-testid прямо на UiButton)
    - .planning/phases/03-e2e-testing/03-PATTERNS.md (паттерн добавления testid в onboarding компоненты)
  </read_first>
  <action>
    Минимальные изменения, по компонентам:

    1) **OnboardingWizard.vue** — три изменения:
       a) Корневой `<div class="wizard-root">` (line 2): добавить `data-testid="onboarding-wizard"`.
       b) Кнопка "Далее" (line 67 в области `<UiButton ... @click="next">`): если UiButton пропускает $attrs (см. ниже) — добавить `data-testid="onboarding-next"`. Если НЕ пропускает — обернуть UiButton в `<div data-testid="onboarding-next-wrapper">` или добавить data-testid на UiButton как пропс, если он его принимает.
       c) Кнопка "Перейти в админку" (line 76): аналогично, `data-testid="onboarding-finish"`.

       Проверить UiButton: Read `packages/ui/src/components/UiButton.vue`, найти `inheritAttrs` (если `inheritAttrs: false` — атрибуты НЕ пробрасываются на root, требуется явное связывание). Если UiButton использует `inheritAttrs: false`, обернуть в `<span data-testid="...">` или добавить custom прокидывание (если UiButton поддерживает `:data-testid` proxy через $attrs).

       Не трогать «Назад» — не критично для теста.

    2) **OnboardingOption.vue** — добавить пропс `testid?: string`:
       a) В `defineProps<{ ... }>()` добавить `testid?: string`.
       b) В шаблоне на `<button class="option">` добавить `:data-testid="testid"`.

    3) **OnboardingStepType.vue** — пробросить testid из шаблона:
       a) В шаблоне в `<OnboardingOption v-for="option in options" ...>` добавить `:testid="`onboarding-type-${option.type}`"`.
       Так на retail получится `data-testid="onboarding-type-retail"`, на services — `onboarding-type-services`.

    4) **OnboardingStepInfo.vue** — добавить testid на inputs:
       a) На input для name: добавить data-testid="onboarding-info-name".
       b) На input для phone: data-testid="onboarding-info-phone".
       Если используются Naive UI input-обёртки из @fastio/ui (FsInput, UiInput), проверить через Read `packages/ui/src/components/UiInput.vue` или `packages/public-ui/.../FsInput.vue`, как пробрасываются атрибуты на нативный input. Если нативный получает их через $attrs — `data-testid="onboarding-info-name"` достаточно. Если нет — обернуть в `<div data-testid="..."><UiInput .../></div>` или использовать ref.

    5) **OnboardingStepBranch.vue** — добавить data-testid="onboarding-branch-name" на главный input (название branch). Опционально — testid на address input, но минимум — name.

    6) **OnboardingStepPlan.vue** — найти selectable элемент плана (card / option). Добавить динамический `:data-testid="\`onboarding-plan-${plan.key}\`"` или подобный (использовать тот ключ плана, что store передаёт в form.plan). Если планов 3 (basic/standard/premium) — все три карточки получат свои testid.

    Никаких других изменений в этих компонентах: не менять пропсы (кроме добавления testid в OnboardingOption), computed, методы, styles. Только добавление атрибутов.

    Проверить, что `pnpm typecheck` остаётся зелёным.
  </action>
  <verify>
    <automated>grep -c 'data-testid="onboarding-wizard"' apps/admin/features/onboarding/components/OnboardingWizard.vue && grep -c 'onboarding-next\|onboarding-finish' apps/admin/features/onboarding/components/OnboardingWizard.vue && grep -c 'testid' apps/admin/features/onboarding/components/OnboardingOption.vue && grep -c 'onboarding-type-' apps/admin/features/onboarding/components/OnboardingStepType.vue && grep -c 'onboarding-info-' apps/admin/features/onboarding/components/OnboardingStepInfo.vue && grep -c 'onboarding-branch-' apps/admin/features/onboarding/components/OnboardingStepBranch.vue && grep -c 'onboarding-plan-' apps/admin/features/onboarding/components/OnboardingStepPlan.vue && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingWizard.vue` содержит `data-testid="onboarding-wizard"` на корневом элементе.
    - `OnboardingWizard.vue` содержит `data-testid="onboarding-next"` и `data-testid="onboarding-finish"` (или wrapper-варианты).
    - `OnboardingOption.vue` имеет новый пропс `testid?: string` в defineProps и `:data-testid="testid"` биндинг на root `<button>`.
    - `OnboardingStepType.vue` использует `:testid="`onboarding-type-${option.type}`"` на OnboardingOption.
    - `OnboardingStepInfo.vue` имеет хотя бы один `data-testid="onboarding-info-name"`.
    - `OnboardingStepBranch.vue` имеет хотя бы один `data-testid="onboarding-branch-name"`.
    - `OnboardingStepPlan.vue` имеет селектор `data-testid="onboarding-plan-..."` (динамический по plan key).
    - `pnpm typecheck` зелёный.
    - Никакие existing tests не сломаны (smoke + admin-login проходят).
  </acceptance_criteria>
  <done>Все необходимые data-testid атрибуты на onboarding компонентах добавлены; тест может надёжно навигировать wizard без зависимости от русского текста.</done>
</task>

<task type="auto">
  <name>Task 2: Создать tests/e2e/onboarding-flow.spec.ts</name>
  <files>tests/e2e/onboarding-flow.spec.ts</files>
  <read_first>
    - apps/admin/features/onboarding/components/OnboardingWizard.vue (после Task 1 — финальные testid)
    - apps/admin/features/onboarding/components/OnboardingStepType.vue (после Task 1 — testid опций)
    - apps/admin/features/onboarding/components/OnboardingStepInfo.vue (после Task 1)
    - apps/admin/features/onboarding/components/OnboardingStepBranch.vue (после Task 1)
    - apps/admin/features/onboarding/components/OnboardingStepPlan.vue (после Task 1)
    - tests/e2e/admin-login.spec.ts (паттерн login)
    - tests/e2e/appointment-flow.spec.ts (паттерн многошагового flow с waitFor)
    - tests/e2e/fixtures.ts (доступные fixtures)
    - .planning/phases/03-e2e-testing/03-PATTERNS.md (Onboarding test skeleton)
  </read_first>
  <action>
    Создать `tests/e2e/onboarding-flow.spec.ts`. Структура:

    ```typescript
    import { test, expect } from '@playwright/test'
    import { fixtures } from './fixtures'

    test.use({ baseURL: 'http://localhost:4710' })

    // E2E-04: полный onboarding wizard. Тест работает только когда тенант
    // pre-seeded с onboardingCompleted=false. На staging такой тенант
    // создаётся seed'ом (e2e@fastio.app), на локальном demo seed —
    // тенант уже completed, поэтому тест себя skip'ает.

    test('onboarding wizard: login → type → menuStyle → plan → info → branch → modules → complete', async ({ page }) => {
      // Step 1: login
      await page.goto('/login')
      await page.getByTestId('admin-login-form').waitFor({ state: 'visible', timeout: 15_000 })
      await page.getByTestId('admin-login-email').locator('input').fill(fixtures.adminEmail)
      await page.getByTestId('admin-login-password').locator('input').fill(fixtures.adminPassword)
      await page.getByTestId('admin-login-submit').click()
      // После login возможны два пути: либо admin-nav (если onboarding completed),
      // либо onboarding-wizard (если нет).
      await expect(page.getByTestId('admin-nav').or(page.getByTestId('onboarding-wizard'))).toBeVisible({ timeout: 20_000 })

      // Step 2: skip если wizard не появился (тенант already-completed)
      const wizardVisible = await page.getByTestId('onboarding-wizard').isVisible()
      test.skip(!wizardVisible, 'Tenant onboarding already completed; wizard not shown. Test requires pre-seeded fresh tenant (e2e@fastio.app on staging).')

      // Step 3: type — выбрать retail
      await page.getByTestId('onboarding-type-retail').click()
      await page.getByTestId('onboarding-next').click()

      // Step 4: menuStyle — дефолт сохраняется, просто next
      // (Если не появилась menu-style страница на следующем шаге — это означает,
      // что step skipped. Wait короткий, не обязателен.)
      await page.getByTestId('onboarding-next').click()

      // Step 5: plan — выбрать basic план. Точный ключ зависит от usePlans().
      // Pre-emptively выбираем первый доступный план.
      const planOption = page.locator('[data-testid^="onboarding-plan-"]').first()
      await planOption.waitFor({ state: 'visible', timeout: 10_000 })
      await planOption.click()
      await page.getByTestId('onboarding-next').click()

      // Step 6: info — заполнить name + phone (если поле empty)
      const infoNameInput = page.getByTestId('onboarding-info-name').locator('input')
      await infoNameInput.waitFor({ state: 'visible', timeout: 10_000 })
      // Не очищаем — usually pre-seeded из tenant. Если пусто — заполняем.
      const currentName = await infoNameInput.inputValue()
      if (!currentName) await infoNameInput.fill('E2E Tenant')
      const phoneInput = page.getByTestId('onboarding-info-phone').locator('input')
      const currentPhone = await phoneInput.inputValue()
      if (!currentPhone) await phoneInput.fill('+7 (777) 123-45-67')
      await page.getByTestId('onboarding-next').click()

      // Step 7: branch — заполнить branch name если empty
      const branchNameInput = page.getByTestId('onboarding-branch-name').locator('input')
      await branchNameInput.waitFor({ state: 'visible', timeout: 10_000 })
      const currentBranch = await branchNameInput.inputValue()
      if (!currentBranch) await branchNameInput.fill('E2E Branch')
      await page.getByTestId('onboarding-next').click()

      // Step 8: modules — дефолт delivery+pickup=true → canAdvance=true сразу
      await page.getByTestId('onboarding-next').click()

      // Step 9: complete — нажать "Перейти в админку"
      await page.getByTestId('onboarding-finish').waitFor({ state: 'visible', timeout: 15_000 })
      await page.getByTestId('onboarding-finish').click()

      // Step 10: wizard должен закрыться, admin-nav должен быть viseme
      await expect(page.getByTestId('onboarding-wizard')).toBeHidden({ timeout: 20_000 })
      await expect(page.getByTestId('admin-nav')).toBeVisible({ timeout: 20_000 })

      // Sanity: проверить, что в admin nav есть ссылка на /menu (witness что онбординг
      // действительно открыл админку). Используем существующий testid 'admin-nav-dashboard'
      // из admin-login.spec.ts.
      await expect(page.getByTestId('admin-nav-dashboard')).toBeVisible()
    })
    ```

    Важные нюансы:
    - Не использовать `page.waitForTimeout`.
    - Если step skipped — например, шаг `menuStyle` для services тенанта не показывается. Тест использует retail (выбрал `onboarding-type-retail`), поэтому menuStyle должен появиться.
    - В step 4 (menuStyle) текущий компонент `OnboardingStepMenuStyle.vue` не прочитан в research; если canAdvance=false по default (нужен выбор стиля) — тест упадёт. В таком случае: добавить data-testid на варианты menuStyle в Task 1 + кликать первый вариант здесь. Если изучение шаблона показало, что step имеет дефолтный selected — просто next.
    - Если шаг plan имеет dynamic plans (загружаются через usePlans → Supabase), нужно дождаться загрузки. waitFor visible на `[data-testid^="onboarding-plan-"]` с timeout 10s.
    - Если usePlans возвращает 0 планов на staging (плановая БД пустая) — тест упадёт на «Step 5» с timeout. В таком случае: добавить в Task 3 проверку и инструкции по добавлению хотя бы одного плана в staging seed (но это уже не E2E-04 vector, а сам seed mvi-04). Записать в SUMMARY как TODO.

    Не использовать ничего из новых импортов — только @playwright/test и fixtures.
  </action>
  <verify>
    <automated>test -f tests/e2e/onboarding-flow.spec.ts && grep -c "onboarding-wizard" tests/e2e/onboarding-flow.spec.ts && grep -c "onboarding-type-retail" tests/e2e/onboarding-flow.spec.ts && grep -c "onboarding-finish" tests/e2e/onboarding-flow.spec.ts && grep -c "test.skip" tests/e2e/onboarding-flow.spec.ts && wc -l tests/e2e/onboarding-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/e2e/onboarding-flow.spec.ts` существует.
    - Содержит admin login паттерн (admin-login-form, admin-login-email, admin-login-password, admin-login-submit).
    - Использует `test.skip` с понятным сообщением для случая «онбординг уже completed».
    - Использует testid `onboarding-wizard`, `onboarding-type-retail`, `onboarding-next`, `onboarding-finish`, `onboarding-info-name`, `onboarding-branch-name`.
    - НЕ использует `page.waitForTimeout`.
    - `wc -l` >= 70 строк.
    - Финальные assertions: wizard hidden, admin-nav visible, admin-nav-dashboard visible.
  </acceptance_criteria>
  <done>Тест существует, проходит wizard от type до complete, корректно skip'ается при уже-completed онбординге; нужны testid из Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Локальный прогон + полный suite regression</name>
  <files>(нет файловых изменений — только верификация)</files>
  <read_first>
    - tests/e2e/onboarding-flow.spec.ts (после Task 2)
    - apps/admin/features/onboarding/components/ (после Task 1)
  </read_first>
  <action>
    1) Локальный прогон именно онбординг-теста:
    ```
    pnpm exec playwright test tests/e2e/onboarding-flow.spec.ts --project=chromium
    ```

    Ожидаемый результат локально: тест должен skip'нуться (demo тенант имеет onboardingCompleted=true). В выводе: "1 skipped". Это OK.

    Если тест НЕ skip'ается и пытается пройти wizard на demo — что-то пошло не так с проверкой `isVisible()` на onboarding-wizard. Возможные причины:
    - У demo тенанта на самом деле onboardingCompleted=false (баг seed).
    - Wizard рендерится моментально и проверка isVisible'а возвращает true. Тест проходит и завершает онбординг — это меняет демо-state, что нарушит другие тесты. В таком случае: усилить условие skip — после login проверить через `page.evaluate` Pinia store значение onboardingCompleted.

    2) Прогон полного suite — все тесты должны проходить или корректно skip'аться:
    ```
    pnpm test:e2e
    ```
    Ожидаемое: smoke + admin-login + order-flow + cross-tenant + account-auth + appointment-flow + reservation-flow + admin-create-dish + auth-invite-staff (passed/skipped depending on env) + onboarding-flow (skipped on local).

    3) `pnpm typecheck` — зелёный.

    Если что-то падает (кроме онбординг-теста, который должен быть skipped) — это регрессия от Task 1 (добавление testid могло сломать UiButton или OnboardingOption). Откатить минимально и зафиксировать в SUMMARY.

    4) Записать в SUMMARY:
       - Что на локальном run тест ПРОПУСКАЕТСЯ (демо тенант уже completed) — это expected behavior.
       - Полное покрытие E2E-04 верифицируется только на staging, где тенант pre-seeded с onboarding_state=NULL. После завершения Phase 3 staging setup (03-01) и применения seed staging, прогон в CI должен showcase the full path.
       - Если usePlans возвращает 0 планов на staging — добавить в TODO: «расширить e2e-staging.sql строкой plans (или предусмотреть, что plans таблица заполняется отдельной миграцией)».
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/onboarding-flow.spec.ts --project=chromium 2>&1 | tail -10 && pnpm exec playwright test smoke-storefront.spec.ts smoke-admin.spec.ts admin-login.spec.ts order-flow.spec.ts cross-tenant.spec.ts --project=chromium 2>&1 | tail -10 && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test tests/e2e/onboarding-flow.spec.ts --project=chromium` возвращает exit code 0.
    - В выводе либо "1 passed" (если каким-то образом запустилось — например, на staging), либо "1 skipped" (локально).
    - Smoke + admin-login + order-flow + cross-tenant зелёные (нет регрессий после добавления testid).
    - `pnpm typecheck` зелёный.
    - SUMMARY содержит: (a) запись об expected skip локально, (b) TODO если usePlans на staging не отдаёт планы (либо confirm что отдаёт).
  </acceptance_criteria>
  <done>E2E-04 покрытие готово, тест корректно проходит на staging и skip'ается локально; никаких регрессий в остальных сьютах; пакетная нить onboarding (testid) на месте.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser context → Admin SPA | admin login, далее все вызовы tenantStore.update / changePlan через @supabase/supabase-js anon + RLS |
| Wizard finish → tenants.onboardingCompleted = true | необратимая операция; тест полагается на reseed чтобы вернуть в исходное состояние |
| onboarding state в БД | хранится в `tenants.onboarding_state` / `onboardingCompleted` columns; staging seed выставляет в исходное состояние перед каждым прогоном |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Tampering | Test mutates real tenant state | mitigate | staging globalSetup truncate + reseed восстанавливает onboardingCompleted=false перед каждым прогоном (03-01 seed уже это обеспечивает); локальный тест skip'ается, поэтому не трогает demo-данные |
| T-3-05b | Information Disclosure | data-testid в production bundle | accept | Testid это публичный DOM-атрибут, без sensitive payload; common practice |
| T-3-05c | Repudiation | usePlans returns 0 plans on staging | mitigate | Task 3 acceptance criteria требует verify, что планов >= 1 на staging; если 0 → TODO в SUMMARY с явным указанием расширить seed |
| T-3-05d | Tampering | UiButton inheritAttrs может не пробросить data-testid | mitigate | Task 1 явно требует Read UiButton.vue и fallback на wrapper div если $attrs не пробрасывается |
| T-3-SC | Tampering | npm install drift | accept | Новых пакетов нет; testid атрибуты — pure DOM mutations |
</threat_model>

<verification>
- Все 6 onboarding-компонентов имеют необходимые data-testid (grep gates в Task 1).
- `tests/e2e/onboarding-flow.spec.ts` существует и использует testid из Task 1.
- Локальный прогон: тест skip'ается с понятным сообщением; smoke + сьют без регрессий.
- `pnpm typecheck` зелёный.
</verification>

<success_criteria>
1. Onboarding компоненты имеют data-testid на wizard root, navigation buttons, business type options, info inputs, branch input, plan options.
2. Существует тест `onboarding-flow.spec.ts`, проходящий wizard end-to-end (retail path: type → menuStyle → plan → info → branch → modules → complete).
3. Тест корректно skip'ается локально (demo тенант completed) и passing'ся на staging (e2e тенант fresh).
4. Никаких регрессий в существующих E2E тестах после добавления testid.
</success_criteria>

<output>
Создать `.planning/phases/03-e2e-testing/03-05-SUMMARY.md` после завершения. Зафиксировать:
- Финальный список добавленных testid (с компонентом-источником).
- Подтверждение, что локальный тест skip'ается (expected); подробности про условие skip.
- Если usePlans staging issue обнаружен — записать TODO о расширении staging seed строкой plans.
- TODO: проверить полный flow онбординга на staging через ручной запуск в CI с staging-env'ами после деплоя.
</output>
