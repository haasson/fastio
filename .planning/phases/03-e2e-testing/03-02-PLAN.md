---
phase: 03-e2e-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/e2e/order-flow.spec.ts
  - apps/admin/features/orders/components/OrderRow.vue
autonomous: true
requirements:
  - E2E-01
user_setup: []

must_haves:
  truths:
    - "Тест order-flow.spec.ts создаёт заказ на витрине, доходит до /order/[id], затем входит в admin-панель и видит созданный заказ в списке /orders по маркеру телефона"
    - "Тест проходит и локально (docker-путь setupLocal), и против staging (staging-путь setupStaging) — без хардкода demo slug, читает retailTenantSlug из fixtures"
    - "Admin OrderRow рендерит data-testid, по которому Playwright находит строку (order-row) и подтверждает попадание phoneMarker"
    - "Тест устойчив к Realtime-задержке: visibility-assertion на admin-строке заказа имеет timeout 30s (Pitfall 3)"
  artifacts:
    - path: "tests/e2e/order-flow.spec.ts"
      provides: "Полный E2E flow customer → admin для E2E-01"
      contains: "admin-nav"
    - path: "apps/admin/features/orders/components/OrderRow.vue"
      provides: "data-testid=order-row на корневом элементе для надёжной локации в E2E"
      contains: "data-testid=\"order-row\""
  key_links:
    - from: "order-flow.spec.ts"
      to: "admin /orders страница"
      via: "context.newPage() + admin login + locator('[data-testid=order-row]')"
      pattern: "admin-nav"
    - from: "order-flow.spec.ts"
      to: "fixtures.retailTenantSlug"
      via: "test.use({ baseURL })"
      pattern: "fixtures\\.retailTenantSlug"
---

<objective>
Закрыть E2E-01 vertical slice: текущий `order-flow.spec.ts` доходит до `/order/[id]` на витрине и останавливается. Расширить его так, чтобы после создания заказа тест переключался в admin-панель тем же `context`, логинился, шёл в `/orders` и подтверждал, что заказ виден по маркеру телефона. Это закрывает second success criterion фазы 3 ("заказ появляется в admin-панели").

Назначение: E2E-01 — самый важный flow проекта (Core Value: «заказ клиента поступает в заведение без потерь»). Сейчас тест проверяет только storefront-сторону, что оставляет admin-side вне покрытия. Эта задача делает flow full-stack без новых файлов спеков — расширяет существующий.

Output: расширенный `order-flow.spec.ts` с admin-side проверкой; добавленный `data-testid="order-row"` на `OrderRow.vue`, чтобы Playwright не зависел от лейаута/текста. Параметризация slug уже существует в файле (`test.use({ baseURL: \`http://${fixtures.retailTenantSlug}.localhost:4711\` })`) — её НЕ менять; на staging-прогоне `retailTenantSlug` будет `'e2e'`, локально — `'demo'`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-e2e-testing/03-RESEARCH.md
@.planning/phases/03-e2e-testing/03-PATTERNS.md
@tests/e2e/order-flow.spec.ts
@tests/e2e/admin-login.spec.ts
@tests/e2e/fixtures.ts
@apps/admin/features/orders/components/OrderRow.vue

<interfaces>
Текущий tests/e2e/order-flow.spec.ts (55 строк):
- test.use({ baseURL: `http://${fixtures.retailTenantSlug}.localhost:4711` }) — slug уже параметризован
- Текст теста заканчивается на line 53: `await expect(page.getByTestId('order-number')).toContainText(/Заказ #/)`
- Маркер телефона — fixtures.phoneMarker, заполняется в checkout-phone (line 47)

tests/e2e/admin-login.spec.ts паттерн (для копирования в admin-шаг):
- test.use({ baseURL: 'http://localhost:4710' })  — admin localhost
- getByTestId('admin-login-form').waitFor({ state: 'visible', timeout: 15_000 })
- getByTestId('admin-login-email').locator('input').fill(fixtures.adminEmail)
- getByTestId('admin-login-password').locator('input').fill(fixtures.adminPassword)
- getByTestId('admin-login-submit').click()
- expect(getByTestId('admin-nav')).toBeVisible({ timeout: 20_000 })

OrderRow.vue (line 26) — текущий разметка:
- <span class="customer-phone">{{ formatPhone(order.customerPhone) }}</span>
- Корневой <tr> или <div> зависит от структуры — открыть Read, найти корень и добавить data-testid="order-row" на нём.
- Также добавить data-testid="order-row-phone" на .customer-phone (опционально, для будущих более точных тестов).

fixtures.ts уже экспортирует:
- adminEmail, adminPassword (паролится в global-setup)
- phoneMarker (уникальный маркер для тестов — `+7777E2E####` шаблон или подобный)
- retailTenantSlug ('demo' локально, 'e2e' на staging)

Admin /orders страница:
- URL: http://localhost:4710/orders
- Использует apps/admin/features/orders/components/OrderList.vue → OrderRow.vue
- Список заказов рендерится через Realtime → Pinia store, поэтому свежий заказ может появиться с задержкой (Pitfall 3: timeout 30s)

Cross-port navigation:
- adminPage = context.newPage() — переиспользуем тот же browser context, чтобы не открывать новый instance (RESEARCH.md "Don't Hand-Roll")
- Cookies storefront-домена (e2e/demo.localhost) изолированы от admin-домена (localhost:4710) — admin требует отдельного логина в этом контексте (нет SSO между storefront customer и admin auth)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить data-testid=order-row на OrderRow.vue (Wave 0)</name>
  <files>apps/admin/features/orders/components/OrderRow.vue</files>
  <read_first>
    - apps/admin/features/orders/components/OrderRow.vue (полный файл — нужно увидеть корневой элемент и понять, какой селектор Playwright сможет надёжно использовать)
    - apps/admin/features/orders/components/OrderList.vue (как OrderRow рендерится в списке — чтобы убедиться, что добавляемый testid не конфликтует с уже существующим в родителе)
    - .planning/phases/03-e2e-testing/03-PATTERNS.md (паттерн testid для строк списка)
  </read_first>
  <action>
    Открыть `apps/admin/features/orders/components/OrderRow.vue`. Найти корневой элемент шаблона (`<tr>`, `<div>`, или другой) и добавить туда атрибут `data-testid="order-row"`. Если корневой элемент — `UiListRow`, `UiCard` или другой компонент из `@fastio/ui`, прочитать его исходник в `packages/ui/src/components/` и убедиться, что он пробрасывает атрибуты через `v-bind="$attrs"` или явный пропс. Если не пробрасывает — обернуть компонент в `<div data-testid="order-row">` или добавить пропс `:data-testid="'order-row'"` (Vue 3 автоматически передаст не-объявленные атрибуты на корневой single-root компонент, но если в компоненте `inheritAttrs: false`, понадобится явное связывание).

    Также добавить `data-testid="order-row-phone"` на элемент `<span class="customer-phone">` (примерно строка 26 — открыть Read, чтобы убедиться в номере строки). Это позволит будущим тестам напрямую искать строку по тексту телефона без необходимости фильтра по тексту.

    Никаких других изменений в OrderRow.vue не делать. Не менять стили, props, computed.

    Если корневой элемент уже имеет другой `data-testid` — НЕ переименовывать его (это означало бы регрессию для других тестов); вместо этого обернуть в `<div data-testid="order-row">` либо использовать другое имя (`order-list-row`). Зафиксировать выбранное имя в SUMMARY и в Task 2 использовать его.
  </action>
  <verify>
    <automated>grep -c 'data-testid="order-row"' apps/admin/features/orders/components/OrderRow.vue && grep -c 'data-testid="order-row-phone"' apps/admin/features/orders/components/OrderRow.vue && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/features/orders/components/OrderRow.vue` содержит хотя бы одно вхождение `data-testid="order-row"`.
    - `apps/admin/features/orders/components/OrderRow.vue` содержит хотя бы одно вхождение `data-testid="order-row-phone"` на элементе с классом `customer-phone`.
    - `pnpm typecheck` остаётся зелёным.
    - Никакие другие пропсы, методы, стили или импорты в OrderRow.vue не изменены — `git diff` показывает только добавление двух `data-testid` атрибутов (плюс возможный wrapping div, если компонент-родитель не пробрасывает атрибуты).
  </acceptance_criteria>
  <done>OrderRow.vue имеет надёжные локаторы для Playwright; admin-side проверки в E2E-01 могут использовать их без зависимости от текста или CSS-классов.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить order-flow.spec.ts admin-side проверкой</name>
  <files>tests/e2e/order-flow.spec.ts</files>
  <read_first>
    - tests/e2e/order-flow.spec.ts (текущий тест — расширяем, а не переписываем)
    - tests/e2e/admin-login.spec.ts (паттерн admin login — скопировать verbatim для admin-шага)
    - tests/e2e/fixtures.ts (доступные fixtures — adminEmail, adminPassword, phoneMarker, retailTenantSlug)
    - apps/admin/features/orders/components/OrderRow.vue (после Task 1 — убедиться, что testid именно `order-row` или то имя, которое выбрали)
    - .planning/phases/03-e2e-testing/03-PATTERNS.md (паттерн order-flow extension; Pitfall 3 — timeout 30s для Realtime; "Don't Hand-Roll" — context.newPage())
  </read_first>
  <action>
    Расширить существующий test в `tests/e2e/order-flow.spec.ts`. Не создавать новый файл — добавить шаги после строки 53 (`await expect(page.getByTestId('order-number')).toContainText(/Заказ #/)`).

    1) Изменить сигнатуру теста, чтобы получить `context` фикстуру:
       ```
       test('order flow: add dish → checkout → submit → order page → admin sees order', async ({ page, context }) => {
       ```
       (имя теста расширено, чтобы отражать новый этап).

    2) После line 53 добавить `test.step('admin sees the order in /orders', async () => { ... })` блок. Внутри:
       a) Открыть новую страницу в том же контексте: `const adminPage = await context.newPage()`. Это переиспользует браузер и не открывает новый instance (RESEARCH.md "Don't Hand-Roll").
       b) Перейти на `http://localhost:4710/login` (абсолютный URL, потому что test.use baseURL — storefront).
       c) Скопировать паттерн admin-login.spec.ts verbatim:
          - `await adminPage.getByTestId('admin-login-form').waitFor({ state: 'visible', timeout: 15_000 })`
          - `await adminPage.getByTestId('admin-login-email').locator('input').fill(fixtures.adminEmail)`
          - `await adminPage.getByTestId('admin-login-password').locator('input').fill(fixtures.adminPassword)`
          - `await adminPage.getByTestId('admin-login-submit').click()`
          - `await expect(adminPage.getByTestId('admin-nav')).toBeVisible({ timeout: 20_000 })`
       d) Перейти в admin orders: `await adminPage.goto('http://localhost:4710/orders')`.
       e) Найти строку заказа по маркеру телефона. Использовать `adminPage.locator('[data-testid="order-row"]', { hasText: fixtures.phoneMarker }).first()`. Wait + assert visible с timeout 30s (Pitfall 3 — Realtime async):
          ```
          const orderRow = adminPage.locator('[data-testid="order-row"]', { hasText: fixtures.phoneMarker }).first()
          await expect(orderRow).toBeVisible({ timeout: 30_000 })
          ```

    3) Если Task 1 выбрал другое имя testid (не `order-row`), использовать выбранное имя — но строго одно и то же в Task 1 и Task 2.

    4) Не менять storefront-часть теста (lines 11-53). Не использовать `page.waitForTimeout`. Не вводить дополнительных утилит — всё в один файл.

    5) Параметризация slug уже существует в файле (строка 8: `test.use({ baseURL: \`http://${fixtures.retailTenantSlug}.localhost:4711\` })`) — НЕ менять. Если по какой-то причине строка содержит хардкод `demo` — заменить на параметризованный, но grep показывает, что параметризация уже на месте.

    6) Импорты — `import { test, expect } from '@playwright/test'` и `import { fixtures } from './fixtures'` (уже есть). Добавить ничего не нужно.
  </action>
  <verify>
    <automated>grep -c "test.step" tests/e2e/order-flow.spec.ts && grep -c "context.newPage" tests/e2e/order-flow.spec.ts && grep -c "admin-nav" tests/e2e/order-flow.spec.ts && grep -c "fixtures.phoneMarker" tests/e2e/order-flow.spec.ts && grep -c "data-testid=\"order-row\"" tests/e2e/order-flow.spec.ts && grep -c "30_000" tests/e2e/order-flow.spec.ts && (grep -v "^#" tests/e2e/order-flow.spec.ts | grep -c "waitForTimeout" || echo "0-ok")</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/order-flow.spec.ts` содержит `test.step('admin sees the order in /orders', ...)` блок.
    - Файл использует `context.newPage()` для admin-страницы (не `browser.newContext()` или `chromium.launch()`).
    - Admin login паттерн внутри `test.step` использует testid `admin-login-form`, `admin-login-email`, `admin-login-password`, `admin-login-submit`, `admin-nav`.
    - Файл использует `fixtures.phoneMarker` для поиска строки заказа в admin.
    - Файл содержит локатор `[data-testid="order-row"]` (или иное имя, выбранное в Task 1) с фильтром по `hasText: fixtures.phoneMarker`.
    - Visibility-assertion на admin order row имеет `timeout: 30_000` (для Realtime async).
    - Файл НЕ содержит `page.waitForTimeout` (grep `-v '^#' | grep -c waitForTimeout` возвращает 0).
    - Storefront-часть теста (строки до 53) не изменена — `git diff` показывает только добавление test.step блока и расширение сигнатуры теста (добавление `context` к деструктуризации).
  </acceptance_criteria>
  <done>order-flow.spec.ts покрывает полный E2E-01 vertical slice: customer создаёт заказ на витрине → заказ доходит до /order/[id] → admin видит заказ в /orders.</done>
</task>

<task type="auto">
  <name>Task 3: Прогнать order-flow.spec.ts локально и подтвердить green</name>
  <files>(нет файловых изменений — только верификация)</files>
  <read_first>
    - tests/e2e/order-flow.spec.ts (после Task 2)
    - apps/admin/features/orders/components/OrderRow.vue (после Task 1)
    - playwright.config.ts (как запускается webServer и какие проекты)
  </read_first>
  <action>
    Прогнать тест локально против существующего docker-Supabase (setupLocal путь — НЕ требует staging-секретов). Команда:
    ```
    pnpm exec playwright test tests/e2e/order-flow.spec.ts --project=chromium
    ```

    Если тест падает на admin-стороне с "Timeout 30000ms exceeded — order-row not visible":
    - Открыть `playwright-report/index.html` и trace
    - Возможные причины:
      a) OrderList использует группировку по дате — заказ может быть в развёрнутой группе. Тогда нужно либо ждать длиннее, либо добавить шаг "развернуть свежую дату". Скорректировать тест, не OrderRow.
      b) Phone маркер форматируется (formatPhone) — `hasText` ищет точное вхождение, а в DOM может быть формат "+7 (777) ...". В этом случае использовать `regex` или передать в `hasText` только цифровую часть маркера: `hasText: new RegExp(fixtures.phoneMarker.replace(/\\D/g, '').slice(-4))` (последние 4 цифры). Скорректировать тест.
      c) Realtime не подписался на demo-тенант — admin авторизован, но tenant store пустой. Проверить, что после login admin реально приземляется в `/orders` и в URL нет редиректа на `/no-access` или `/suspended`.

    Не помечать «green», если тест не зелёный. Если возникла любая из проблем — починить в `order-flow.spec.ts` (НЕ в production-коде admin).

    После того как тест зелёный — запустить smoke-suite, чтобы убедиться, что нет регрессий:
    ```
    pnpm exec playwright test smoke-storefront.spec.ts smoke-admin.spec.ts admin-login.spec.ts --project=chromium
    ```
    Они должны проходить (storefront + admin поднимаются, admin login работает).
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/order-flow.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test tests/e2e/order-flow.spec.ts --project=chromium` возвращает exit code 0.
    - В выводе: "1 passed".
    - Smoke-сьют `pnpm exec playwright test smoke-storefront.spec.ts smoke-admin.spec.ts admin-login.spec.ts --project=chromium` тоже зелёный (2-3 passed).
    - Никаких изменений в production-коде admin не сделано в этой задаче (`git status` показывает только мутации, согласованные с Task 1 и Task 2).
  </acceptance_criteria>
  <done>order-flow.spec.ts полностью покрывает E2E-01 и зелёный локально; никаких регрессий в smoke-suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser context → Storefront SSR | гостевой checkout — без auth, тест проверяет valid path; нет защиты для probe-инвариантов |
| Browser context → Admin SPA | admin login через email/password (тестовый), тест-данные не содержат PII за пределами test fixtures |
| OrderRow rendering | data-testid атрибут — visible в DOM; не утекает в client bundle ничего sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | data-testid атрибуты в production bundle | accept | `data-testid` это публичный DOM-атрибут; не несёт sensitive payload; убирать через build-step out-of-scope (общий проектный паттерн — testid остаются и в проде) |
| T-3-02b | Tampering | Test реальные данные (phoneMarker) в БД | mitigate | phoneMarker — синтетический маркер (типа +7777...), не реальный телефон; cleanup происходит в next globalSetup (truncate перед каждым запуском) |
| T-3-02c | Repudiation | Race между storefront submit и admin Realtime | mitigate | Pitfall 3: timeout 30s на admin assertion даёт Realtime + Pinia store достаточно времени; flakiness signal — увеличить до 45s локально, на staging — investigate |
| T-3-SC | Tampering | npm install drift | accept | Новых пакетов не ставим; `@playwright/test` 1.60.0 уже в deps |
</threat_model>

<verification>
- OrderRow.vue имеет `data-testid="order-row"` и `data-testid="order-row-phone"` (grep gates).
- order-flow.spec.ts содержит test.step admin block с правильными testid и timeout 30s.
- `pnpm exec playwright test tests/e2e/order-flow.spec.ts` зелёный локально.
- Никаких регрессий в smoke-сьюте.
</verification>

<success_criteria>
1. Тест `order-flow.spec.ts` проходит локально (setupLocal путь) end-to-end: customer создаёт заказ → /order/[id] → admin видит его в /orders.
2. Никаких хардкодов `demo` в order-flow.spec.ts — slug читается из `fixtures.retailTenantSlug` (уже параметризовано).
3. Admin OrderRow имеет `data-testid="order-row"` для надёжной локации в E2E.
4. `pnpm typecheck` зелёный; smoke-сьют зелёный.
</success_criteria>

<output>
Создать `.planning/phases/03-e2e-testing/03-02-SUMMARY.md` после завершения. Зафиксировать в нём: финальное имя testid (если отличается от `order-row`), любые наблюдённые flakiness (с команд playwright test), и решение по timeout (стандартный 30s или увеличенный).
</output>
