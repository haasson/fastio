---
phase: 04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - apps/ops/server/utils/supabase.ts
  - apps/ops/server/utils/auth.ts
  - apps/ops/server/utils/telegramFetch.ts
  - apps/ops/server/utils/telegramBroadcast.ts
  - apps/ops/server/api/health.get.ts
  - apps/ops/server/api/telegram/notify.post.ts
  - apps/ops/server/api/telegram/notify-reservation.post.ts
  - apps/ops/server/api/telegram/notify-appointment-group.post.ts
  - apps/ops/server/api/telegram/notify-table-call.post.ts
  - apps/ops/server/api/telegram/notify-alert.post.ts
  - apps/ops/server/api/telegram/send-appointment-reminders.post.ts
  - apps/ops/public/email-templates/invite.html
  - apps/ops/public/email-templates/recovery.html
  - apps/ops/public/email-templates/confirm_signup.html
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "GET /api/health на ops dev-сервере (port 4714) возвращает 200 со статусом ok"
    - "Все 6 Telegram-хендлеров скомпилированы (nuxt typecheck зелёный) и используют локальные ops utils"
    - "notify-alert.post.ts использует reportError из @fastio/shared/observability — НЕТ ни одного импорта @sentry/nuxt в apps/ops/"
    - "Все 3 email-шаблона доступны по dev URL http://localhost:4714/email-templates/{invite,recovery,confirm_signup}.html и идентичны исходникам в supabase/templates/"
    - "telegram-bot-setup плагин НЕ скопирован в ops (нет apps/ops/server/plugins/telegram-bot-setup.ts)"
  artifacts:
    - path: "apps/ops/server/api/health.get.ts"
      provides: "Coolify liveness probe — возвращает { status: 'ok' }"
      min_lines: 3
    - path: "apps/ops/server/api/telegram/notify.post.ts"
      provides: "Order notification handler (port из admin)"
      contains: "requireInternalSecret"
    - path: "apps/ops/server/api/telegram/notify-reservation.post.ts"
      provides: "Reservation notification handler"
      contains: "requireInternalSecret"
    - path: "apps/ops/server/api/telegram/notify-appointment-group.post.ts"
      provides: "Appointment group notification handler"
      contains: "requireInternalSecret"
    - path: "apps/ops/server/api/telegram/notify-table-call.post.ts"
      provides: "Table call notification handler"
      contains: "requireInternalSecret"
    - path: "apps/ops/server/api/telegram/notify-alert.post.ts"
      provides: "Edge errors alert handler (с reportError вместо Sentry.captureMessage)"
      contains: "reportError"
    - path: "apps/ops/server/api/telegram/send-appointment-reminders.post.ts"
      provides: "Cron handler reminding clients about upcoming appointments"
      contains: "reminderCronSecret"
    - path: "apps/ops/server/utils/auth.ts"
      provides: "ТОЛЬКО requireInternalSecret и requireRelaySecret (без requireTelegramWebhookSecret и requireMemberOfTenant)"
      contains: "requireInternalSecret"
    - path: "apps/ops/server/utils/supabase.ts"
      provides: "Service-role Supabase singleton (1:1 копия admin)"
      contains: "getServerSupabase"
    - path: "apps/ops/server/utils/telegramFetch.ts"
      provides: "Proxy-aware undici fetch wrapper"
      contains: "ProxyAgent"
    - path: "apps/ops/server/utils/telegramBroadcast.ts"
      provides: "broadcastToTenantTelegram с авто-pruning мёртвых подписок"
      contains: "broadcastToTenantTelegram"
    - path: "apps/ops/public/email-templates/invite.html"
      provides: "GoTrue invite template"
    - path: "apps/ops/public/email-templates/recovery.html"
      provides: "GoTrue password recovery template"
    - path: "apps/ops/public/email-templates/confirm_signup.html"
      provides: "GoTrue email confirmation template"
  key_links:
    - from: "apps/ops/server/api/telegram/*.post.ts"
      to: "apps/ops/server/utils/auth.ts"
      via: "requireInternalSecret import"
      pattern: "from '../../utils/auth'"
    - from: "apps/ops/server/api/telegram/notify-alert.post.ts"
      to: "@fastio/shared/observability"
      via: "reportError import (НЕ Sentry.captureMessage)"
      pattern: "from '@fastio/shared/observability'"
    - from: "apps/ops/server/utils/auth.ts"
      to: "apps/ops/server/utils/supabase.ts"
      via: "resetServerSupabase import (если оставлен — auth.ts его НЕ использует в ops-версии, но импорт может быть скопирован)"
      pattern: "from './supabase'"
---

<objective>
Перенести содержимое infrastructure HTTP-слоя из `apps/admin/server/` в `apps/ops/server/`. Это копирование (не git mv) — оригиналы в admin остаются работающими до конца Wave 3 (cutover & cleanup). Один файл (`notify-alert.post.ts`) требует адаптации: заменить прямой импорт `@sentry/nuxt` на `reportError` из `@fastio/shared/observability` (см. RESEARCH Pitfall 2).

Purpose: ops получает все 6 хендлеров и 4 util'a, чтобы Coolify-деплой ops.fastio.ru мог отвечать на pg_net/pg_cron вызовы. Email-шаблоны кладутся в `public/`, чтобы GoTrue мог их фетчить по HTTP.
Output: рабочий ops dev-server на порту 4714, который проходит typecheck и отвечает 200 на `/api/health`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-01-SUMMARY.md
@CLAUDE.md

<!-- Source files — копируй ТОЧНО, кроме явных адаптаций в action -->
@apps/admin/server/utils/supabase.ts
@apps/admin/server/utils/auth.ts
@apps/admin/server/utils/telegramFetch.ts
@apps/admin/server/utils/telegramBroadcast.ts
@apps/admin/server/api/telegram/notify.post.ts
@apps/admin/server/api/telegram/notify-reservation.post.ts
@apps/admin/server/api/telegram/notify-appointment-group.post.ts
@apps/admin/server/api/telegram/notify-table-call.post.ts
@apps/admin/server/api/telegram/notify-alert.post.ts
@apps/admin/server/api/telegram/send-appointment-reminders.post.ts
@supabase/templates/invite.html
@supabase/templates/recovery.html
@supabase/templates/confirm_signup.html

<interfaces>
<!-- Контракты, которые хендлеры ОЖИДАЮТ на стороне ops/server/utils/ -->

From apps/ops/server/utils/auth.ts (copy of admin/server/utils/auth.ts, trimmed):
```typescript
export function requireInternalSecret(event: H3Event): void
// Reads useRuntimeConfig().internalApiSecret, compares with x-internal-secret header.
// Throws 500 if env missing, 403 on mismatch.

export function requireRelaySecret(event: H3Event): void
// PREPROD-212 — defense-in-depth для Vercel relay. Dev-bypass через import.meta.dev.
// В ops НЕ используется ни одним хендлером (relay уходит в admin), но копируем для будущего.
```

From apps/ops/server/utils/supabase.ts (1:1 copy):
```typescript
export function getServerSupabase(): SupabaseClient
export function resetServerSupabase(): void
```

From apps/ops/server/utils/telegramFetch.ts (1:1 copy):
```typescript
export const TELEGRAM_API_BASE = 'https://api.telegram.org'
export function telegramApiUrl(token: string, method: string): string
export const telegramFetch: typeof fetch
```

From apps/ops/server/utils/telegramBroadcast.ts (1:1 copy):
```typescript
export type BroadcastResult = { sent: number; failed: number; removed: number }
export async function broadcastToTenantTelegram(
  supabase: SupabaseClient,
  token: string,
  tenantId: string,
  payloadBuilder: (sub: Subscriber) => Record<string, unknown>,
  logTag: string,
): Promise<BroadcastResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Скопировать 4 server utils в apps/ops/server/utils/ (с trimming auth.ts)</name>
  <files>apps/ops/server/utils/supabase.ts, apps/ops/server/utils/auth.ts, apps/ops/server/utils/telegramFetch.ts, apps/ops/server/utils/telegramBroadcast.ts</files>
  <read_first>
    - apps/admin/server/utils/supabase.ts (полностью — 1:1 копия)
    - apps/admin/server/utils/auth.ts (полностью — копируем ТОЛЬКО функции requireInternalSecret и requireRelaySecret, БЕЗ requireTelegramWebhookSecret и requireMemberOfTenant)
    - apps/admin/server/utils/telegramFetch.ts (полностью — 1:1 копия)
    - apps/admin/server/utils/telegramBroadcast.ts (полностью — 1:1 копия)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — таблица "Server utils that MOVE" (строки 70-78)
  </read_first>
  <action>
    Скопировать содержимое исходных файлов в `apps/ops/server/utils/`:

    1. `apps/ops/server/utils/supabase.ts` — БАЙТ-В-БАЙТ копия `apps/admin/server/utils/supabase.ts`. Никаких изменений: тот же singleton, тот же resetServerSupabase, тот же createClient через useRuntimeConfig.

    2. `apps/ops/server/utils/telegramFetch.ts` — БАЙТ-В-БАЙТ копия `apps/admin/server/utils/telegramFetch.ts`. Те же экспорты: `TELEGRAM_API_BASE`, `telegramApiUrl(token, method)`, `telegramFetch` (proxy-aware).

    3. `apps/ops/server/utils/telegramBroadcast.ts` — БАЙТ-В-БАЙТ копия `apps/admin/server/utils/telegramBroadcast.ts`. Импорт `./telegramFetch` остаётся как есть — резолвится через локальный путь.

    4. `apps/ops/server/utils/auth.ts` — УРЕЗАННАЯ копия. Скопировать TOЛЬКО:
       - имports: `createError, getHeader, type H3Event` from 'h3'; `useRuntimeConfig` from '#imports'
       - функция `requireInternalSecret(event)` — полный код из admin/auth.ts строки 13-25
       - функция `requireRelaySecret(event)` — полный код из admin/auth.ts строки 55-71

       НЕ копировать:
       - функцию `requireTelegramWebhookSecret` (нужна только для admin webhook.post.ts / auth-webhook.post.ts, которые остаются в admin)
       - функцию `requireMemberOfTenant` (нужна для admin endpoints с пользовательской сессией — в ops нет таких endpoints)
       - импорты `createClient` from '@supabase/supabase-js', `reportError` from '@fastio/shared/observability', `getServerSupabase, resetServerSupabase` from './supabase' — все они нужны были только для requireMemberOfTenant
  </action>
  <verify>
    <automated>diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/utils/supabase.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/utils/supabase.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/utils/telegramFetch.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/utils/telegramFetch.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/utils/telegramBroadcast.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/utils/telegramBroadcast.ts && grep -c 'requireInternalSecret\|requireRelaySecret' /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/utils/auth.ts | awk '$1 >= 2' && grep -cE 'requireTelegramWebhookSecret|requireMemberOfTenant' /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/utils/auth.ts | awk '$1 == 0'</automated>
  </verify>
  <done>
    - `apps/ops/server/utils/supabase.ts` — diff с admin/utils/supabase.ts пустой
    - `apps/ops/server/utils/telegramFetch.ts` — diff с admin/utils/telegramFetch.ts пустой
    - `apps/ops/server/utils/telegramBroadcast.ts` — diff с admin/utils/telegramBroadcast.ts пустой
    - `apps/ops/server/utils/auth.ts` содержит requireInternalSecret и requireRelaySecret
    - `apps/ops/server/utils/auth.ts` НЕ содержит requireTelegramWebhookSecret и requireMemberOfTenant
    - `grep -r '@fastio/shared/observability' apps/ops/server/utils/auth.ts` — пусто
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать health endpoint + скопировать 5 простых notify-хендлеров (notify, notify-reservation, notify-appointment-group, notify-table-call, send-appointment-reminders) — БЕЗ изменений</name>
  <files>apps/ops/server/api/health.get.ts, apps/ops/server/api/telegram/notify.post.ts, apps/ops/server/api/telegram/notify-reservation.post.ts, apps/ops/server/api/telegram/notify-appointment-group.post.ts, apps/ops/server/api/telegram/notify-table-call.post.ts, apps/ops/server/api/telegram/send-appointment-reminders.post.ts</files>
  <read_first>
    - apps/admin/server/api/telegram/notify.post.ts (полностью)
    - apps/admin/server/api/telegram/notify-reservation.post.ts (полностью)
    - apps/admin/server/api/telegram/notify-appointment-group.post.ts (полностью)
    - apps/admin/server/api/telegram/notify-table-call.post.ts (полностью)
    - apps/admin/server/api/telegram/send-appointment-reminders.post.ts (полностью)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — таблица "Handlers that MOVE to apps/ops/" (строки 48-55)
  </read_first>
  <action>
    Создать `apps/ops/server/api/health.get.ts` — простейший liveness probe:
    ```ts
    import { defineEventHandler } from 'h3'

    // Coolify liveness probe. Намеренно НЕ требует auth — endpoint публичный.
    // НЕ дёргает базу (Supabase): liveness ≠ readiness, мы не хотим, чтобы Coolify
    // считал контейнер нездоровым из-за лага БД и рестартил его.
    export default defineEventHandler(() => ({ status: 'ok' }))
    ```

    Скопировать БАЙТ-В-БАЙТ из admin в ops следующие 5 хендлеров:

    1. `apps/admin/server/api/telegram/notify.post.ts` → `apps/ops/server/api/telegram/notify.post.ts`
    2. `apps/admin/server/api/telegram/notify-reservation.post.ts` → `apps/ops/server/api/telegram/notify-reservation.post.ts`
    3. `apps/admin/server/api/telegram/notify-appointment-group.post.ts` → `apps/ops/server/api/telegram/notify-appointment-group.post.ts`
    4. `apps/admin/server/api/telegram/notify-table-call.post.ts` → `apps/ops/server/api/telegram/notify-table-call.post.ts`
    5. `apps/admin/server/api/telegram/send-appointment-reminders.post.ts` → `apps/ops/server/api/telegram/send-appointment-reminders.post.ts`

    Все импорты в этих хендлерах ОТНОСИТЕЛЬНЫЕ (`../../utils/supabase`, `../../utils/auth`, `../../utils/telegramBroadcast`, `../../utils/telegramFetch`, `@fastio/shared`, `@fastio/shared/observability`, `#imports`, `h3`). Они автоматически зарезолвятся к ops-локальным utils, скопированным в Task 1.

    НЕ копировать (остаются в admin):
    - `webhook.post.ts` (Vercel relay hardcodes admin.fastio.ru)
    - `auth-webhook.post.ts` (та же причина)
    - `glitchtip-alert.post.ts` (GlitchTip alert URL — defer, см. Wave 3 TECHDEBT entry)

    notify-alert.post.ts обрабатывается ОТДЕЛЬНО в Task 3 — требует Sentry → reportError адаптацию.
  </action>
  <verify>
    <automated>diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify.post.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify.post.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-reservation.post.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-reservation.post.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-appointment-group.post.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-appointment-group.post.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-table-call.post.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-table-call.post.ts && diff /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/send-appointment-reminders.post.ts /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/send-appointment-reminders.post.ts && grep -c "status: 'ok'" /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/health.get.ts</automated>
  </verify>
  <done>
    - `apps/ops/server/api/health.get.ts` существует и возвращает `{ status: 'ok' }`
    - 5 хендлеров (notify, notify-reservation, notify-appointment-group, notify-table-call, send-appointment-reminders) скопированы байт-в-байт (все diff пустые)
    - `webhook.post.ts`, `auth-webhook.post.ts`, `glitchtip-alert.post.ts` ОТСУТСТВУЮТ в apps/ops/server/api/telegram/
    - `apps/ops/server/plugins/` директория НЕ создана (см. Pitfall 3 — telegram-bot-setup плагин должен остаться в admin)
  </done>
</task>

<task type="auto">
  <name>Task 3: Скопировать notify-alert.post.ts с заменой @sentry/nuxt → reportError (RESEARCH Pitfall 2)</name>
  <files>apps/ops/server/api/telegram/notify-alert.post.ts</files>
  <read_first>
    - apps/admin/server/api/telegram/notify-alert.post.ts (полностью — это исходник, который надо адаптировать; обратить внимание на строки 2 и 56)
    - apps/admin/server/api/telegram/send-appointment-reminders.post.ts (строка 6 — паттерн как импортируется reportError: `import { reportError } from '@fastio/shared/observability'`)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Notes on notify-alert.post.ts" (строка 57) и "Pitfall 2: notify-alert.post.ts uses @sentry/nuxt directly" (строки 377-381)
  </read_first>
  <action>
    Скопировать `apps/admin/server/api/telegram/notify-alert.post.ts` в `apps/ops/server/api/telegram/notify-alert.post.ts` со следующими ТОЧНЫМИ изменениями:

    1. Удалить строку 2: `import * as Sentry from '@sentry/nuxt'`
    2. Добавить вместо неё (после строки `import { useRuntimeConfig } from '#imports'`):
       `import { reportError } from '@fastio/shared/observability'`
    3. На строке 56 заменить:
       ```ts
       Sentry.captureMessage(`telegram notify-alert sendMessage failed: ${detail}`, 'error')
       ```
       на:
       ```ts
       reportError(new Error(`telegram notify-alert sendMessage failed: ${detail}`))
       ```

    Всё остальное содержимое файла (логика чтения body, `requireInternalSecret`, формирование текста, вызов telegramFetch) — БЕЗ изменений. Эта замена единственная семантическая разница между ops и admin версиями notify-alert.

    Финальная структура импортов ops/notify-alert.post.ts:
    ```ts
    import { defineEventHandler, readBody } from 'h3'
    import { useRuntimeConfig } from '#imports'
    import { reportError } from '@fastio/shared/observability'
    import { requireInternalSecret } from '../../utils/auth'
    import { telegramApiUrl, telegramFetch } from '../../utils/telegramFetch'
    ```

    Никаких `@sentry/nuxt` импортов в `apps/ops/` быть не должно.
  </action>
  <verify>
    <automated>grep -c "from '@sentry/nuxt'" /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-alert.post.ts | awk '$1 == 0' && grep -c "from '@fastio/shared/observability'" /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-alert.post.ts | awk '$1 >= 1' && grep -c "reportError(new Error" /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/api/telegram/notify-alert.post.ts | awk '$1 >= 1' && (grep -rEn '@sentry/nuxt' /Users/evgeniy/WebstormProjects/fastio/apps/ops/server/ ; true) | grep -v '^$' | grep -cE '.' | awk '$1 == 0'</automated>
  </verify>
  <done>
    - `apps/ops/server/api/telegram/notify-alert.post.ts` существует
    - НЕТ строки `import * as Sentry from '@sentry/nuxt'`
    - ЕСТЬ строка `import { reportError } from '@fastio/shared/observability'`
    - Вызов `Sentry.captureMessage(...)` заменён на `reportError(new Error(...))`
    - `grep -r '@sentry/nuxt' apps/ops/server/` — пусто (никакой остаточной утечки Sentry-импортов в server/)
    - Остальная логика (errorCount/totalCount парсинг, formatting, telegramFetch вызов) идентична admin-версии
  </done>
</task>

<task type="auto">
  <name>Task 4: Скопировать 3 email-шаблона в apps/ops/public/email-templates/ + smoke-test dev server</name>
  <files>apps/ops/public/email-templates/invite.html, apps/ops/public/email-templates/recovery.html, apps/ops/public/email-templates/confirm_signup.html</files>
  <read_first>
    - supabase/templates/invite.html (источник — НЕ удалять оригинал, локальный dev Supabase его читает через config.toml content_path)
    - supabase/templates/recovery.html
    - supabase/templates/confirm_signup.html
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Email templates that MOVE" (строки 84-93) и "GoTrue Email Template Configuration" (строки 195-206)
  </read_first>
  <action>
    Скопировать (НЕ переместить — оригиналы остаются для локального dev Supabase) три HTML-шаблона:

    1. `supabase/templates/invite.html` → `apps/ops/public/email-templates/invite.html`
    2. `supabase/templates/recovery.html` → `apps/ops/public/email-templates/recovery.html`
    3. `supabase/templates/confirm_signup.html` → `apps/ops/public/email-templates/confirm_signup.html`

    Файлы должны быть идентичны байт-в-байт (md5sum совпадает). Никаких правок содержимого — GoTrue использует свои собственные плейсхолдеры (`{{ .ConfirmationURL }}` и т.п.), которые уже в шаблонах. Менять плейсхолдеры здесь нельзя — это сломает GoTrue.

    После копирования запустить smoke-тест dev-сервера ops:
    1. В отдельном терминале (или через run_in_background): `pnpm --filter @fastio/ops dev`
    2. Дождаться вывода `Listening on http://localhost:4714` (Nuxt стартует ~3-10 сек на холодную)
    3. Проверить три ответа:
       - `curl -s -o /dev/null -w "%{http_code}" http://localhost:4714/api/health` → должен быть `200`
       - `curl -s http://localhost:4714/api/health | jq -r .status` → должен быть `ok`
       - `curl -s -o /dev/null -w "%{http_code}" http://localhost:4714/email-templates/invite.html` → `200`
       - `diff <(curl -s http://localhost:4714/email-templates/invite.html) supabase/templates/invite.html` → пусто
    4. Остановить dev-server (ctrl+c или kill background process).
  </action>
  <verify>
    <automated>md5sum /Users/evgeniy/WebstormProjects/fastio/supabase/templates/invite.html /Users/evgeniy/WebstormProjects/fastio/apps/ops/public/email-templates/invite.html | awk '{print $1}' | sort -u | wc -l | awk '$1 == 1' && md5sum /Users/evgeniy/WebstormProjects/fastio/supabase/templates/recovery.html /Users/evgeniy/WebstormProjects/fastio/apps/ops/public/email-templates/recovery.html | awk '{print $1}' | sort -u | wc -l | awk '$1 == 1' && md5sum /Users/evgeniy/WebstormProjects/fastio/supabase/templates/confirm_signup.html /Users/evgeniy/WebstormProjects/fastio/apps/ops/public/email-templates/confirm_signup.html | awk '{print $1}' | sort -u | wc -l | awk '$1 == 1' && cd /Users/evgeniy/WebstormProjects/fastio && pnpm --filter @fastio/ops typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 3 html-файла лежат в `apps/ops/public/email-templates/`, md5 каждого совпадает с исходником в `supabase/templates/`
    - Исходники в `supabase/templates/` НЕ удалены (локальный dev Supabase их продолжает использовать)
    - `pnpm --filter @fastio/ops typecheck` exit 0
    - `pnpm --filter @fastio/ops dev` стартует на порту 4714, `/api/health` отвечает `{"status":"ok"}` (HTTP 200), `/email-templates/invite.html` отдаёт идентичное содержимое исходнику
    - Никаких ошибок Nitro при старте dev-сервера (типа missing #imports, неразрешённых импортов)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_net trigger / pg_cron → ops Nitro | x-internal-secret / x-reminder-cron-secret в HTTP header |
| Future: GoTrue → ops (Wave 3) | HTTP GET email-template — публичные HTML, шаблоны не содержат секретов |
| ops Nitro → Telegram Bot API | через telegramFetch + ProxyAgent (sing-box, RKN bypass) |
| ops Nitro → Supabase (service-role) | через service-role JWT, тот же ключ что в admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03 | Spoofing | notify endpoints | mitigate | requireInternalSecret() — 500 если env missing (secure by default), 403 на mismatch — БАЙТ-В-БАЙТ копия из admin auth.ts |
| T-04.1-04 | Spoofing | send-appointment-reminders endpoint | mitigate | Inline x-reminder-cron-secret check — 500 если env missing, 403 на mismatch (см. send-appointment-reminders.post.ts строки 22-37) |
| T-04.1-05 | Tampering | notify-alert.post.ts (Sentry replace) | mitigate | reportError из @fastio/shared/observability — same fingerprint behavior + Coolify-compatible without @sentry/nuxt module wired в ops; код проверяется grep'ом в acceptance criteria |
| T-04.1-06 | Information Disclosure | Email templates served publicly | accept | Шаблоны содержат только GoTrue-плейсхолдеры (`{{ .ConfirmationURL }}`), не статичные секреты; они и так публичны через GoTrue email |
| T-04.1-07 | DoS | Public /api/health endpoint | accept | Тривиальный JSON, не дёргает БД (specifically designed чтобы Coolify не рестартил pod при лаге БД); rate-limit на уровне Coolify/Traefik |
| T-04.1-08 | Spoofing | Future webhook receivers (если кто-то скопирует webhook.post.ts) | mitigate | requireTelegramWebhookSecret НЕ скопирован в ops/utils/auth.ts — даже если кто-то скопирует webhook.post.ts вручную, он не скомпилируется без missing utility |
| T-04.1-SC | Tampering | Не устанавливаем новых пакетов | accept | Все импорты резолвятся к workspace-deps, уже установленным в Wave 1 |
</threat_model>

<verification>
- `pnpm --filter @fastio/ops typecheck` exit 0 (никаких ошибок типов в скопированных файлах)
- `pnpm --filter @fastio/ops dev` стартует и отвечает на /api/health и /email-templates/invite.html
- `grep -r '@sentry/nuxt' apps/ops/` — пусто
- `grep -r 'requireMemberOfTenant\|requireTelegramWebhookSecret' apps/ops/server/utils/auth.ts` — пусто
- 3 email-шаблона md5-идентичны исходникам в supabase/templates/
- 5 простых хендлеров diff-идентичны исходникам в admin
- `ls apps/ops/server/plugins/ 2>/dev/null` — пусто (telegram-bot-setup плагин НЕ скопирован)
</verification>

<success_criteria>
1. `apps/ops/server/api/health.get.ts` отдаёт `{"status":"ok"}` (HTTP 200) на dev-сервере (port 4714)
2. Все 6 Telegram-хендлеров присутствуют в `apps/ops/server/api/telegram/`: notify, notify-reservation, notify-appointment-group, notify-table-call, notify-alert, send-appointment-reminders
3. 4 util-файла присутствуют в `apps/ops/server/utils/`: supabase.ts, auth.ts (только requireInternalSecret + requireRelaySecret), telegramFetch.ts, telegramBroadcast.ts
4. notify-alert.post.ts использует `reportError` из `@fastio/shared/observability` — НЕТ ни одного импорта `@sentry/nuxt` в `apps/ops/server/`
5. `pnpm --filter @fastio/ops typecheck` exit 0
6. 3 email-шаблона (invite.html, recovery.html, confirm_signup.html) лежат в `apps/ops/public/email-templates/`, md5-идентичны исходникам в `supabase/templates/`
7. Никаких файлов в `apps/ops/server/plugins/` (нет telegram-bot-setup в ops — см. RESEARCH Pitfall 3)
8. Никаких изменений в `apps/admin/server/` — оригиналы продолжают работать до Wave 3
</success_criteria>

<output>
Создать `.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-02-SUMMARY.md` после выполнения. Зафиксировать вывод smoke-теста dev-сервера (curl /api/health, /email-templates/invite.html), результат `pnpm typecheck`, и any deltas в скопированных хендлерах сверх предписанного notify-alert Sentry-фикса.
</output>
