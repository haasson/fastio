---
phase: 04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ops/package.json
  - apps/ops/nuxt.config.ts
  - apps/ops/tsconfig.json
  - apps/ops/eslint.config.mjs
  - apps/ops/.gitignore
  - pnpm-workspace.yaml
  - vitest.config.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "pnpm install в корне репо создаёт node_modules в apps/ops без ошибок зависимостей и без drift Vue версии"
    - "pnpm --filter @fastio/ops typecheck завершается с exit 0 (тип-чек скаффолда успешен)"
    - "vitest подхватывает файлы вида apps/ops/**/*.test.ts (для будущих тестов из Wave 2)"
  artifacts:
    - path: "apps/ops/package.json"
      provides: "Описание @fastio/ops с минимальными деps (nuxt, supabase-js, undici, sentry, shared)"
      contains: "@fastio/ops"
    - path: "apps/ops/nuxt.config.ts"
      provides: "Nitro-only Nuxt конфиг: ssr:false, autoImport:false, components:false, без UI-модулей"
      contains: "defineNuxtConfig"
    - path: "apps/ops/tsconfig.json"
      provides: "TS extends .nuxt/tsconfig.json (тот же паттерн, что в admin/backoffice)"
    - path: "apps/ops/eslint.config.mjs"
      provides: "Минимальный ESLint конфиг, наследуется от @fastio/shared/configs/eslint"
    - path: "pnpm-workspace.yaml"
      provides: "apps/* glob уже покрывает apps/ops — никакой ручной добавки не нужно, но валидируем"
      contains: "apps/*"
    - path: "vitest.config.ts"
      provides: "include array содержит 'apps/ops/**/*.test.ts'"
      contains: "apps/ops/**/*.test.ts"
  key_links:
    - from: "apps/ops/package.json"
      to: "package.json (root pnpm.overrides)"
      via: "vue version pinning"
      pattern: "\"vue\":\\s*\"\\^3\\.5\\.13\""
    - from: "apps/ops/nuxt.config.ts"
      to: "runtime env vars"
      via: "runtimeConfig keys"
      pattern: "internalApiSecret|reminderCronSecret|telegramTenantBotToken"
---

<objective>
Создать каркас отдельного Nitro-only Nuxt-приложения `apps/ops/` — без Vue UI, без features/, без auto-imports. Это foundation для Wave 2 (миграция Telegram notify-хендлеров) и Wave 3 (cutover). Включает обновление workspace-конфига (pnpm-workspace.yaml уже покрывает apps/*, валидируем) и расширение vitest include на `apps/ops/**/*.test.ts`.

Purpose: разделить admin и infrastructure HTTP-слой по архитектурным границам — admin SPA не должен быть платформенной инфраструктурой. INFRA-01 явно требует Nuxt/Nitro-сервис без UI на ops.fastio.ru.
Output: компилирующийся пустой Nuxt-app в `apps/ops/`, готовый к копированию хендлеров.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md
@CLAUDE.md

<!-- Source-of-truth для шаблонов конфигурации. Не угадывай — копируй паттерн. -->
@apps/admin/nuxt.config.ts
@apps/backoffice/nuxt.config.ts
@apps/admin/package.json
@apps/admin/tsconfig.json
@apps/admin/eslint.config.mjs
@pnpm-workspace.yaml
@vitest.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать пакетный скаффолд apps/ops/ (package.json + tsconfig + eslint + .gitignore)</name>
  <files>apps/ops/package.json, apps/ops/tsconfig.json, apps/ops/eslint.config.mjs, apps/ops/.gitignore</files>
  <read_first>
    - apps/admin/package.json (источник версий и engines)
    - apps/backoffice/package.json (минимальный пакет без UI — ближайший аналог по форме)
    - apps/admin/tsconfig.json (паттерн extends .nuxt/tsconfig.json)
    - package.json (root) — секция pnpm.overrides пиннит vue 3.5.34; в apps/ops/package.json указываем "vue": "^3.5.13", чтобы не было drift
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "apps/ops/package.json skeleton" (строки 320-352) и "Pitfall 7: pnpm workspace — no Vue version drift"
  </read_first>
  <action>
    Создать `apps/ops/package.json` со следующим содержимым:
    - `"name": "@fastio/ops"`, `"private": true`
    - scripts: `dev` (nuxt dev), `build` (nuxt build), `preview` (nuxt preview), `typecheck` (nuxt typecheck), `lint` (eslint .), `lint:fix` (eslint . --fix)
    - dependencies (точные строки версий — копируй из admin/package.json):
      * `@fastio/shared: workspace:*`
      * `@sentry/nuxt: ^10.47.0` — для будущей инструментации (env NUXT_PUBLIC_SENTRY_DSN), пока не активирован модулем (см. Task 2)
      * `@supabase/supabase-js: ^2.98.0`
      * `nuxt: ^3.21.6`
      * `undici: ^7.25.0`
      * `vue: ^3.5.13` — версия должна совпасть с admin, реальный pin делает pnpm.overrides в root package.json (см. Pitfall 7)
    - devDependencies:
      * `eslint: ^9.35.0`
      * `typescript: ^5.7.2`
    - `engines.node: ">=20"`

    Создать `apps/ops/tsconfig.json` со следующим содержимым (точная копия admin/tsconfig.json):
    ```json
    {
      "extends": "./.nuxt/tsconfig.json"
    }
    ```

    Создать `apps/ops/eslint.config.mjs` — минимальный конфиг без vertical isolation rules (в ops нет features/), наследуется от `@fastio/shared/configs/eslint`. Достаточно одной строки экспорта default array из shared eslint config + блока для `server/**` с `'no-restricted-imports': 'off'` (паттерн из admin/eslint.config.mjs строки 295-299).

    Создать `apps/ops/.gitignore` со стандартными игнорами Nuxt:
    ```
    .nuxt
    .output
    node_modules
    dist
    .env
    .env.local
    *.log
    ```

    NOT включать в package.json: naive-ui, tiptap, ApexCharts, driver.js, pinia, vueuse, sass, dompurify, lru-cache, qrcode, vue-yandex-maps, jspdf, ai, openai — это всё UI/Vue-stack, ops его не использует.
  </action>
  <verify>
    <automated>cd /Users/evgeniy/WebstormProjects/fastio && pnpm install --frozen-lockfile=false 2>&1 | tail -20 && jq -e '.name == "@fastio/ops" and .dependencies.nuxt' apps/ops/package.json && grep -c '"vue": "\^3.5.13"' apps/ops/package.json</automated>
  </verify>
  <done>
    - `apps/ops/package.json` существует с `"name": "@fastio/ops"`, всеми 6 deps и 2 devDeps из списка
    - `pnpm install` в корне завершается успешно, в `apps/ops/node_modules/` появился `nuxt`
    - `apps/ops/tsconfig.json` существует с extends на `./.nuxt/tsconfig.json`
    - `apps/ops/eslint.config.mjs` существует и импортирует `@fastio/shared/configs/eslint`
    - `apps/ops/.gitignore` существует и игнорирует `.nuxt`, `.output`, `node_modules`
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать nuxt.config.ts для apps/ops/ (Nitro-only режим, ssr:false, без UI)</name>
  <files>apps/ops/nuxt.config.ts</files>
  <read_first>
    - apps/admin/nuxt.config.ts (паттерн runtimeConfig + Sentry — но в ops не активируем @sentry/nuxt module пока, см. Open Question 3 в RESEARCH)
    - apps/backoffice/nuxt.config.ts (минимальная форма ssr:false + autoImport:false + runtimeConfig — ближайший аналог)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секции "Env Vars apps/ops/ Needs" (строки 211-247), "Recommended apps/ops/ Structure" (строки 250-316), "Pitfall 6: Nuxt app.vue is required for ssr:true" (строки 401-417)
  </read_first>
  <action>
    Создать `apps/ops/nuxt.config.ts` со следующей структурой (RESEARCH рекомендует `ssr: false` чтобы избежать app.vue требования — Nitro серверная часть работает независимо от SSR):

    ```ts
    export default defineNuxtConfig({
      compatibilityDate: '2024-11-01',

      devServer: { port: 4714 },

      ssr: false,

      imports: {
        autoImport: false,
      },

      components: false,

      // NB: @sentry/nuxt модуль НЕ подключаем в Wave 1 — добавим follow-up tasks позже,
      // когда GlitchTip-проект для ops будет создан (см. RESEARCH Open Question 3).
      // Сам пакет в deps есть, чтобы reportError из @fastio/shared/observability работал
      // если в Wave 2 кому-то понадобится.

      runtimeConfig: {
        supabaseServiceRoleKey: '',      // NUXT_SUPABASE_SERVICE_ROLE_KEY
        internalApiSecret: '',           // NUXT_INTERNAL_API_SECRET — для requireInternalSecret()
        reminderCronSecret: '',          // NUXT_REMINDER_CRON_SECRET — для send-appointment-reminders
        telegramTenantBotToken: '',      // NUXT_TELEGRAM_TENANT_BOT_TOKEN — notify, notify-reservation, notify-appointment-group, notify-table-call
        telegramClientBotToken: '',      // NUXT_TELEGRAM_CLIENT_BOT_TOKEN — send-appointment-reminders
        telegramOpsBotToken: '',         // NUXT_TELEGRAM_OPS_BOT_TOKEN — notify-alert
        telegramAlertChatId: '',         // NUXT_TELEGRAM_ALERT_CHAT_ID — chat id для notify-alert (не в vault by design)
        telegramProxyUrl: '',            // NUXT_TELEGRAM_PROXY_URL — sing-box HTTP proxy для RKN bypass
        adminUrl: '',                    // NUXT_ADMIN_URL — для inline-кнопок "Позвонить" в notify хендлерах
        public: {
          supabaseUrl: '',               // NUXT_PUBLIC_SUPABASE_URL — используется в utils/supabase.ts
        },
      },
    })
    ```

    Ключи runtimeConfig точно повторяют те, что используются в admin (см. admin/nuxt.config.ts строки 101-129 и admin/server/api/telegram/*.ts), за вычетом ключей которые ops не использует: openaiApiKey, telegramWebhookSecret, telegramAuthWebhookUrl, telegramTenantWebhookUrl, glitchtipWebhookSecret, relaySecret, dadataApiKey, supabaseAnonKey, yandexMapsApiKey, telegramTenantBotUsername, helpUrl, auditLogEnabled, sentryDsn.

    НЕ добавляем: `modules`, `css`, `app.head`, `nitro.storage` (нет AI knowledge / KB), `routeRules` — ops не serv'ит HTML.
  </action>
  <verify>
    <automated>cd /Users/evgeniy/WebstormProjects/fastio && pnpm --filter @fastio/ops exec nuxt prepare 2>&1 | tail -10 && grep -c 'internalApiSecret\|reminderCronSecret\|telegramTenantBotToken\|telegramOpsBotToken\|telegramAlertChatId\|adminUrl' apps/ops/nuxt.config.ts | awk '$1 >= 6'</automated>
  </verify>
  <done>
    - `apps/ops/nuxt.config.ts` существует
    - `ssr: false`, `imports: { autoImport: false }`, `components: false` присутствуют
    - runtimeConfig содержит все 9 server keys (supabaseServiceRoleKey, internalApiSecret, reminderCronSecret, telegramTenantBotToken, telegramClientBotToken, telegramOpsBotToken, telegramAlertChatId, telegramProxyUrl, adminUrl) и public.supabaseUrl
    - `pnpm --filter @fastio/ops exec nuxt prepare` отрабатывает без ошибок, создаёт apps/ops/.nuxt/tsconfig.json
    - Конфиг НЕ содержит `modules`, `app.head`, `nitro.storage`, `routeRules`, `css`
  </done>
</task>

<task type="auto">
  <name>Task 3: Обновить vitest.config.ts (добавить apps/ops/**/*.test.ts в include)</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (полностью — там include array строки 29-36)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Wave 0 Gaps" (строки 509-514) и "Validation Architecture" (строки 487-498)
    - pnpm-workspace.yaml — валидировать, что `apps/*` glob УЖЕ покрывает apps/ops (никакой ручной правки workspace-файла НЕ требуется)
  </read_first>
  <action>
    В `vitest.config.ts` в массиве `test.include` (строки 29-36) добавить новую строку `'apps/ops/**/*.test.ts'` после `'apps/backoffice/**/*.test.ts'` (строка 34). Финальный массив должен содержать 7 паттернов:
    ```ts
    include: [
      'packages/shared/src/**/*.test.ts',
      'packages/public-ui/src/**/*.test.ts',
      'apps/admin/**/*.test.ts',
      'apps/storefront/**/*.test.ts',
      'apps/backoffice/**/*.test.ts',
      'apps/ops/**/*.test.ts',
      'supabase/tests/**/*.test.ts',
    ],
    ```

    Алиасы (resolve.alias) НЕ трогаем: ops использует те же `@fastio/shared`, `@fastio/shared/server`, `@fastio/shared/observability` пути, которые уже задействованы для admin. `~/` alias указывает на admin — для ops тестов это OK, потому что Wave 2 будет писать тесты с относительными импортами (`../../utils/auth` и т.п.), не через `~/`.

    pnpm-workspace.yaml уже содержит `apps/*` (строка 2), который покрывает `apps/ops`. Ручной правки НЕ требуется — валидируем grep'ом что ops подхватывается.
  </action>
  <verify>
    <automated>grep -c "apps/ops/\*\*/\*.test.ts" /Users/evgeniy/WebstormProjects/fastio/vitest.config.ts && cd /Users/evgeniy/WebstormProjects/fastio && pnpm list --filter @fastio/ops 2>&1 | grep -c "@fastio/ops"</automated>
  </verify>
  <done>
    - `vitest.config.ts` include array содержит `'apps/ops/**/*.test.ts'` (новая строка)
    - `pnpm list --filter @fastio/ops` показывает пакет (workspace его подхватил через `apps/*` glob)
    - `pnpm vitest run apps/ops/dummy.test.ts 2>&1 | grep -v "No test files found"` — отсутствие test files OK на этом этапе (Wave 2 их создаст); главное чтобы vitest НЕ ругался на отсутствие паттерна
  </done>
</task>

<task id="4" type="auto">
  <title>Wave 0 test stubs: health, auth, notify</title>
  <files>
    - apps/ops/server/api/health.get.test.ts (создать)
    - apps/ops/server/utils/auth.test.ts (создать)
    - apps/ops/server/api/telegram/notify.post.test.ts (создать)
    - apps/admin/server/api/telegram/__tests__/auth-webhook.post.test.ts (read — паттерн для vi.mock структуры)
  </files>
  <read_first>
    - apps/admin/server/api/telegram/__tests__/auth-webhook.post.test.ts — паттерн vi.mock для #imports, supabase, telegramFetch, auth
    - apps/admin/server/api/health.get.ts — что именно возвращает health endpoint (для acceptance criteria теста)
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-VALIDATION.md — Wave 0 Requirements
  </read_first>
  <action>
    Создать три минимальных test-стаба используя шаблон из `auth-webhook.post.test.ts`.

    `apps/ops/server/api/health.get.test.ts`:
    - `import { describe, it, expect } from 'vitest'`
    - `describe('health.get', () => { it('returns status ok', () => { expect(true).toBe(true) }) })`
    - Это placeholder — Wave 2 расширит до реального теста после копирования хендлера.

    `apps/ops/server/utils/auth.test.ts`:
    - `import { describe, it, expect, vi } from 'vitest'`
    - vi.mock структуры аналогично auth-webhook.post.test.ts (мокаем nuxt `#imports`)
    - describe('requireInternalSecret', ...) с placeholder it.todo('returns 403 on wrong secret')

    `apps/ops/server/api/telegram/notify.post.test.ts`:
    - Аналогичный placeholder структуры
    - describe('notify.post', ...) с it.todo('processes order notification body')

    Все три файла должны: (1) компилироваться без ошибок TypeScript, (2) успешно запускаться vitest (exit 0 даже с todo/pending тестами).
  </action>
  <verify>
    <automated>cd /Users/evgeniy/WebstormProjects/fastio && pnpm vitest run apps/ops/server/api/health.get.test.ts apps/ops/server/utils/auth.test.ts apps/ops/server/api/telegram/notify.post.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Все три test-файла существуют в указанных путях
    - `pnpm vitest run apps/ops/server/api/health.get.test.ts apps/ops/server/utils/auth.test.ts apps/ops/server/api/telegram/notify.post.test.ts` завершается exit 0
    - Тесты могут быть todo/pending — главное что vitest не падает на отсутствующие файлы
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build → workspace install | pnpm подтягивает все пакеты из workspace, drift версий vue ломает реактивность |
| Future: pg_net → ops | Будет в Wave 2 — на этом этапе ops ничего не принимает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | apps/ops/package.json vue version | mitigate | Указать "vue": "^3.5.13" точно как в admin/package.json; root pnpm.overrides пиннит 3.5.34 |
| T-04.1-02 | Spoofing | npm package "@fastio/ops" | accept | Private workspace package, не публикуется в registry; private:true в package.json |
| T-04.1-SC | Tampering | npm/pnpm installs | mitigate | Все deps уже присутствуют в admin (legitimacy validated); никаких новых пакетов в Wave 1 — только воркспейс-копии |
</threat_model>

<verification>
- `pnpm install` в корне отрабатывает без ошибок и без warnings о peer deps Vue
- `pnpm --filter @fastio/ops exec nuxt prepare` создаёт `apps/ops/.nuxt/` с tsconfig.json
- `pnpm --filter @fastio/ops typecheck` exit 0 (на скаффолде без хендлеров)
- `grep -r 'naive-ui\|tiptap\|apexcharts\|driver.js\|pinia' apps/ops/package.json` — пусто (UI-stack не утащили случайно)
- `vitest.config.ts include` содержит 'apps/ops/**/*.test.ts'
</verification>

<success_criteria>
1. `apps/ops/` существует с 5 файлами: package.json, nuxt.config.ts, tsconfig.json, eslint.config.mjs, .gitignore
2. `pnpm install && pnpm --filter @fastio/ops typecheck` exit 0
3. Vitest подхватывает `apps/ops/**/*.test.ts` паттерн
4. Vue version совпадает с admin (^3.5.13 в deps, root pnpm.overrides пиннит 3.5.34)
5. apps/ops/package.json НЕ содержит UI-deps (naive-ui, tiptap, apexcharts, driver.js, pinia, vueuse, sass, vue-yandex-maps, jspdf, ai/openai)
6. Wave 0 test stubs созданы и `pnpm vitest run apps/ops/server/api/health.get.test.ts apps/ops/server/utils/auth.test.ts apps/ops/server/api/telegram/notify.post.test.ts` завершается exit 0
</success_criteria>

<output>
Создать `.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-01-SUMMARY.md` после выполнения с фиксацией версий установленных пакетов (pnpm list --filter @fastio/ops) и любых отклонений от плана.
</output>
