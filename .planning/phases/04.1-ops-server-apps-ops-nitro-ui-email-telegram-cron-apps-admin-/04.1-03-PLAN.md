---
phase: 04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-
plan: 03
type: execute
wave: 3
depends_on:
  - 04.1-02
files_modified:
  - apps/admin/server/api/telegram/notify.post.ts
  - apps/admin/server/api/telegram/notify-reservation.post.ts
  - apps/admin/server/api/telegram/notify-appointment-group.post.ts
  - apps/admin/server/api/telegram/notify-table-call.post.ts
  - apps/admin/server/api/telegram/notify-alert.post.ts
  - apps/admin/server/api/telegram/send-appointment-reminders.post.ts
  - TECHDEBT.md
autonomous: false

requirements:
  - INFRA-01

must_haves:
  truths:
    - "ops.fastio.ru DNS A-record указывает на 109.71.242.205 и резолвится"
    - "GET https://ops.fastio.ru/api/health возвращает HTTP 200 с {\"status\":\"ok\"}"
    - "6 vault.secrets URL-записей указывают на ops.fastio.ru (не admin.fastio.ru)"
    - "Минимум одно тестовое уведомление (создан тестовый order или table_call) долетело в Telegram-канал тенанта после vault-обновления"
    - "GoTrue env vars GOTRUE_MAILER_TEMPLATES_INVITE/RECOVERY/CONFIRMATION указывают на ops.fastio.ru и Supabase-сервис перезапущен"
    - "Кастомный invite-шаблон активен: тестовый invite через Supabase Studio приходит с брендингом из ops/public/email-templates/invite.html (не дефолтный GoTrue)"
    - "В apps/admin/server/api/telegram/ остались только 4 файла: webhook.post.ts, auth-webhook.post.ts, glitchtip-alert.post.ts, __tests__/"
    - "TECHDEBT.md содержит запись про glitchtip-alert.post.ts (не мигрирован — требует обновления GlitchTip webhook URL)"
  artifacts:
    - path: "TECHDEBT.md"
      provides: "Долг по миграции glitchtip-alert.post.ts из admin в ops"
      contains: "glitchtip-alert"
  key_links:
    - from: "vault.secrets (Supabase Studio)"
      to: "https://ops.fastio.ru"
      via: "vault.update_secret(id, value)"
      pattern: "ops\\.fastio\\.ru"
    - from: "GoTrue (Supabase Coolify service)"
      to: "https://ops.fastio.ru/email-templates/"
      via: "GOTRUE_MAILER_TEMPLATES_* env vars"
      pattern: "ops\\.fastio\\.ru/email-templates"
    - from: "apps/admin/server/api/telegram/"
      to: "(deleted) notify.post.ts, notify-reservation.post.ts, notify-appointment-group.post.ts, notify-table-call.post.ts, notify-alert.post.ts, send-appointment-reminders.post.ts"
      via: "rm + git remove"
      pattern: "(должны отсутствовать после Task 4)"
---

<objective>
Cutover runbook — миграция production-трафика из admin в ops, без окна потери Telegram-уведомлений. Это обязательный человеческий процесс: DNS, Coolify-деплой, vault-апдейт, GoTrue env, верификация, чистка admin. План фиксирует точный порядок шагов и блокирующие checkpoint'ы, чтобы исключить классические pitfall'ы из RESEARCH (особенно #4: ops должен быть healthy ДО обновления vault, и #5: ops должен быть healthy ДО обновления GoTrue env).

Purpose: довести INFRA-01 до Done — INFRA-01 SC1 (ops.fastio.ru health=200), SC2 (notify-хендлеры удалены из admin и работают на ops), SC3 (email-шаблоны GoTrue смотрят на ops), SC4 (appointment_reminder_url в vault указывает на ops). Дополнительно фиксируется TECHDEBT по неперенесённому glitchtip-alert.post.ts.

Output: production живёт на ops.fastio.ru для всех Telegram-уведомлений и кастомных email-шаблонов; admin содержит ТОЛЬКО webhook-приёмники Telegram.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-01-SUMMARY.md
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-02-SUMMARY.md

<!-- Источник правды для cutover sequence — Pitfall 4 и Pitfall 5 -->
@.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [HUMAN] Добавить DNS A-record ops.fastio.ru → 109.71.242.205 + создать Coolify-приложение для @fastio/ops + первый деплой</name>
  <files>(внешняя инфраструктура — Timeweb DNS panel + Coolify UI)</files>
  <read_first>
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Environment Availability" (строки 519-530) и "Env Vars apps/ops/ Needs" (строки 211-247) для списка env vars, которые надо задать в Coolify
    - docs/coolify-migration-handoff.md — паттерны деплоя других apps на этот же VPS (если файл существует)
  </read_first>
  <what-built>
    Wave 2 уже создала рабочий `apps/ops/` с health-endpoint, 6 хендлерами, и email-шаблонами. Сейчас нужно поднять его в production через Coolify:
    - DNS A-record: `ops.fastio.ru` → `109.71.242.205` (Timeweb DNS)
    - Coolify application: новый app, build pack `nixpacks` (или такой же как admin), root context `apps/ops`, build command `pnpm --filter @fastio/ops build`, start command `node .output/server/index.mjs` (стандартный Nuxt output)
    - Coolify env vars (все из списка RESEARCH строки 211-247): NUXT_SUPABASE_SERVICE_ROLE_KEY, NUXT_INTERNAL_API_SECRET, NUXT_REMINDER_CRON_SECRET, NUXT_TELEGRAM_TENANT_BOT_TOKEN, NUXT_TELEGRAM_CLIENT_BOT_TOKEN, NUXT_TELEGRAM_OPS_BOT_TOKEN, NUXT_TELEGRAM_ALERT_CHAT_ID, NUXT_TELEGRAM_PROXY_URL (значение совпадает с admin — sing-box), NUXT_ADMIN_URL=https://admin.fastio.ru, NUXT_PUBLIC_SUPABASE_URL
    - Coolify domain: `ops.fastio.ru` с автоматическим Let's Encrypt через Traefik
  </what-built>
  <how-to-verify>
    После того как DNS прорезолвится и Coolify завершит деплой, выполнить ЛОКАЛЬНО (или из любой машины):

    1. DNS resolution:
       `dig +short ops.fastio.ru` → должен вернуть `109.71.242.205`

    2. TLS handshake:
       `curl -sI https://ops.fastio.ru/api/health | head -1` → `HTTP/2 200`

    3. Liveness probe:
       `curl -s https://ops.fastio.ru/api/health` → ровно `{"status":"ok"}`

    4. Email template availability:
       `curl -s -o /dev/null -w "%{http_code}" https://ops.fastio.ru/email-templates/invite.html` → `200`
       `curl -s https://ops.fastio.ru/email-templates/invite.html | head -1` → должен начинаться с `<!DOCTYPE` или `<html` (содержательный HTML, не пустой)
       (повторить для recovery.html и confirm_signup.html)

    5. Auth gate работает (без секрета должен быть 500/403, не 200):
       `curl -s -o /dev/null -w "%{http_code}" -X POST https://ops.fastio.ru/api/telegram/notify -H "Content-Type: application/json" -d '{}'` → `403` (header x-internal-secret отсутствует, env задан, expected mismatch)

    Если любой из шагов не проходит — НЕ продолжать к Task 2 (Pitfall 4: ops должен быть healthy ДО vault-обновления, иначе все уведомления пропадут на время window).
  </how-to-verify>
  <resume-signal>
    Введите "deployed" если все 5 проверок прошли + приложите вывод `curl -s https://ops.fastio.ru/api/health`. Если что-то не работает — опишите ошибку (например, "5xx от Coolify", "TLS handshake fail", "env var X missing").
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [HUMAN] Обновить 6 vault.secrets URL-записей в Supabase Studio (admin.fastio.ru → ops.fastio.ru)</name>
  <files>(внешний state — Supabase Studio SQL Editor на production проекте)</files>
  <read_first>
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Vault Secrets Requiring Update" (строки 177-191) и "Pitfall 1: The fallback regex in pg_net triggers" (строки 369-375)
    - scripts/coolify/seed-vault-secrets.sql (если файл существует — содержит текущие vault-URL'ы для справки)
  </read_first>
  <what-built>
    Wave 2 deployed ops, Task 1 верифицировал health. Сейчас обновляем production vault, чтобы pg_net триггеры (orders, reservations, appointment_groups, table_calls) и pg_cron (send-appointment-reminders, monitor-edge-errors) начали бить в ops.fastio.ru вместо admin.fastio.ru. 6 vault-секретов изменяются, 2 (`telegram_internal_secret`, `reminder_cron_secret`) НЕ трогаются — это сами секреты, не URL.
  </what-built>
  <how-to-verify>
    Открыть Supabase Studio → SQL Editor production-проекта. ВЫПОЛНИТЬ ПО ОДНОЙ команде, проверяя возврат каждой:

    1. Сначала прочитать текущие значения (sanity check):
       ```sql
       SELECT name, decrypted_secret
       FROM vault.decrypted_secrets
       WHERE name IN (
         'telegram_notify_url',
         'telegram_notify_reservation_url',
         'telegram_notify_appointment_group_url',
         'telegram_notify_table_call_url',
         'appointment_reminder_url',
         'telegram_alert_url'
       );
       ```
       Ожидаемый результат: каждая запись содержит `admin.fastio.ru` (если уже `ops.fastio.ru` — Task 2 уже сделан, перейти к Task 3).

    2. Получить ID каждого секрета:
       ```sql
       SELECT id, name FROM vault.secrets
       WHERE name IN (
         'telegram_notify_url',
         'telegram_notify_reservation_url',
         'telegram_notify_appointment_group_url',
         'telegram_notify_table_call_url',
         'appointment_reminder_url',
         'telegram_alert_url'
       );
       ```

    3. Обновить каждый секрет через `vault.update_secret(secret_id, new_secret)`. RESEARCH Pitfall 1 предупреждает: обновить ВСЕ 6, даже если фоллбэк через regexp_replace формально работал. Пример (подставить реальный id из шага 2):
       ```sql
       SELECT vault.update_secret(
         '<id-из-шага-2>'::uuid,
         'https://ops.fastio.ru/api/telegram/notify'
       );
       ```
       Повторить 6 раз с правильными парами (id, новый URL):
       - `telegram_notify_url` → `https://ops.fastio.ru/api/telegram/notify`
       - `telegram_notify_reservation_url` → `https://ops.fastio.ru/api/telegram/notify-reservation`
       - `telegram_notify_appointment_group_url` → `https://ops.fastio.ru/api/telegram/notify-appointment-group`
       - `telegram_notify_table_call_url` → `https://ops.fastio.ru/api/telegram/notify-table-call`
       - `appointment_reminder_url` → `https://ops.fastio.ru/api/telegram/send-appointment-reminders`
       - `telegram_alert_url` → `https://ops.fastio.ru/api/telegram/notify-alert`

    4. Повторить запрос из шага 1 — все 6 значений должны теперь содержать `ops.fastio.ru`.

    5. Triggered smoke-test: создать ТЕСТОВЫЙ table_call через Supabase Studio:
       ```sql
       -- Найти тестового тенанта (тот, у кого настроен tenant_telegram_subscribers
       -- и подписан тестовый канал команды разработки):
       SELECT tenant_id, chat_id FROM tenant_telegram_subscribers LIMIT 5;

       -- INSERT table_call для этого тенанта (потребуется реальный table_id):
       INSERT INTO table_calls (tenant_id, table_id, call_type_name)
       VALUES ('<tenant_id>', '<existing_table_id>', 'Test (cutover ops)')
       RETURNING id;
       ```

    6. В течение ~30 секунд проверить:
       - В Telegram-канале тенанта появилось уведомление о вызове ("📢 Вызов официанта" с текстом "Test (cutover ops)")
       - В Supabase Studio `SELECT * FROM net._http_response ORDER BY created DESC LIMIT 5;` — последний запрос содержит `https://ops.fastio.ru/api/telegram/notify-table-call` и status_code=200

    Если в Telegram не пришло сообщение или net._http_response показывает status_code != 200 — НЕ продолжать к Task 3 и НЕ выполнять Task 4 (rollback: вернуть vault на admin.fastio.ru тем же `vault.update_secret`).
  </how-to-verify>
  <resume-signal>
    Введите "vault-updated" + приложите вывод финального SELECT из шага 4 (6 строк с ops.fastio.ru URL) и подтверждение про доставленное Telegram-сообщение в шаге 6. Если был rollback — введите "rollback" и опишите проблему.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [HUMAN] Обновить GOTRUE_MAILER_TEMPLATES_* env vars в Coolify (Supabase service) и перезапустить GoTrue</name>
  <files>(внешняя инфраструктура — Coolify UI, Supabase service environment)</files>
  <read_first>
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "GoTrue Email Template Configuration" (строки 195-206) и "Pitfall 5: Email templates served before GoTrue env vars are updated" (строки 393-399)
  </read_first>
  <what-built>
    На предыдущих шагах подняли ops с шаблонами и обновили vault. Сейчас переключаем GoTrue (часть Supabase Coolify stack) на эти шаблоны. RESEARCH Pitfall 5: ops должен уже быть healthy на шаге Task 1 — иначе GoTrue будет фоллбэчить на дефолтные шаблоны и юзеры получат уродские email'ы вместо брендированных.

    В Coolify это "hardcoded env" Supabase-сервиса: меняется через Edit Compose File (не через regular env vars UI), потому что Supabase Coolify-stack — это один docker-compose с несколькими сервисами.
  </what-built>
  <how-to-verify>
    1. Coolify UI → Supabase service → Edit Compose File. В блок `services.auth.environment` (имя сервиса может быть `gotrue` или `auth` в зависимости от версии compose-файла Coolify) добавить или обновить 3 переменные:
       ```yaml
       GOTRUE_MAILER_TEMPLATES_INVITE: https://ops.fastio.ru/email-templates/invite.html
       GOTRUE_MAILER_TEMPLATES_RECOVERY: https://ops.fastio.ru/email-templates/recovery.html
       GOTRUE_MAILER_TEMPLATES_CONFIRMATION: https://ops.fastio.ru/email-templates/confirm_signup.html
       ```

    2. Coolify → Deploy / Restart Supabase service. Дождаться, пока auth-контейнер перезапустится (обычно 30-90 сек).

    3. Verify env vars применились:
       - Coolify → Supabase service → terminal → `docker exec <supabase-auth-container> env | grep GOTRUE_MAILER_TEMPLATES` → должны быть видны все 3
       - Альтернатива через SSH на VPS: `docker exec $(docker ps --filter name=supabase-auth -q) env | grep GOTRUE_MAILER`

    4. Functional test (отправить тестовый invite):
       - Открыть Supabase Studio → Authentication → Users → "Invite user"
       - Указать тестовый email (свой, доступный): `dev+ops-cutover@<твоя_доменная_область>`
       - Получить email и сравнить визуально с `apps/ops/public/email-templates/invite.html` (тот же layout, тот же бренд) — НЕ дефолтный GoTrue (тот выглядит как голый текст с unstyled ссылкой)

    5. Если email пришёл с дефолтным GoTrue-стилем (не брендированный) — проверить, что:
       - `curl -s https://ops.fastio.ru/email-templates/invite.html` всё ещё возвращает HTML (ops жив)
       - GoTrue-логи в Coolify не показывают ошибок fetching template URL
       - env vars применились на правильный контейнер (auth, а не postgrest или storage)
  </how-to-verify>
  <resume-signal>
    Введите "gotrue-updated" + приложите скриншот/копию полученного invite-email (или текст body, по которому видно что это кастомный шаблон, а не дефолт GoTrue). Если шаблон не активировался — введите "blocked" и опишите GoTrue-логи.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Удалить 6 мигрированных хендлеров из apps/admin/server/api/telegram/</name>
  <files>apps/admin/server/api/telegram/notify.post.ts, apps/admin/server/api/telegram/notify-reservation.post.ts, apps/admin/server/api/telegram/notify-appointment-group.post.ts, apps/admin/server/api/telegram/notify-table-call.post.ts, apps/admin/server/api/telegram/notify-alert.post.ts, apps/admin/server/api/telegram/send-appointment-reminders.post.ts</files>
  <read_first>
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — таблица "Handlers that STAY in admin" (строки 60-67): после удаления в admin должны остаться webhook.post.ts, auth-webhook.post.ts, glitchtip-alert.post.ts (и __tests__/)
    - apps/admin/server/api/telegram/ (текущий список файлов перед удалением)
    - Wave 2 SUMMARY (04.1-02-SUMMARY.md) — подтверждение, что ops-копии работают, перед удалением оригиналов
    - Task 2 (Wave 3) "vault-updated" сигнал — обязательное предусловие, без которого удалять admin-хендлеры опасно (rollback на admin невозможен, если vault уже на ops)
  </read_first>
  <action>
    Выполнить ТОЛЬКО если Task 1, Task 2 и Task 3 показали "deployed", "vault-updated", "gotrue-updated" соответственно. В противном случае админ-хендлеры остаются как safety net.

    Удалить файлы:
    ```
    apps/admin/server/api/telegram/notify.post.ts
    apps/admin/server/api/telegram/notify-reservation.post.ts
    apps/admin/server/api/telegram/notify-appointment-group.post.ts
    apps/admin/server/api/telegram/notify-table-call.post.ts
    apps/admin/server/api/telegram/notify-alert.post.ts
    apps/admin/server/api/telegram/send-appointment-reminders.post.ts
    ```

    НЕ удалять:
    - `apps/admin/server/api/telegram/webhook.post.ts` (Vercel relay приёмник)
    - `apps/admin/server/api/telegram/auth-webhook.post.ts` (Vercel relay приёмник)
    - `apps/admin/server/api/telegram/glitchtip-alert.post.ts` (GlitchTip webhook — defer, см. TECHDEBT в Task 5)
    - `apps/admin/server/api/telegram/__tests__/` (тест auth-webhook остаётся)
    - `apps/admin/server/utils/supabase.ts`, `auth.ts`, `telegramFetch.ts`, `telegramBroadcast.ts` (admin webhook'и их продолжают использовать)
    - `apps/admin/server/plugins/telegram-bot-setup.ts` (регистрирует webhook'и, остаётся в admin)

    После удаления:
    1. Запустить `pnpm --filter admin typecheck` — должен пройти зелёным (никакие admin-файлы не импортировали мигрированные хендлеры).
    2. Запустить `pnpm vitest run apps/admin/server/api/telegram/__tests__/` — auth-webhook.post.test.ts должен остаться зелёным.
    3. Запустить `grep -rn "api/telegram/notify\|api/telegram/send-appointment" apps/admin/ packages/ supabase/` — если найдутся жёстко зашитые URL к admin-хендлерам в коде или миграциях, это блокер: понадобится миграция, обновляющая URL. Скорее всего НЕ найдётся (вся маршрутизация через vault).
  </action>
  <verify>
    <automated>! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify.post.ts && ! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-reservation.post.ts && ! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-appointment-group.post.ts && ! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-table-call.post.ts && ! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/notify-alert.post.ts && ! test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/send-appointment-reminders.post.ts && test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/webhook.post.ts && test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/auth-webhook.post.ts && test -e /Users/evgeniy/WebstormProjects/fastio/apps/admin/server/api/telegram/glitchtip-alert.post.ts && cd /Users/evgeniy/WebstormProjects/fastio && pnpm --filter admin typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 6 файлов отсутствуют в `apps/admin/server/api/telegram/`
    - 3 файла остались (webhook.post.ts, auth-webhook.post.ts, glitchtip-alert.post.ts) + `__tests__/` директория
    - `pnpm --filter admin typecheck` exit 0
    - `pnpm vitest run apps/admin/server/api/telegram/__tests__/` экзит 0 (тест auth-webhook не сломан)
    - `apps/admin/server/utils/` НЕ тронут — все 4 utility-файла остаются
    - `apps/admin/server/plugins/telegram-bot-setup.ts` НЕ тронут
  </done>
</task>

<task type="auto">
  <name>Task 5: Записать TECHDEBT.md — glitchtip-alert.post.ts остаётся в admin</name>
  <files>TECHDEBT.md</files>
  <read_first>
    - TECHDEBT.md (текущее содержимое — если файла нет в корне репо, создать новый по структуре, описанной в CLAUDE.md строки "TECHDEBT.md")
    - CLAUDE.md — раздел "TECHDEBT.md и LATER.md" (правила: один абзац = одна запись, "что/почему")
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — секция "Open Questions" item 1 (строки 578-582) и таблица "Handlers that STAY in admin" (строки 60-67)
  </read_first>
  <action>
    Открыть `TECHDEBT.md` (если файла нет — создать с заголовком `# TECHDEBT` и краткой шапкой по примеру из CLAUDE.md). Добавить новую запись (один абзац) под заголовком/датой:

    ```markdown
    ## glitchtip-alert.post.ts — не мигрирован в apps/ops/ (фаза 04.1)

    Хендлер `apps/admin/server/api/telegram/glitchtip-alert.post.ts` остался в admin, потому что URL для GlitchTip webhook alert настраивается вручную в GlitchTip UI (errors.fastio.ru → Project Settings → Alerts → Slack-compatible webhook). Фаза 04.1 не включала обновление этой конфигурации.

    Что нужно сделать:
    1. Скопировать `apps/admin/server/api/telegram/glitchtip-alert.post.ts` в `apps/ops/server/api/telegram/glitchtip-alert.post.ts` (тот же паттерн копирования утилит и нотифай-хендлеров из фазы 04.1, Wave 2).
    2. В GlitchTip UI обновить webhook URL: `https://admin.fastio.ru/api/telegram/glitchtip-alert` → `https://ops.fastio.ru/api/telegram/glitchtip-alert`.
    3. Триггернуть тестовую ошибку в GlitchTip (или дождаться реального alert'а), проверить доставку в Telegram-канал.
    4. Удалить `apps/admin/server/api/telegram/glitchtip-alert.post.ts`.

    Почему не сделали сразу: GlitchTip конфиг живёт вне репозитория (UI-state), и совмещение его правки с массовой миграцией хендлеров увеличивало бы blast radius фазы 04.1. Архитектурно glitchtip-alert принадлежит ops (ops_bot_token + alert_chat_id), поэтому это технический долг, а не дизайн-решение.

    Связано: фаза 04.1 (INFRA-01), RESEARCH Open Question 1.
    ```

    Если в TECHDEBT.md уже есть похожие записи — добавить эту в хронологическом порядке (новые сверху или снизу — следуй существующему стилю файла).
  </action>
  <verify>
    <automated>grep -c "glitchtip-alert" /Users/evgeniy/WebstormProjects/fastio/TECHDEBT.md | awk '$1 >= 1' && grep -c "apps/ops" /Users/evgeniy/WebstormProjects/fastio/TECHDEBT.md | awk '$1 >= 1' && grep -c "INFRA-01\|фаза 04.1\|Phase 04.1\|phase 04.1" /Users/evgeniy/WebstormProjects/fastio/TECHDEBT.md | awk '$1 >= 1'</automated>
  </verify>
  <done>
    - `TECHDEBT.md` содержит секцию про glitchtip-alert.post.ts
    - Описаны: WHAT (что осталось в admin), WHY (почему не сделали в этой фазе), HOW (4 конкретных шага к закрытию долга)
    - Запись ссылается на фазу 04.1 / INFRA-01 для traceability
    - Стиль соответствует существующему TECHDEBT.md (если файл уже был)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: [HUMAN] Final end-to-end smoke (после deploy admin без хендлеров)</name>
  <files>(внешняя верификация — после auto-deploy admin Task 4 commit'а)</files>
  <read_first>
    - .planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-RESEARCH.md — раздел "Migration Risks" (строки 473-481), особенно severity HIGH про окно потери уведомлений
  </read_first>
  <what-built>
    Wave 1-2 создали ops, Task 1-3 (Wave 3) перевели prod трафик. Task 4 удалил handlers из admin (Coolify задеплоит admin без них). Task 5 зафиксировал долг. Сейчас финальная проверка: ничего не сломалось в окне переключения, оба сервиса живы и независимы.
  </what-built>
  <how-to-verify>
    1. Coolify admin app: дождаться, пока auto-deploy после Task 4 commit'а завершится (либо проверить, что версия в production показывает свежий sha).

    2. Health-checks:
       - `curl -s https://admin.fastio.ru/api/health` → admin жив
       - `curl -s https://ops.fastio.ru/api/health` → `{"status":"ok"}`
       - `curl -s -o /dev/null -w "%{http_code}" https://admin.fastio.ru/api/telegram/notify` → `404` (хендлер удалён — это и ожидается)
       - `curl -s -o /dev/null -w "%{http_code}" https://admin.fastio.ru/api/telegram/webhook` → НЕ 404 (admin webhook должен остаться, например 403/405 на GET — но не 404)

    3. End-to-end по каждому критическому флоу (минимум по одному из каждой группы):
       - Создать тестовый order через storefront тестового тенанта → проверить, что Telegram-уведомление пришло
       - Создать тестовый reservation → Telegram пришло
       - Создать тестовый table_call → Telegram пришло
       - Создать тестовый appointment_group → Telegram пришло (если в тестовом тенанте есть services-вертикаль; если нет — пропустить)
       - Создать тестовый appointment_reminder через создание appointment c remind_before_minutes=1 на старт +2 мин → подождать минуту → reminder пришёл

    4. Vault sanity recheck:
       ```sql
       SELECT name, decrypted_secret
       FROM vault.decrypted_secrets
       WHERE name IN (
         'telegram_notify_url',
         'telegram_notify_reservation_url',
         'telegram_notify_appointment_group_url',
         'telegram_notify_table_call_url',
         'appointment_reminder_url',
         'telegram_alert_url'
       );
       ```
       → все 6 содержат `ops.fastio.ru`

    5. Telegram bot webhook'и НЕ сломаны (Pitfall 3):
       Через Telegram Bot API проверить, что webhook_info по обоим ботам (tenant и client) всё ещё указывает на admin.fastio.ru, не на ops:
       ```bash
       curl -s "https://api.telegram.org/bot$NUXT_TELEGRAM_TENANT_BOT_TOKEN/getWebhookInfo" | jq '.result.url'
       # expected: вид https://...vercel.app/...
       ```
       (URL может содержать vercel-relay, а не напрямую admin — это OK)

    6. /api/health admin — все остальные admin-роуты живы (sanity на random page):
       Войти в admin.fastio.ru, открыть /menu/dishes, /orders/list, /team — никаких 500-х.
  </how-to-verify>
  <resume-signal>
    Введите "done" с приложенным списком результатов 6 проверок. Если какая-то проверка показала regression — введите "regression: <details>" и опишите, что не работает. Хотя бы один смоук-флоу (orders/reservations/table_calls/appointments/reminders) должен явно подтвердить, что Telegram-сообщение долетело.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify env vars → ops container | Secrets injected at deploy time; Coolify обеспечивает изоляцию |
| Supabase Studio (production) → vault.secrets | Только developer с правами проекта Supabase может выполнять vault.update_secret |
| GlitchTip → admin.fastio.ru/api/telegram/glitchtip-alert | Остаётся как до фазы 04.1; обновление URL отложено в TECHDEBT |
| GoTrue → ops.fastio.ru/email-templates/ | Public HTTP GET; шаблоны не содержат секретов, только GoTrue placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-09 | DoS | Окно между vault-update и ops-deploy | mitigate | Жёсткий порядок: Task 1 (ops deploy + verify) → Task 2 (vault update + verify) → Task 3 (gotrue). Pitfall 4. |
| T-04.1-10 | DoS | Окно между GoTrue env update и ops health | mitigate | Pitfall 5: ops уже healthy на момент Task 1; Task 3 идёт строго после Task 1. |
| T-04.1-11 | Tampering | Случайное удаление admin webhook handlers | mitigate | Task 4 явно перечисляет файлы для удаления; верификация что webhook.post.ts, auth-webhook.post.ts, glitchtip-alert.post.ts остались. |
| T-04.1-12 | Repudiation | Vault update без аудита | accept | Supabase Studio логирует SQL-запросы; vault.secrets имеет updated_at. Не требует дополнительного аудита для INFRA-01. |
| T-04.1-13 | Spoofing | Удалённый admin endpoint доступен на ops без auth | mitigate | requireInternalSecret и x-reminder-cron-secret скопированы в ops Wave 2; pg_net должен слать соответствующие заголовки (vault содержит секреты в отдельных записях). |
| T-04.1-14 | Tampering | Cutover может прерваться посередине | mitigate | Wave 3 идёт линейно через 5 явных checkpoint'ов; на каждом шаге явный rollback-сценарий описан (откат vault.update_secret обратно на admin.fastio.ru). |
| T-04.1-15 | Information Disclosure | Сообщения "TECHDEBT" в repo | accept | Не содержат секретов, только архитектурный долг. |
| T-04.1-SC | Tampering | Никаких новых пакетов в Wave 3 | accept | Wave 3 только меняет конфигурацию (DNS, Coolify env, vault, Supabase env) и удаляет код. Никаких install'ов. |
</threat_model>

<verification>
- DNS `ops.fastio.ru` резолвится на `109.71.242.205`
- `curl https://ops.fastio.ru/api/health` → 200, `{"status":"ok"}`
- 6 vault.secrets URL содержат `ops.fastio.ru`
- `GOTRUE_MAILER_TEMPLATES_*` env vars в Coolify Supabase-сервисе указывают на ops
- Тестовый invite-email приходит брендированным (как ops/public/email-templates/invite.html)
- В `apps/admin/server/api/telegram/` остались только 3 файла: webhook.post.ts, auth-webhook.post.ts, glitchtip-alert.post.ts + `__tests__/`
- `pnpm --filter admin typecheck` exit 0 после удаления 6 файлов
- `TECHDEBT.md` содержит запись про glitchtip-alert
- E2E smoke (order/reservation/table-call/reminder) — Telegram-сообщения долетают через ops
- Telegram webhook'и (через Vercel relay) продолжают указывать на admin — НЕ сломаны (Pitfall 3 mitigated)
</verification>

<success_criteria>
1. INFRA-01 SC1: `GET https://ops.fastio.ru/api/health` возвращает 200
2. INFRA-01 SC2: Telegram notify-хендлеры отвечают на pg_net/pg_cron на ops.fastio.ru И удалены из apps/admin (e2e smoke подтверждает доставку через ops)
3. INFRA-01 SC3: `https://ops.fastio.ru/email-templates/*.html` доступны; GoTrue env vars обновлены, GoTrue перезапущен, тестовый invite email приходит брендированным
4. INFRA-01 SC4: `appointment_reminder_url` в vault указывает на ops.fastio.ru (+ остальные 5 URL-секретов)
5. TECHDEBT.md содержит запись про glitchtip-alert.post.ts (отложенная миграция)
6. Никаких regression'ов в admin (typecheck зелёный, /api/health жив, webhook'и работают)
</success_criteria>

<output>
Создать `.planning/phases/04.1-ops-server-apps-ops-nitro-ui-email-telegram-cron-apps-admin-/04.1-03-SUMMARY.md` после Task 6 "done" сигнала. Зафиксировать:
- Точное время cutover'а (Task 2 vault-update timestamp) — для последующего analytics через net._http_response
- Полученные curl-ответы для health, email-templates, /api/telegram/notify (403)
- Подтверждение из Telegram о доставленных тест-сообщениях (по одному из каждой группы)
- Список оставшихся в admin/server/api/telegram/ файлов
- TECHDEBT entry формат (одна цитата абзаца)
</output>
