---
plan: 04.1-01
phase: 04.1
status: complete
completed: 2026-05-22
key-files:
  created:
    - apps/ops/package.json
    - apps/ops/nuxt.config.ts
    - apps/ops/tsconfig.json
    - apps/ops/eslint.config.mjs
    - apps/ops/.gitignore
    - apps/ops/server/api/health.get.test.ts
    - apps/ops/server/utils/auth.test.ts
    - apps/ops/server/api/telegram/notify.post.test.ts
  modified:
    - vitest.config.ts
---

## Summary

Создан каркас `apps/ops/` — отдельный Nitro-only Nuxt app (`@fastio/ops`) без Vue UI, без features/, без auto-imports. Готов к копированию Telegram-хендлеров в Wave 2.

## What Was Built

**Task 1 — Package scaffold:** `apps/ops/package.json` с 5 зависимостями (nuxt 3.21.6, @fastio/shared, @supabase/supabase-js, undici, @sentry/nuxt) и без UI-stack. `tsconfig.json` (extends `./.nuxt/tsconfig.json`), `eslint.config.mjs` (наследует `@fastio/shared/configs/eslint`), `.gitignore`.

**Task 2 — nuxt.config.ts:** `ssr: false`, `autoImport: false`, `components: false`, порт 4714. runtimeConfig с 9 server-keys (supabaseServiceRoleKey, internalApiSecret, reminderCronSecret, telegramTenantBotToken, telegramClientBotToken, telegramOpsBotToken, telegramAlertChatId, telegramProxyUrl, adminUrl) + `public.supabaseUrl`. @sentry/nuxt модуль не активирован — ждёт GlitchTip-проект (RESEARCH Open Question 3).

**Task 3 — vitest.config.ts:** Добавлен паттерн `'apps/ops/**/*.test.ts'` в include array. pnpm-workspace.yaml не трогали — `apps/*` уже покрывает ops.

**Task 4 — Wave 0 test stubs:** 3 placeholder-теста: `health.get.test.ts` (1 passing), `auth.test.ts` (2 todo), `notify.post.test.ts` (2 todo). Vitest завершается exit 0.

## Deviations

Нет отклонений от плана. Агент был прерван лимитом сессии после Task 3 — Task 4 и SUMMARY.md завершены оркестратором вручную.

## Self-Check: PASSED

- ✓ `apps/ops/` существует с 5 конфиг-файлами + .gitignore
- ✓ vitest.config.ts содержит `apps/ops/**/*.test.ts`
- ✓ Wave 0 test stubs созданы, vitest exit 0
- ✓ Vue version `^3.5.13` в package.json (без UI-deps)
- ✓ nuxt.config.ts: ssr:false, autoImport:false, 9 runtimeConfig keys
