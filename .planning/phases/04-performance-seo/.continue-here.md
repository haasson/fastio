---
context: phase
phase: 04-performance-seo
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-05-23T07:31:47Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified in this session._

## Critical Anti-Patterns

_None identified._

<current_state>
Phase 04 (performance-seo) — Wave 1 complete, Wave 2 is next.

Plan 04-01 (OG/SEO meta tags) fully executed and merged to main:
- `buildHead.ts` pure helper created
- `app.vue` rewired to use helper
- 7 Vitest unit tests GREEN
- Human-verify checkpoint approved by user

Next up: plan 04-02 — install `@nuxt/image`, configure IPX provider, add `Vary: Host` routeRules, replace 4 `<img>` / 1 CSS bg with `<NuxtImg>`.
</current_state>

<completed_work>
- Plan 04-01 — OG/SEO metadata fix (COMPLETE)
  - Task 1: Vitest tests for buildHead (RED) — commit 835a01c1
  - Task 2: buildHead helper + app.vue (GREEN) — commit d3ca5499
  - Task 3: Human verify checkpoint — approved
  - SUMMARY.md committed — docs commit aa3df28d
  - Merged to main via worktree — merge commit a9d2be35
</completed_work>

<remaining_work>
- Plan 04-02 (Wave 2) — @nuxt/image + IPX + NuxtImg — NOT STARTED
  - Install `@nuxt/image` in apps/storefront
  - Configure IPX provider + `Vary: Host` routeRules in nuxt.config.ts
  - Replace img in SiteHeader.vue (logo: eager, fetchpriority=high)
  - Replace img in HeroSection.vue (hero: eager, fetchpriority=high)
  - Replace img in BannersSection.vue (lazy)
  - Replace img in SfProductCard.vue (lazy)
  - Checkpoint: human verify IPX routes + Vary header

- Plan 04-03 (Wave 3) — LHCI GitHub Actions — NOT STARTED
  - Add lighthouserc.json
  - Patch .github/workflows/ci.yml
  - Checkpoint: human verify CI gate
</remaining_work>

<decisions_made>
- buildHead extracted as pure composable (not inline in app.vue) — testable without Vue rendering
- Conditional meta spread `...(value ? [{...}] : [])` for optional OG fields — prevents undefined in tags
- Null-safe slug URL: `tenant?.slug ? \`${tenant.slug}.fastio.ru\` : ''`
- Human-verify checkpoint for 04-01 approved (7 tests GREEN, typecheck clean)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-performance-seo/04-02-PLAN.md` — plan for @nuxt/image integration
2. `.planning/phases/04-performance-seo/04-01-SUMMARY.md` — what was built in wave 1
3. `./CLAUDE.md` — project conventions

## Infrastructure State
- Supabase local: unknown (may need `pnpm supabase:start`)
- Dev server: not running
- pnpm-lock.yaml will change when 04-02 installs @nuxt/image

<context>
Session paused after Wave 1 completion. User approved 04-01 checkpoint and requested new session.
Plans 04-02 and 04-03 are both `autonomous: false` — they'll need user checkpoints during execution.
04-02 is the heavy one: touches pnpm-lock.yaml (needs pnpm install after), multiple Vue components.
</context>

<next_action>
Run `/gsd:execute-phase 4` — it will discover 04-01 is done (SUMMARY.md exists), skip it, and start Wave 2 (04-02).
</next_action>
