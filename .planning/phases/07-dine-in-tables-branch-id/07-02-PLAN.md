---
phase: 07-dine-in-tables-branch-id
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/storefront/server/services/order-delivery.ts
  - apps/storefront/server/services/__tests__/order-delivery.test.ts
autonomous: true
requirements: [FIX-TABLE-BRANCH-STOREFRONT]
must_haves:
  truths:
    - "dine_in заказ со стола с branch_id маршрутизируется на филиал стола (branchId = tableRecord.branchId)"
    - "dine_in заказ со стола без филиала на single-branch тенанте использует единственный филиал (D-12 fallback)"
    - "dine_in заказ со стола без филиала на мультибранч-тенанте отклоняется с 400 (D-12, не тихий null)"
    - "TableRecord содержит branchId, validateTable селектит branch_id"
  artifacts:
    - path: "apps/storefront/server/services/order-delivery.ts"
      provides: "branch-маршрутизация dine_in (D-11/D-12)"
      contains: "tableRecord.branchId"
    - path: "apps/storefront/server/services/__tests__/order-delivery.test.ts"
      provides: "unit-покрытие D-11/D-12"
      contains: "dine_in"
  key_links:
    - from: "resolveDelivery dine_in"
      to: "orders.branch_id"
      via: "branchId = tableRecord.branchId → orders.post.ts insert"
      pattern: "branchId = tableRecord\\.branchId"
---

<objective>
Ядро P0 (D-11/D-12): расширить `TableRecord` полем `branchId`, `validateTable` селектит `branch_id`, `resolveDelivery` для dine_in ставит `branchId` из стола. Убрать тихий null-bypass для dine_in. Сначала написать падающие тесты (Wave 0), затем фикс.

Purpose: На мультибранч-тенанте dine-in заказ сейчас уходит с `branch_id=null` → невидим для кухни. Это корень бага фазы.
Output: исправленный order-delivery.ts + unit-тесты D-11/D-12.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dine-in-tables-branch-id/07-CONTEXT.md
@.planning/phases/07-dine-in-tables-branch-id/07-RESEARCH.md
@.planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md

<interfaces>
<!-- order-delivery.ts:7-10 — TableRecord { id, name } (НЕТ branchId). -->
<!-- validateTable:30-41 — .select('id, name, is_open'), return { id, name }. -->
<!-- resolveDelivery:60-62 — для dine_in вызывает validateTable, кладёт в tableRecord. -->
<!-- resolveDelivery:64-67 — branchRows загружены через Promise.all ДО блока привязки. -->
<!-- resolveDelivery:116-127 — блок if(!branchId): pickup из body.branchId, иначе single-branch fallback. dine_in НЕ обрабатывается. -->
<!-- resolveDelivery:129 — if(!branchId && deliveryType !== 'dine_in') throw — dine_in ТИХО уходит с null. -->
<!-- storefront server НЕ использует типизированный Supabase-клиент → tableData.branch_id требует cast `as string | null`. -->
<!-- orders.post.ts:121 — branch_id: branchId ?? null (уже готов принять корректный branchId). -->
<!-- createError({ statusCode, message }) — Nitro auto-import, явный импорт не нужен. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [Wave 0] Падающие unit-тесты для dine_in branch-маршрутизации (D-11/D-12)</name>
  <files>apps/storefront/server/services/__tests__/order-delivery.test.ts</files>
  <behavior>
    - Test 1 (D-11): dine_in + стол с branchId='park' → resolveDelivery возвращает branchId='park'.
    - Test 2 (D-12 fallback): dine_in + стол branchId=null + один активный филиал → branchId = единственный филиал.
    - Test 3 (D-12 ошибка): dine_in + стол branchId=null + два активных филиала → throw 400 (не null).
    - Test 4 (регресс): dine_in + стол с branchId на мультибранче → branchId = стол.branchId (НЕ один из branchRows наугад).
  </behavior>
  <read_first>
    - apps/storefront/server/services/__tests__/order-calc.test.ts (образец структуры теста storefront-сервиса, моки)
    - apps/storefront/server/services/order-delivery.ts (сигнатуры resolveDelivery / validateTable / TableRecord)
    - .planning/phases/07-dine-in-tables-branch-id/07-VALIDATION.md (Wave 0 требования)
  </read_first>
  <action>
    Создать `__tests__/order-delivery.test.ts`. Замокать SupabaseClient так, чтобы `tables` select возвращал стол (с/без branch_id) и `branches` select возвращал 1 или 2 активных филиала. Покрыть 4 кейса из behavior. Тесты пишутся ПЕРЕД фиксом → Test 1/3/4 должны падать на текущем коде (dine_in уходит с null), Test 2 может проходить (single-branch fallback уже есть). Vitest, мок-объект Supabase с цепочкой `.from().select().eq()...single()`. Использовать createError-перехват через try/catch + ожидание statusCode 400.
  </action>
  <acceptance_criteria>
    - Файл `apps/storefront/server/services/__tests__/order-delivery.test.ts` существует.
    - `pnpm vitest run apps/storefront/server/services/__tests__/order-delivery.test.ts` запускается без синтаксических ошибок.
    - На ТЕКУЩЕМ (нефикшенном) order-delivery.ts тесты D-11 (Test 1) и D-12-ошибка (Test 3) КРАСНЫЕ (RED) — доказывают баг.
    - Тест-кейсы явно различают branchId='park' и список branchRows.
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/storefront/server/services/__tests__/order-delivery.test.ts && pnpm vitest run apps/storefront/server/services/__tests__/order-delivery.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>4 теста написаны, ключевые (D-11/D-12-ошибка) красные на текущем коде.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [GREEN] Фикс order-delivery.ts — TableRecord.branchId + validateTable + resolveDelivery dine_in</name>
  <files>apps/storefront/server/services/order-delivery.ts</files>
  <behavior>
    - После фикса все 4 теста из Task 1 зелёные.
    - dine_in никогда не уходит с branch_id=null: либо branchId стола, либо single-branch fallback, либо 400.
  </behavior>
  <read_first>
    - apps/storefront/server/services/order-delivery.ts (self — строки 7-10, 30-41, 64, 116-131)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция order-delivery.ts — пошаговый diff Step 1-4)
  </read_first>
  <action>
    Шаг 1: в `type TableRecord` добавить `branchId: string | null`.
    Шаг 2: в `validateTable` изменить `.select('id, name, is_open')` → `.select('id, name, is_open, branch_id')`.
    Шаг 3: return дополнить `branchId: tableData.branch_id as string | null` (явный cast — storefront не типизирован).
    Шаг 4: в `resolveDelivery`, внутри блока `if (!branchId)` (строки ~116), добавить ветку для dine_in ПОСЛЕ pickup-ветки: если `deliveryType === 'dine_in' && tableRecord` → если `tableRecord.branchId` ставить его; иначе если `branchRows?.length === 1` — единственный филиал (D-12 fallback); иначе если `branchRows.length > 1` — `throw createError({ statusCode: 400, message: 'Стол не привязан к филиалу. Обратитесь к администратору заведения.' })` (D-12).
    Шаг 5: строку 129 `if (!branchId && deliveryType !== 'dine_in')` заменить на `if (!branchId)` — dine_in теперь всегда имеет branchId или уже кинул ошибку. НЕ менять блок проверки расписания на строке 134 (там `deliveryType !== 'dine_in'` оставить — у dine_in нет проверки часов работы).
    reportError не требуется (createError — пользовательская валидация, не сбой). branchRows уже загружены (строка 64) — лишних запросов нет.
  </action>
  <acceptance_criteria>
    - `grep -c 'branchId: string | null' apps/storefront/server/services/order-delivery.ts` >= 1 (в TableRecord).
    - `grep -c 'branch_id' apps/storefront/server/services/order-delivery.ts` >= 2 (select + return cast).
    - `grep -c "tableRecord.branchId" apps/storefront/server/services/order-delivery.ts` >= 1.
    - Строки 129-эквивалента: НЕТ `deliveryType !== 'dine_in'` в финальном `if (!branchId)` (тихий null убран). Проверка: `grep -n "if (!branchId)" apps/storefront/server/services/order-delivery.ts` присутствует как самостоятельное условие.
    - `pnpm vitest run apps/storefront/server/services/__tests__/order-delivery.test.ts` — все 4 теста зелёные.
    - `pnpm --filter @fastio/storefront typecheck` не падает на order-delivery.ts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/storefront/server/services/__tests__/order-delivery.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>dine_in маршрутизируется на branchId стола; single-branch fallback работает; мультибранч без branch_id → 400; тихий null убран; 4 теста зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storefront client → orders.post (Nitro) | клиент шлёт tableId; сервер валидирует стол по tenant_id |
| validateTable → orders.branch_id | branchId из стола определяет, на какую кухню уйдёт заказ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | validateTable cross-tenant | mitigate | validateTable уже фильтрует `.eq('tenant_id', tenantId)` — стол чужого тенанта вернёт 404; branch_id берётся из строки, проверенной в рамках tenant_id, чужой филиал недостижим |
| T-07-02 | Tampering | dine_in branchId=null bypass | mitigate | D-12: на мультибранче без branch_id стола → throw 400; тихий null устранён — заказ не уйдёт в никуда |
| T-07-03 | Information Disclosure | error message | accept | сообщение 'Стол не привязан к филиалу' не раскрывает PII/структуру; общая конфиг-ошибка |
| T-07-SC | Tampering | npm installs | mitigate | новых пакетов нет; supply-chain вектор отсутствует |
</threat_model>

<verification>
- `pnpm vitest run apps/storefront/server/services/__tests__/order-delivery.test.ts` — 4/4 зелёных.
- `pnpm vitest run apps/storefront/server/services` — весь сервис-сьют зелёный (нет регрессов в order-calc).
- Ручная проверка (E2E, manual-only): на проде T1 (2 филиала) заказ со стола Парка → orders.branch_id = Парк → виден в кухонной очереди Парка.
</verification>

<success_criteria>
- TableRecord.branchId присутствует, validateTable селектит и возвращает branch_id.
- resolveDelivery для dine_in ставит branchId стола / single-branch fallback / 400 на мультибранче без филиала.
- Тихий null-bypass (`deliveryType !== 'dine_in'`) удалён из финального guard.
- 4 unit-теста зелёные.
</success_criteria>

<output>
Create `.planning/phases/07-dine-in-tables-branch-id/07-02-SUMMARY.md` when done
</output>
