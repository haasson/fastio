# Phase 7: dine-in-tables-branch-id - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in 07-CONTEXT.md — this log preserves the discussion.

**Date:** 2026-06-01
**Phase:** 07-dine-in-tables-branch-id
**Mode:** discuss
**Origin:** Найдено при ручном тестировании по TESTING_PLAN.md (префлайт 3.7 «Столы и QR»).

## Gray Areas Identified

1. Обязательность привязки `branch_id` + поведение при одном филиале
2. Бэкфилл существующих столов
3. Влияние филиала стола на меню витрины
4. Archive guard при наличии столов

## Discussion

### Реформулировка развилки 1 (по правке пользователя)
Пользователь указал: селектор филиала **глобальный в сайдбаре**, отдельного для столов не делаем. Пер-стольный дропдаун отвергнут. Столы становятся branch-scoped по глобальному селектору.

Выявлена ловушка: `currentBranchId === null` = «Все филиалы» (`useBranch.ts:14`), владелец single-branch по умолчанию сидит в `null` → при наивной заглушке никогда не увидит свои столы. Решение — развязка по числу филиалов (D-04).

### Вопрос: экран «Все филиалы» при мультибранче
- Опции: лёгкая саммари (реком) / простая заглушка / саммари+занятость.
- **Выбор пользователя:** Лёгкая саммари. → D-06. Реалтайм-занятость и вызовы по филиалам отложены (deferred).

### Остальные развилки — приняты дефолты (без возражений)
- Бэкфилл: A (умный) → D-08
- Меню: A (только маршрутизация) → D-09
- Archive guard: A (блокировать) → D-10

## Decisions Captured
См. 07-CONTEXT.md `<decisions>` — D-01…D-12.

## Deferred
- Богатый дашборд «Все филиалы» (занятость, вызовы)
- Пофилиальное меню на странице стола
- Перенос стола между филиалами как явная операция
