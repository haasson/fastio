---
phase: 07-dine-in-tables-branch-id
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-03"]
files_modified:
  - apps/admin/features/branches/api/branches.ts
  - apps/admin/features/branches/__tests__/branches.test.ts
  - apps/admin/pages/branches/index.vue
autonomous: true
requirements: [FIX-TABLE-BRANCH-GUARD, FIX-TABLE-BRANCH-NUMBERING]
must_haves:
  truths:
    - "branchesApi.hasTables возвращает true при наличии активных столов у филиала, false иначе"
    - "Попытка архивировать филиал со столами блокируется модалкой «у филиала есть столы»"
    - "Пер-филиальная нумерация (per_branch) применяется к dine_in автоматически после простановки branch_id — подтверждено verification, доп. кода нет"
  artifacts:
    - path: "apps/admin/features/branches/api/branches.ts"
      provides: "hasTables(sb, branchId)"
      contains: "hasTables"
    - path: "apps/admin/pages/branches/index.vue"
      provides: "hasTables-check в handleArchive"
      contains: "hasTables"
    - path: "apps/admin/features/branches/__tests__/branches.test.ts"
      provides: "unit-покрытие hasTables"
      contains: "hasTables"
  key_links:
    - from: "pages/branches/index.vue handleArchive"
      to: "branchesApi.hasTables"
      via: "Promise.all + блокирующая модалка"
      pattern: "hasTables"
---

<objective>
D-10: блокировать архивацию филиала со столами. Добавить `branchesApi.hasTables` по образцу `hasActiveAppointments`, подключить в `handleArchive` (Promise.all + блокирующая модалка). Подтвердить verification'ом, что пер-филиальная нумерация (D-11) применяется к dine_in автоматически после простановки branch_id — отдельного кода нет.

Purpose: Не дать осиротить столы при архивации филиала (FK ON DELETE SET NULL обнулил бы branch_id, столы потеряли бы маршрутизацию). Зафиксировать, что нумерация работает без доработок.
Output: archive guard + тест + verification-заметка по нумерации.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dine-in-tables-branch-id/07-CONTEXT.md
@.planning/phases/07-dine-in-tables-branch-id/07-RESEARCH.md
@.planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md

<interfaces>
<!-- branchesApi.hasActiveAppointments (branches.ts:217-235) — эталон: count-query + reportError + fail-safe return true. -->
<!-- branchesApi.hasActiveOrders/hasActiveReservations — те же сигнатуры (sb, branchId, tenantId). hasTables — только (sb, branchId), фильтр branch_id+is_active. -->
<!-- reportError импортирован в branches.ts (line 4) из @fastio/shared/observability — новых импортов нет. -->
<!-- pages/branches/index.vue handleArchive:144-197 — Promise.all([hasActiveOrders, hasActiveReservations, hasActiveAppointments]); серия if(hasX){ await confirm({title, message, confirmText:false, cancelText:'Понятно'}); return }. -->
<!-- useConfirm из @fastio/kit; confirm-шейп: { title, message, confirmText: false (блок), cancelText: 'Понятно' }. -->
<!-- useDatabase автоматически биндит hasTables через bindAll(branchesApi, sb) — useDatabase.ts менять НЕ нужно. -->
<!-- Нумерация: триггер set_order_number (migration 080) уже передаёт NEW.branch_id в generate_order_number; per_branch scope применяется автоматом. -->
<!-- Существующий тест-файл: apps/admin/features/branches/__tests__/branches.test.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: branchesApi.hasTables + unit-тест</name>
  <files>apps/admin/features/branches/api/branches.ts, apps/admin/features/branches/__tests__/branches.test.ts</files>
  <behavior>
    - Test 1: hasTables возвращает true, когда count активных столов с branch_id == filterId > 0.
    - Test 2: hasTables возвращает false, когда count == 0.
    - Test 3: при ошибке Supabase hasTables возвращает true (fail-safe — блокируем архивацию).
  </behavior>
  <read_first>
    - apps/admin/features/branches/api/branches.ts (self — hasActiveAppointments 217-235, импорт reportError line 4)
    - apps/admin/features/branches/__tests__/branches.test.ts (self — образец моков Supabase для существующих guard-тестов)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция branches.ts hasTables)
  </read_first>
  <action>
    В `branchesApi` после `hasActiveAppointments` добавить `async hasTables(sb, branchId: string): Promise<boolean>`: count-query `sb.from('tables').select('id', { count: 'exact', head: true }).eq('branch_id', branchId).eq('is_active', true)`. При `error` → `reportError(error, { context: 'branches.hasTables', branchId })` и `return true` (fail-safe, парность с hasActive*). Иначе `return (count ?? 0) > 0`. Сигнатура БЕЗ tenantId (фильтр по branch_id достаточен, как в RESEARCH).
    В `branches.test.ts` дописать 3 теста из behavior, мок Supabase-цепочки `.from().select().eq().eq()` возвращает `{ count, error }`.
  </action>
  <acceptance_criteria>
    - `grep -c "hasTables" apps/admin/features/branches/api/branches.ts` >= 1.
    - hasTables использует `reportError(error, { context: 'branches.hasTables', branchId })` на error-пути и `return true` (fail-safe).
    - `grep -c "hasTables" apps/admin/features/branches/__tests__/branches.test.ts` >= 1.
    - `pnpm vitest run apps/admin/features/branches/__tests__/branches.test.ts` — все тесты hasTables зелёные.
    - `pnpm --filter @fastio/admin typecheck` зелёный.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/admin/features/branches/__tests__/branches.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>hasTables реализован с reportError fail-safe; 3 теста зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: handleArchive guard в pages/branches/index.vue (D-10)</name>
  <files>apps/admin/pages/branches/index.vue</files>
  <read_first>
    - apps/admin/pages/branches/index.vue (self — handleArchive 144-197, существующие if-блоки guard'ов)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция branches/index.vue)
  </read_first>
  <action>
    В `handleArchive` добавить `api.branches.hasTables(branch.id)` в существующий `Promise.all` (destructure `hasTables` четвёртым). После блоков hasOrders/hasReservations/hasAppointments добавить `if (hasTables) { await confirm({ title: 'Нельзя архивировать филиал', message: `У филиала «${branch.name}» есть столы. Перенесите или удалите их, а затем попробуйте снова.`, confirmText: false, cancelText: 'Понятно' }); return }`. Шейп confirm — точная копия существующих блокирующих гардов. НЕ менять финальный confirm архивации.
  </action>
  <acceptance_criteria>
    - `grep -c "hasTables" apps/admin/pages/branches/index.vue` >= 2 (в Promise.all + в if-блоке).
    - Promise.all содержит `api.branches.hasTables(branch.id)`.
    - if(hasTables)-блок использует `confirmText: false` (блокирующая модалка) + `return`.
    - `pnpm --filter @fastio/admin typecheck` зелёный.
    - `pnpm --filter @fastio/admin lint apps/admin/pages/branches/index.vue` без ошибок.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "hasTables" apps/admin/pages/branches/index.vue</automated>
  </verify>
  <done>Архивация филиала со столами блокируется модалкой в едином стиле с гардами по броням/записям.</done>
</task>

<task type="auto">
  <name>Task 3: Verification — пер-филиальная нумерация dine_in (D-11), доп. кода нет</name>
  <files>.planning/phases/07-dine-in-tables-branch-id/07-04-NUMBERING-VERIFICATION.md</files>
  <read_first>
    - supabase/migrations/080_order_numbering.sql (триггер set_order_number строки 127-144; generate_order_number per_branch 54-65)
    - apps/storefront/server/api/orders.post.ts (строка ~121: branch_id: branchId ?? null)
    - .planning/phases/07-dine-in-tables-branch-id/07-RESEARCH.md (Investigation Target 6)
  </read_first>
  <action>
    Создать verification-заметку. Подтвердить трассировкой по коду: (1) после Plan 02 dine_in заказ уходит с корректным `orders.branch_id`; (2) триггер `set_order_number` (BEFORE INSERT) вызывает `generate_order_number(tenant_id, config, NEW.branch_id)`; (3) при `scope='per_branch' AND branch_id IS NOT NULL` период = `branch_<id>` → счётчик и префикс филиала применяются автоматически. Вывод: отдельного кода нумерации НЕ требуется. Указать file:line цитаты. Если трассировка выявит, что триггер НЕ передаёт branch_id или scope игнорируется для dine_in — зафиксировать как gap и пометить план неполным.
  </action>
  <acceptance_criteria>
    - Файл `.planning/phases/07-dine-in-tables-branch-id/07-04-NUMBERING-VERIFICATION.md` существует.
    - Содержит file:line цитаты из migration 080 (set_order_number + generate_order_number) и orders.post.ts.
    - Явный вывод: «доп. кода нумерации не требуется» ИЛИ зафиксированный gap.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-dine-in-tables-branch-id/07-04-NUMBERING-VERIFICATION.md && grep -c "generate_order_number" .planning/phases/07-dine-in-tables-branch-id/07-04-NUMBERING-VERIFICATION.md</automated>
  </verify>
  <done>Подтверждено трассировкой: пер-филиальная нумерация применяется к dine_in автоматически после простановки branch_id; verification-заметка создана.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → branchesApi.hasTables | владелец архивирует филиал; guard проверяет связанные столы |
| FK ON DELETE SET NULL | последняя защита БД, если UI-guard обойдён |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-G1 | Denial of Service | архивация филиала со столами | mitigate | UI-guard блокирует архивацию; FK ON DELETE SET NULL (Plan 01) обнуляет branch_id если guard обойдён — столы не удаляются |
| T-07-G2 | Tampering | hasTables error path | mitigate | fail-safe `return true` при ошибке Supabase — при сбое архивация блокируется, а не разрешается по умолчанию |
| T-07-G3 | Information Disclosure | hasTables cross-branch | accept | count-query по branch_id проходит RLS (tenant_id), чужой филиал недостижим; reportError не логирует PII |
| T-07-SC | Tampering | npm installs | mitigate | новых пакетов нет |
</threat_model>

<verification>
- `pnpm vitest run apps/admin/features/branches/__tests__/branches.test.ts` — hasTables-тесты зелёные.
- `pnpm --filter @fastio/admin typecheck` + lint затронутых файлов зелёные.
- 07-04-NUMBERING-VERIFICATION.md подтверждает автоматизм per_branch нумерации.
- Ручная (manual-only): попытка архивации филиала со столами → блокирующая модалка.
</verification>

<success_criteria>
- hasTables реализован (fail-safe) + покрыт unit-тестом.
- handleArchive блокирует архивацию филиала со столами (D-10).
- Подтверждено: пер-филиальная нумерация dine_in работает автоматом, доп. кода нет.
</success_criteria>

<output>
Create `.planning/phases/07-dine-in-tables-branch-id/07-04-SUMMARY.md` when done
</output>
