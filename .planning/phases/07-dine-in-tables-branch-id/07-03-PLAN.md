---
phase: 07-dine-in-tables-branch-id
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/admin/features/tables/api/tables.ts
  - apps/admin/features/tables/__tests__/tables.test.ts
  - apps/admin/features/tables/components/TablesBranchSummary.vue
  - apps/admin/features/tables/composables/useTablesContext.ts
  - apps/admin/features/tables/feature.manifest.ts
  - apps/admin/pages/tables.vue
  - apps/admin/pages/tables/list.vue
  - apps/admin/pages/tables/layout.vue
autonomous: true
requirements: [FIX-TABLE-BRANCH-ADMIN]
must_haves:
  truths:
    - "Single-branch тенант (branches.length<=1): видит все столы, без саммари, UX как раньше (D-04)"
    - "Мультибранч + конкретный филиал: видит только столы этого филиала (D-05)"
    - "Мультибранч + «Все филиалы» (null): видит саммари-список филиалов со счётчиками, клик → setBranch (D-06)"
    - "Создание стола проставляет branch_id = выбранный/единственный филиал (D-02/D-03)"
    - "/tables/layout при «Все» (мультибранч) показывает саммари, не план зала (D-07)"
    - "Столы без branch_id на мультибранче показывают алерт «назначьте филиал» (D-08)"
    - "tablesApi.list(branchId=undefined) НЕ применяет branch-фильтр; tablesApi.list(branchId='X') применяет .eq('branch_id','X') — покрыто unit-тестом (Nyquist Wave 0)"
  artifacts:
    - path: "apps/admin/features/tables/api/tables.ts"
      provides: "mapTable.branchId + list(branchId?) + add(branchId)"
      contains: "branch_id"
    - path: "apps/admin/features/tables/__tests__/tables.test.ts"
      provides: "unit-тест branch-фильтра tablesApi.list (undefined → без фильтра; branchId → .eq)"
      contains: "branch_id"
    - path: "apps/admin/features/tables/composables/useTablesContext.ts"
      provides: "TablesContext расширен: showSummary, tableCountByBranch, hasUnassignedTables, branches"
      contains: "showSummary"
    - path: "apps/admin/features/tables/components/TablesBranchSummary.vue"
      provides: "саммари филиалов + счётчики + setBranch"
      contains: "setBranch"
    - path: "apps/admin/pages/tables.vue"
      provides: "effectiveBranchId + showSummary + branch-reactive load"
      contains: "effectiveBranchId"
  key_links:
    - from: "pages/tables.vue effectiveBranchId"
      to: "tablesApi.list"
      via: "load(id) → api.tables.list(id, effectiveBranchId.value)"
      pattern: "tables\\.list\\("
    - from: "TablesBranchSummary"
      to: "branchStore.setBranch"
      via: "клик по строке филиала"
      pattern: "setBranch"
    - from: "pages/tables.vue provide(TablesContextKey)"
      to: "useTablesContext.ts TablesContext type"
      via: "provide-объект должен соответствовать типу (typecheck-gate)"
      pattern: "showSummary"
---

<objective>
Сделать админскую страницу столов branch-scoped по образцу orders/kitchen (D-02..D-08): api-слой (mapTable.branchId, list с фильтром, add с branch_id) + unit-тест branch-фильтра (Nyquist Wave 0), расширение типа `TablesContext`, новый компонент саммари «Все филиалы», реактивность `pages/tables.vue` на глобальный селектор, развязка single-branch (D-04), саммари при «Все» (D-06/D-07), алерт незакреплённых столов.

Purpose: Чтобы владелец управлял столами в контексте филиала, а dine-in заказы получали корректный branch_id при создании стола.
Output: branch-aware admin tables feature с типобезопасным provide-контекстом и покрытым branch-фильтром.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dine-in-tables-branch-id/07-CONTEXT.md
@.planning/phases/07-dine-in-tables-branch-id/07-RESEARCH.md
@.planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md

<interfaces>
<!-- branchStore (apps/admin/shared/stores/branch.ts): branches: Ref<Branch[]>, currentBranchId: Ref<string|null> (null='Все'), setBranch(id), hasBranches. -->
<!-- Эталон branch-scope: apps/admin/pages/orders/index.vue:33-37 — const branchId = computed(() => branchStore.currentBranchId). -->
<!-- tablesApi.list:52 — list(sb, tenantId) БЕЗ branchId. mapTable:27-49 — НЕ маппит branchId. add:60 — insert БЕЗ branch_id. -->
<!-- query() helper (apps/admin/shared/utils/query.ts): await PromiseLike<{data,error}> → data | throw. Builder в тесте мокается как awaitable (.then). -->
<!-- pages/tables.vue: load(id) на строке ~158 вызывает api.tables.list(id); watch(tenantId) ~179; provide TablesContextKey ~488; upsertTableFromRealtime ~239. -->
<!-- pages/tables/list.vue: createTable ~119 вызывает api.tables.add(tenantId, { name }) БЕЗ branchId. -->
<!-- pages/tables/layout.vue: inject TablesContext, рендерит TablesCanvas. -->
<!-- TablesContext (apps/admin/features/tables/composables/useTablesContext.ts): тип provide-контракта. provide(...) в tables.vue ДОЛЖЕН соответствовать этому типу — иначе typecheck падает. -->
<!-- Аналог fluent-мока PostgREST builder для unit-теста list: apps/admin/features/branches/__tests__/branches.test.ts (makeBuilder/makeSb, chainSpy). -->
<!-- Аналог саммари-компонента: apps/admin/features/catalog/components/CategoryList.vue (UiCard + UiListRow + счётчик). -->
<!-- UI: UiCard, UiText, UiEmpty, UiSectionHeader, UiTag из @fastio/ui (читать исходник перед использованием пропсов). -->
<!-- useDatabase автоматически биндит tablesApi — менять useDatabase.ts НЕ нужно. -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (Wave 0): api-слой + unit-тест branch-фильтра list</name>
  <files>apps/admin/features/tables/api/tables.ts, apps/admin/features/tables/__tests__/tables.test.ts</files>
  <read_first>
    - apps/admin/features/tables/api/tables.ts (self — mapTable 27-49, list 52, add 60)
    - apps/admin/features/branches/api/branches.ts (эталон mapBranch + insert)
    - apps/admin/features/branches/__tests__/branches.test.ts (эталон fluent-мока builder: makeBuilder/makeSb/chainSpy, awaitable .then)
    - apps/admin/shared/utils/query.ts (query() awaitable-контракт {data,error})
    - apps/admin/pages/orders/index.vue (эталон branch-фильтра)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция tables/api/tables.ts)
    - .planning/phases/07-dine-in-tables-branch-id/07-VALIDATION.md (Wave 0: branch-фильтр list)
  </read_first>
  <behavior>
    Тесты (RED перед реализацией) в `apps/admin/features/tables/__tests__/tables.test.ts`:
    - Test 1 (mapper): `mapTable({ ...row, branch_id: 'br-1' }).branchId === 'br-1'`.
    - Test 2 (mapper null): `mapTable({ ...row, branch_id: null }).branchId === null`.
    - Test 3 (list без фильтра): `tablesApi.list(sb, 'tenant-1')` (branchId=undefined) → в chainSpy.eq есть `['tenant_id','tenant-1']` и `['is_active', true]`, но НЕТ ни одного `['branch_id', ...]`.
    - Test 4 (list с фильтром): `tablesApi.list(sb, 'tenant-1', 'br-1')` → chainSpy.eq содержит `['branch_id','br-1']`.
    - Test 5 (list null = без фильтра, D-04): `tablesApi.list(sb, 'tenant-1', null)` → НЕТ `['branch_id', ...]` в chainSpy.eq.
    Мок: fluent builder по образцу branches.test.ts (chainable `select/eq/order` → возвращают builder; `.then` резолвит `{ data: [], error: null }`; chainSpy ловит аргументы `eq`/`order`). Добавить `order` в список chainable-методов, т.к. list его вызывает.
  </behavior>
  <action>
    Реализация (GREEN после RED):
    В `mapTable` добавить `branchId: row.branch_id ?? null` сразу после `tenantId: row.tenant_id`.
    Изменить сигнатуру `list(sb, tenantId, branchId?: string | null)`: построить запрос пошагово (`let q = sb.from('tables').select('*').eq('tenant_id', tenantId).eq('is_active', true).order('created_at')`), затем при `branchId !== undefined && branchId !== null` → `q = q.eq('branch_id', branchId)`; передать `q` в `query(...)`. При undefined/null — без branch-фильтра (D-04 — все столы тенанта).
    В `add` в insert-payload добавить `branch_id: data.branchId ?? null`.
    НЕ менять updateMeta/setActive/setOpen/loadSums/archive. camelCase↔snake_case по образцу mapBranch.
    Запускать тест в run-режиме (без watch) до GREEN.
  </action>
  <acceptance_criteria>
    - Файл `apps/admin/features/tables/__tests__/tables.test.ts` существует, покрывает mapper.branchId + list (undefined/null → без branch-фильтра; конкретный branchId → `.eq('branch_id', X)`).
    - `pnpm vitest run apps/admin/features/tables/__tests__/tables.test.ts` зелёный (5 ассертов выше проходят).
    - `grep -c "branchId: row.branch_id" apps/admin/features/tables/api/tables.ts` == 1.
    - `list` имеет параметр `branchId?: string | null` и условие `.eq('branch_id', branchId)` под guard `!== undefined && !== null`.
    - `add` insert содержит `branch_id: data.branchId ?? null`.
    - `pnpm --filter @fastio/admin typecheck` не падает на tables.ts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run apps/admin/features/tables/__tests__/tables.test.ts</automated>
  </verify>
  <done>mapTable маппит branchId, list фильтрует опционально (покрыто тестом branch-фильтра), add пишет branch_id.</done>
</task>

<task type="auto">
  <name>Task 2: TablesContext type + TablesBranchSummary.vue (D-06) + feature.manifest dependsOn</name>
  <files>apps/admin/features/tables/composables/useTablesContext.ts, apps/admin/features/tables/components/TablesBranchSummary.vue, apps/admin/features/tables/feature.manifest.ts</files>
  <read_first>
    - apps/admin/features/tables/composables/useTablesContext.ts (self — тип TablesContext, импорты Ref/ComputedRef из vue, доменные типы из @fastio/shared)
    - apps/admin/features/catalog/components/CategoryList.vue (аналог: UiCard + строки + счётчик-мета)
    - apps/admin/shared/stores/branch.ts (setBranch, branches)
    - packages/ui/src/components/UiCard.vue, UiText.vue, UiEmpty.vue, UiSectionHeader.vue (пропсы перед использованием)
    - apps/admin/features/tables/feature.manifest.ts (self — массив dependsOn)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция TablesBranchSummary)
  </read_first>
  <action>
    useTablesContext.ts (BLOCKER 1 — typecheck-gate):
    - Расширить тип `TablesContext` новыми полями, которые провайдит tables.vue в Task 3:
      `showSummary: ComputedRef<boolean>`
      `tableCountByBranch: ComputedRef<Record<string, { total: number; open: number }>>`
      `hasUnassignedTables: ComputedRef<boolean>`
      `branches: Ref<Branch[]>`
    - `ComputedRef`/`Ref` уже импортированы из vue (строка 2) — проверить, добавить недостающее. Тип `Branch` импортировать из `@fastio/shared` (добавить в существующий `import type { ... } from '@fastio/shared'` на строке 3). Имена полей должны совпадать с ключами provide-объекта в Task 3 (showSummary, tableCountByBranch, hasUnassignedTables, branches).
    TablesBranchSummary.vue:
    Создать `TablesBranchSummary.vue`. Пропсы (type, не interface): `branches: Branch[]`, `tableCountByBranch: Record<string, { total: number; open: number }>`. Рендер: UiSectionHeader «Выберите филиал» + UiCard, внутри v-for по branches — строка с UiText(name) + UiText(счётчик `N столов · M открыто`), клик → `branchStore.setBranch(branch.id)`. UiEmpty при пустом списке. Импорты явные (auto-import off). Стили scoped, токены var(--space-*), var(--radius-*), var(--color-*), без БЭМ, корневой класс `.summary-root`.
    feature.manifest.ts:
    Добавить `'shared.stores.branch'` в массив `dependsOn`.
  </action>
  <acceptance_criteria>
    - `useTablesContext.ts` тип `TablesContext` содержит `showSummary`, `tableCountByBranch`, `hasUnassignedTables`, `branches` с указанными выше типами; `Branch` импортирован из `@fastio/shared`.
    - `grep -c "showSummary" apps/admin/features/tables/composables/useTablesContext.ts` >= 1.
    - Файл `apps/admin/features/tables/components/TablesBranchSummary.vue` существует.
    - `grep -c "setBranch" apps/admin/features/tables/components/TablesBranchSummary.vue` >= 1.
    - Компонент использует UiCard/UiText (не raw `<div class="card">`/`<p>`), импорты явные из @fastio/ui.
    - Нет хардкода цвет/отступ — только var(--…) токены.
    - `grep -c "shared.stores.branch" apps/admin/features/tables/feature.manifest.ts` == 1.
    - `pnpm --filter @fastio/admin typecheck` не падает.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "showSummary" apps/admin/features/tables/composables/useTablesContext.ts && test -f apps/admin/features/tables/components/TablesBranchSummary.vue && grep -c "setBranch" apps/admin/features/tables/components/TablesBranchSummary.vue</automated>
  </verify>
  <done>TablesContext расширен 4 полями (typecheck-совместимо с provide из Task 3); саммари-компонент со счётчиками и setBranch создан; manifest dependsOn обновлён.</done>
</task>

<task type="auto">
  <name>Task 3: pages/tables.vue — branch-реактивность, computed-поля, provide-контекст</name>
  <files>apps/admin/pages/tables.vue</files>
  <read_first>
    - apps/admin/pages/tables.vue (self — load 158, watch 179, upsertTableFromRealtime 239, provide 488)
    - apps/admin/features/tables/composables/useTablesContext.ts (self после Task 2 — расширенный тип TablesContext: provide должен ему соответствовать)
    - apps/admin/pages/orders/index.vue (эталон branchStore + computed)
    - apps/admin/shared/stores/branch.ts (currentBranchId, branches, setBranch)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция tables.vue)
  </read_first>
  <action>
    pages/tables.vue:
    - Импортировать useBranchStore, storeToRefs (явно, auto-import off); `const branchStore = useBranchStore()`, `const { branches } = storeToRefs(branchStore)`.
    - `effectiveBranchId = computed<string|undefined>`: если `branchStore.branches.length <= 1` → undefined (D-04, не фильтруем); иначе `branchStore.currentBranchId ?? undefined` (D-05; null → undefined → саммари-режим без branch-фильтра, D-06).
    - `showSummary = computed(() => branchStore.branches.length > 1 && branchStore.currentBranchId === null)` (D-06/D-07).
    - `tableCountByBranch = computed`: пройти `tables.value`, сгруппировать по branchId (пропуская столы без branchId), `{ total, open(isOpen) }`.
    - `hasUnassignedTables = computed(() => tables.value.some((t) => !t.branchId))` (D-08 алерт).
    - В `load(id)` передать `effectiveBranchId.value` вторым аргументом в `api.tables.list`.
    - Заменить `watch(tenantId, ...)` на `watch([tenantId, effectiveBranchId], ([id]) => { if (!id) return; void load(id) }, { immediate: true })` (Pitfall 2: guard `if (!id) return`).
    - В `upsertTableFromRealtime` добавить guard: если `effectiveBranchId.value && table.branchId && table.branchId !== effectiveBranchId.value` → return (не подмешивать чужой филиал).
    - В provide-объект TablesContextKey добавить `showSummary`, `tableCountByBranch`, `hasUnassignedTables`, `branches` — ключи и типы ДОЛЖНЫ совпадать с расширенным `TablesContext` из Task 2 (иначе typecheck падает).
    Все импорты явные (auto-import off). reportError не требуется (нет новых failure-путей; load уже обёрнут).
  </action>
  <acceptance_criteria>
    - `grep -c "effectiveBranchId" apps/admin/pages/tables.vue` >= 3 (computed + load + watch).
    - `grep -c "showSummary" apps/admin/pages/tables.vue` >= 1, `grep -c "hasUnassignedTables" apps/admin/pages/tables.vue` >= 1, `grep -c "tableCountByBranch" apps/admin/pages/tables.vue` >= 1.
    - `watch` включает `effectiveBranchId` в массив зависимостей.
    - provide-объект TablesContextKey содержит showSummary, tableCountByBranch, hasUnassignedTables, branches.
    - `pnpm --filter @fastio/admin typecheck` зелёный (provide соответствует TablesContext).
    - `pnpm --filter @fastio/admin lint apps/admin/pages/tables.vue` без ошибок.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "effectiveBranchId" apps/admin/pages/tables.vue && grep -c "tableCountByBranch" apps/admin/pages/tables.vue</automated>
  </verify>
  <done>Страница столов реагирует на глобальный селектор; single-branch без изменений (D-04); провайдит саммари-поля типобезопасно; realtime-guard от чужого филиала.</done>
</task>

<task type="auto">
  <name>Task 4: pages/tables/list.vue + layout.vue — саммари-рендер, создание стола, алерт</name>
  <files>apps/admin/pages/tables/list.vue, apps/admin/pages/tables/layout.vue</files>
  <read_first>
    - apps/admin/pages/tables/list.vue (self — createTable 119, template-структура)
    - apps/admin/pages/tables/layout.vue (self — inject context, шаблон)
    - apps/admin/features/tables/composables/useTablesContext.ts (расширенный TablesContext: ctx.showSummary/branches/tableCountByBranch/hasUnassignedTables)
    - apps/admin/features/tables/components/TablesBranchSummary.vue (пропсы branches + tableCountByBranch)
    - apps/admin/shared/stores/branch.ts (currentBranchId)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секции list.vue / layout.vue)
  </read_first>
  <action>
    list.vue:
    - В шаблоне обернуть существующий контент: `<template v-if="ctx.showSummary"><TablesBranchSummary :branches="ctx.branches" :table-count-by-branch="ctx.tableCountByBranch" /></template><template v-else>…существующий список…</template>`. Импортировать TablesBranchSummary явно (`~/features/tables/components/TablesBranchSummary.vue`).
    - `createTable`: резолв branchId — `ctx.branches.length === 1 ? ctx.branches[0].id : branchStore.currentBranchId` (при мультибранч+null кнопка скрыта саммари-режимом). Передать `branchId` в `api.tables.add(tenantId, { name, branchId })`. Доступ к branchStore через явный импорт useBranchStore.
    - Алерт: UiTag type="warning" с текстом про незакреплённые столы при `ctx.branches.length > 1 && ctx.hasUnassignedTables`.
    layout.vue:
    - В начале шаблона: `<template v-if="ctx.showSummary"><TablesBranchSummary :branches="ctx.branches" :table-count-by-branch="ctx.tableCountByBranch" /></template><template v-else>…существующий план зала…</template>` (D-07). Импортировать TablesBranchSummary явно.
    Все импорты явные (auto-import off).
  </action>
  <acceptance_criteria>
    - `grep -c "showSummary" apps/admin/pages/tables/list.vue` >= 1 и `grep -c "showSummary" apps/admin/pages/tables/layout.vue` >= 1.
    - `list.vue` createTable передаёт `branchId` в `api.tables.add` (grep `branchId` в createTable-области).
    - `list.vue` и `layout.vue` импортируют `TablesBranchSummary` явно (нет auto-import).
    - `list.vue` содержит UiTag-алерт под guard `ctx.branches.length > 1 && ctx.hasUnassignedTables`.
    - `pnpm --filter @fastio/admin typecheck` зелёный.
    - `pnpm --filter @fastio/admin lint apps/admin/pages/tables/list.vue apps/admin/pages/tables/layout.vue` без ошибок.
  </acceptance_criteria>
  <verify>
    <automated>grep -l "TablesBranchSummary" apps/admin/pages/tables/list.vue apps/admin/pages/tables/layout.vue && grep -c "showSummary" apps/admin/pages/tables/list.vue</automated>
  </verify>
  <done>Саммари при «Все» рендерится в list + layout (D-06/D-07); создание стола проставляет branch_id (D-02/D-03); алерт незакреплённых столов (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → tablesApi (Supabase) | владелец фильтрует/создаёт столы; RLS по tenant_id + has_permission('tables.manage') |
| branch-фильтр в list | branchId фильтрует UI; не должен протекать между филиалами |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-A1 | Information Disclosure | tablesApi.list branch filter | mitigate | list всегда фильтрует `.eq('tenant_id', tenantId)`; branchId — доп. UI-фильтр в рамках того же тенанта; RLS не ослаблена, cross-tenant невозможен; покрыто unit-тестом (branchId undefined → без фильтра, branchId='X' → `.eq('branch_id','X')`) |
| T-07-A2 | Tampering | createTable branch_id | mitigate | branch_id берётся из branchStore (глобальный селектор тенанта), не из произвольного ввода; insert проходит RLS has_permission |
| T-07-A3 | Spoofing | realtime upsert чужого филиала | accept/mitigate | realtime отдаёт только строки tenant_id юзера (RLS); добавлен guard в upsertTableFromRealtime от подмешивания чужого филиала в отфильтрованный список |
| T-07-SC | Tampering | npm installs | mitigate | новых пакетов нет |
</threat_model>

<verification>
- `pnpm vitest run apps/admin/features/tables/__tests__/tables.test.ts` зелёный (branch-фильтр list покрыт — Nyquist Wave 0).
- `pnpm --filter @fastio/admin typecheck` зелёный (provide соответствует расширенному TablesContext).
- `pnpm --filter @fastio/admin lint` на затронутых файлах без ошибок.
- Ручная (manual-only): single-branch → все столы без саммари; мультибранч+филиал → столы филиала; мультибранч+«Все» → саммари, клик → проваливается в столы филиала; новый стол получает branch_id.
</verification>

<success_criteria>
- D-02..D-08 реализованы: branch-aware list/add, саммари «Все», layout-guard, алерт незакреплённых, single-branch UX без изменений.
- TablesContext расширен и provide типобезопасен (typecheck зелёный).
- branch-фильтр list покрыт unit-тестом.
- TablesBranchSummary создан и подключён в list + layout.
- typecheck/lint/vitest зелёные.
</success_criteria>

<output>
Create `.planning/phases/07-dine-in-tables-branch-id/07-03-SUMMARY.md` when done
</output>
