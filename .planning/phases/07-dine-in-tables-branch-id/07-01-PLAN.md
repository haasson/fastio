---
phase: 07-dine-in-tables-branch-id
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/307_tables_branch_id.sql
  - packages/shared/src/types/table.ts
  - apps/admin/shared/data/database.types.ts
autonomous: false
requirements: [FIX-TABLE-BRANCH-SCHEMA]
must_haves:
  truths:
    - "Колонка tables.branch_id существует как nullable FK на branches(id) с ON DELETE SET NULL"
    - "Тенант с одним активным филиалом: все его столы привязаны к этому филиалу после миграции"
    - "Тенант с несколькими активными филиалами: branch_id столов остаётся NULL после миграции"
    - "Доменный тип Table содержит branchId, TableFormData содержит опциональный branchId"
  artifacts:
    - path: "supabase/migrations/307_tables_branch_id.sql"
      provides: "ADD COLUMN branch_id + индекс + умный бэкфилл D-08"
      contains: "ALTER TABLE tables"
    - path: "packages/shared/src/types/table.ts"
      provides: "branchId в Table и TableFormData"
      contains: "branchId"
    - path: "apps/admin/shared/data/database.types.ts"
      provides: "branch_id в tables Row/Insert/Update после регена"
      contains: "branch_id"
  key_links:
    - from: "tables.branch_id"
      to: "branches.id"
      via: "FK ON DELETE SET NULL"
      pattern: "REFERENCES branches\\(id\\) ON DELETE SET NULL"
---

<objective>
Заложить схему: nullable FK `tables.branch_id → branches(id) ON DELETE SET NULL` + умный бэкфилл (D-08) + индекс. Обновить доменные типы `Table`/`TableFormData` и регенерировать `database.types.ts`. Применить миграцию локально для честной верификации последующих волн.

Purpose: Без `branch_id` в схеме storefront-фикс (Plan 02) и admin-фильтрация (Plan 03) невозможны — это фундамент фазы.
Output: миграция 307, обновлённые shared-типы, реген database.types.ts, применённая локальная миграция.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dine-in-tables-branch-id/07-CONTEXT.md
@.planning/phases/07-dine-in-tables-branch-id/07-RESEARCH.md
@.planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md

<interfaces>
<!-- Текущий тип Table (packages/shared/src/types/table.ts:21-39): id, tenantId, name, isOpen, isActive, openedAt, createdAt, capacity, tags, positionX, positionY, shape, tableWidth, tableHeight, rotation, color, notes. НЕТ branchId. -->
<!-- TableFormData (table.ts:41-53): name, capacity?, tags?, notes?, shape?, tableWidth?, tableHeight?, positionX?, positionY?. НЕТ branchId. -->
<!-- Последняя проде-миграция: 306. Новая — 307. -->
<!-- branches schema: имеет колонки id, tenant_id, archived_at (бэкфилл считает только archived_at IS NULL). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция 307 — ADD COLUMN branch_id + индекс + бэкфилл D-08</name>
  <files>supabase/migrations/307_tables_branch_id.sql</files>
  <read_first>
    - supabase/migrations/029_orders_delivery_zone.sql (аналог nullable FK ON DELETE SET NULL)
    - supabase/migrations/255_backfill_unavailability_from_overrides.sql (аналог idempotent UPDATE-бэкфилла + header-комментарий)
    - supabase/migrations/306_kitchen_combo_items_guard.sql (последняя проде-миграция — подтвердить нумерацию)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция migration 307, готовый SQL)
  </read_first>
  <action>
    Создать `307_tables_branch_id.sql`. Header-комментарий по образцу migration 255: одна строка цели + корень бага (dine-in branch_id=null) + объяснение бэкфилла D-08 + рационал ON DELETE SET NULL.
    Тело: `ALTER TABLE tables ADD COLUMN branch_id UUID REFERENCES branches(id) ON DELETE SET NULL;` затем `CREATE INDEX idx_tables_branch_id ON tables(branch_id);`.
    Бэкфилл D-08: `UPDATE tables t SET branch_id = (SELECT id FROM branches b WHERE b.tenant_id = t.tenant_id AND b.archived_at IS NULL LIMIT 1) WHERE (SELECT COUNT(*) FROM branches b WHERE b.tenant_id = t.tenant_id AND b.archived_at IS NULL) = 1;` — привязывать столы ТОЛЬКО у тенантов с ровно одним активным филиалом. Мультибранч и без-филиала тенанты остаются NULL (D-08).
    НЕ менять RLS — политики на tables/table_calls фильтруют по tenant_id, branch_id их не затрагивает. НЕ использовать CONCURRENTLY (таблица малая). НЕ NOT NULL (D-01).
  </action>
  <acceptance_criteria>
    - Файл `supabase/migrations/307_tables_branch_id.sql` существует.
    - `grep -v '^--' supabase/migrations/307_tables_branch_id.sql | grep -c 'REFERENCES branches(id) ON DELETE SET NULL'` == 1.
    - `grep -v '^--' supabase/migrations/307_tables_branch_id.sql | grep -c 'CREATE INDEX idx_tables_branch_id'` == 1.
    - SQL содержит count-guard `) = 1` в бэкфилл-UPDATE (только single-branch тенанты).
    - Нет упоминаний NOT NULL, DROP, CASCADE применительно к branch_id.
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/307_tables_branch_id.sql && grep -v '^--' supabase/migrations/307_tables_branch_id.sql | grep -c 'ON DELETE SET NULL'</automated>
  </verify>
  <done>Миграция 307 создана: nullable FK branch_id + индекс + бэкфилл D-08 для single-branch тенантов, RLS не тронут.</done>
</task>

<task type="auto">
  <name>Task 2: Доменные типы — branchId в Table и TableFormData</name>
  <files>packages/shared/src/types/table.ts</files>
  <read_first>
    - packages/shared/src/types/table.ts (self — типы Table строки 21-39, TableFormData строки 41-53)
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция table.ts)
  </read_first>
  <action>
    В `type Table` добавить поле `branchId: string | null` сразу после `tenantId`. В `type TableFormData` добавить опциональное `branchId?: string | null`. Использовать `type` (не interface), camelCase. Остальные поля не трогать.
  </action>
  <acceptance_criteria>
    - `grep -c 'branchId' packages/shared/src/types/table.ts` >= 2.
    - В `type Table` есть строка `branchId: string | null`.
    - В `type TableFormData` есть строка `branchId?: string | null`.
    - `pnpm vitest run packages/shared 2>/dev/null || pnpm --filter @fastio/shared typecheck` проходит (типы валидны).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "branchId" packages/shared/src/types/table.ts</automated>
  </verify>
  <done>Table.branchId и TableFormData.branchId добавлены, тип `type` сохранён.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Применить миграцию 307 локально + реген database.types.ts</name>
  <files>apps/admin/shared/data/database.types.ts</files>
  <read_first>
    - .planning/phases/07-dine-in-tables-branch-id/07-PATTERNS.md (секция database.types.ts — команда регена)
    - supabase/migrations/307_tables_branch_id.sql (созданная в Task 1)
  </read_first>
  <what-built>
    Миграция 307 создана. Применяем её к ЛОКАЛЬНОЙ базе через docker exec psql (НЕ `supabase db reset` — запрещено, дропает базу). Прод накатывается автоматом через GH Actions при merge — вручную НЕ трогать. Затем регенерируем типы.
  </what-built>
  <how-to-verify>
    1. Применить миграцию (ОДНУ, не reset):
       `docker exec -i supabase_db_fastio psql -U postgres -d postgres < supabase/migrations/307_tables_branch_id.sql`
    2. Проверить колонку:
       `docker exec supabase_db_fastio psql -U postgres -d postgres -c "SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='tables' AND column_name='branch_id';"` → должна вернуть одну строку, is_nullable = YES.
    3. Проверить FK ON DELETE SET NULL:
       `docker exec supabase_db_fastio psql -U postgres -d postgres -c "SELECT confdeltype FROM pg_constraint WHERE conname LIKE '%tables_branch_id%';"` → confdeltype = 'n' (SET NULL).
    4. Реген типов:
       `supabase gen types typescript --local > apps/admin/shared/data/database.types.ts`
       Если CLI недоступен/ошибка — вручную добавить `branch_id: string | null` в tables Row/Insert/Update + Relationships-запись на branches (по образцу существующих FK в файле).
    5. Проверить: `grep -c "branch_id" apps/admin/shared/data/database.types.ts` >= 3 (Row/Insert/Update).
  </how-to-verify>
  <acceptance_criteria>
    - В локальной БД колонка tables.branch_id существует, nullable, FK confdeltype='n'.
    - `apps/admin/shared/data/database.types.ts` содержит `branch_id` в tables Row/Insert/Update (grep >= 3).
    - `pnpm --filter @fastio/admin typecheck` не падает на типе tables.
  </acceptance_criteria>
  <resume-signal>Type "applied" после успешного наката + регена, или опиши ошибку psql/gen types</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| миграция → прод БД | DDL применяется автоматом через GH Actions; ошибка миграции затрагивает все тенанты |
| бэкфилл → tenant-данные | UPDATE проставляет branch_id; неверный филиал = заказы уходят на чужую кухню |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-S1 | Tampering | бэкфилл UPDATE D-08 | mitigate | count-guard `) = 1` гарантирует привязку только при единственном активном филиале; мультибранч остаётся NULL — нет риска привязки к неверному филиалу |
| T-07-I1 | Information Disclosure | RLS на tables | accept | RLS фильтрует по tenant_id; branch_id не ослабляет изоляцию — политики не трогаются, cross-tenant утечка невозможна по построению |
| T-07-D1 | Denial of Service | ON DELETE SET NULL | mitigate | при архивации/удалении филиала стол не удаляется, branch_id обнуляется — данные столов не теряются |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | новых пакетов не добавляется — supply-chain вектор отсутствует |
</threat_model>

<verification>
- Миграция применена локально, колонка branch_id существует (psql проверка из Task 3).
- Бэкфилл: на single-branch тенанте `SELECT COUNT(*) FROM tables WHERE branch_id IS NOT NULL` > 0; на мультибранч — branch_id остаётся NULL.
- `grep "branchId" packages/shared/src/types/table.ts` присутствует в Table и TableFormData.
- `grep "branch_id" apps/admin/shared/data/database.types.ts` в Row/Insert/Update.
</verification>

<success_criteria>
- tables.branch_id — nullable FK на branches(id) ON DELETE SET NULL, с индексом.
- Бэкфилл D-08 проставляет branch_id только тенантам с одним активным филиалом.
- Table.branchId и TableFormData.branchId в shared-типах.
- database.types.ts регенерирован с branch_id.
- RLS не изменён.
</success_criteria>

<output>
Create `.planning/phases/07-dine-in-tables-branch-id/07-01-SUMMARY.md` when done
</output>
